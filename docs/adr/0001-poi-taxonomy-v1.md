# ADR-0001. Таксономия POI v1

**Статус:** **принят к реализации** 11.08.2026. Реестр, схемы и тесты в репозитории; контур проверен независимо — ajv 8.20.0 в корне, lock-файл согласован (`4044cef`), 16 наборов тестов и typecheck зелёные, lint без ошибок.
**Реализация:** шаги §13 идут по одному потребителю за коммит. Подключён потребитель № 1 — read-only loader и валидатор `src/lib/poi-taxonomy.ts` (`4912ad2`, поправки `9df9dbb` и `2c4395a`). Остальные потребители не подключены.
**Дата:** 12.08.2026, редакция 3 (после подключения потребителя № 1)
**Контекст:** разметка Осаки остановлена до принятия схемы — размеченный набор закрепил бы модель, которую мы собираемся менять. Решений там 141 в трёх очередях, см. §9.

---

## 1. Проблема

Одно поле `POI Category (RU)` отвечает сразу на пять вопросов:

| Вопрос | Пример значения |
|---|---|
| Что это за сущность? | Буддийский храм |
| Пускать ли в POI вообще? | Ресторан, Японский отель |
| В какую таблицу направить? | Ресторан → ресурсы, не POI |
| Какова туристическая функция? | Смотровая площадка |
| Как показывать в продукте? | Знаковый вид |

Отсюда список, где рядом стоят храм, ресторан, отель, транспортный узел, СПА и «Знаковый вид».

### Расхождение уже произошло — измерено 11.08.2026

Источников правды не один, а **четыре**, и они расходятся:

| Источник | Значений | «Знаковый вид» |
|---|---|---|
| `src/lib/poi-canon.ts` → `POI_CATEGORIES_RU` | 18 | есть |
| `scripts/poi-portals/lib/enrich.mjs` → промпт классификатора | 17 | нет |
| `scripts/poi-portals/lib/airtable-store.mjs` → `CATEGORY_RU_TO_EN` | 17 | нет |
| опции самого поля в Airtable | **20** | есть |

Живая база отвечает четвёртым списком, и в нём есть значения, которых нет ни в одном модуле: **«Ландшафтный сад» (7 записей), «Историческая локация» (7), «Парк» (6), «Городская достопримечательность» (3)**.

Ещё по 466 записям: **136 (29 %) без категории вовсе**, у **67** проставлено две и больше.

---

## 2. Решение: четыре поля вместо одного

Первая редакция смешивала в `catalog_target` адрес назначения, вид сущности и исход приёма. Разведено:

| Поле | Отвечает на вопрос | Значения | Кто присваивает |
|---|---|---|---|
| `entity_kind` | Что найдено | см. §3 | классификатор предлагает |
| `intake_disposition` | Что делать с находкой | `route` · `exclude` · `needs_review` | **детерминированная политика** |
| `catalog_target` | В какую базу | `poi` · `hotel` · `restaurant` · `event` · `experience` · `null` | **детерминированная политика** |
| `poi_primary_type` | Что это за POI | 19 рабочих типов + резерв, §4 | классификатор предлагает, человек утверждает |

Плюс два независимых слоя: `facets` (§6) и `badges` (§7), и отношения `relations` (`part_of`, `experience_at`, `view_of`, `depart_from`, `branch_of`) — согласованы в хендоффе, здесь не переопределяются.

**Почему маршрут не выводится из `poi_primary_type`.** Все рабочие типы по определению относятся к POI: политике не из чего выбирать. Маршрут определяет `entity_kind`, который умеет сказать «это отель» и «это автовокзал».

`poi_primary_type` заполняется **только** при `catalog_target = poi`. В остальных случаях он `null`.

**Ключевое правило.** Модель классифицирует сущность. Маршрут выбирает политика: «очень известный ресторан» не становится POI оттого, что агент так решил.

Схема согласуется с внешними моделями: Schema.org допускает `TouristAttraction` как **дополнительный** тип к основному, а не вместо него; Overture разводит основной тип, иерархию и альтернативные категории. Иерархия и синонимы хранятся отдельно от кодов — обычный concept scheme в духе SKOS.

---

## 3. `entity_kind` и детерминированная политика маршрута

| `entity_kind` | `intake_disposition` | `catalog_target` | `exclude_reason` |
|---|---|---|---|
| `tourist_poi`, тип известен | `route` | `poi` | — |
| `tourist_poi`, тип не определён | `needs_review` | `null` | — |
| `tourist_poi`, резервный тип от модели | `needs_review` | `null` | — |
| `tourist_poi`, резервный тип от человека с заметкой | `route` | `poi` | — |
| `accommodation` | `route` | `hotel` | — |
| `food_service` | `route` | `restaurant` | — |
| `event` | `route` | `event` | — |
| `experience` | `route` | `experience` | — |
| `transport_infrastructure` | `exclude` | `null` | `infrastructure_not_catalogued` |
| `retail_shop` | `exclude` | `null` | `not_a_destination` |
| `service_business` | `exclude` | `null` | `not_a_destination` |
| `unknown` | `needs_review` | `null` | — |

Таблица и есть политика: у неё нет ветвлений и исключений, и агент её не переопределяет.

---

## 4. `poi_primary_type` — 20 кодов: 19 рабочих типов и один резерв

Машинный код **неизменен**. Подписи RU/EN меняются **только версионируемо** (§5).

| Группа | Код | RU | EN |
|---|---|---|---|
| религия | `shinto_shrine` | Синтоистское святилище | Shinto Shrine |
| | `buddhist_temple` | Буддийский храм | Buddhist Temple |
| | `religious_site_other` | Другой религиозный объект | Religious Site |
| культура | `museum` | Музей | Museum |
| | `art_venue` | Галерея / арт-пространство | Art Venue |
| | `performing_arts_venue` | Театр / культурная площадка | Performing Arts Venue |
| наследие | `historic_site` | Историческое место | Historic Site |
| | `architectural_landmark` | Архитектурный объект | Architectural Landmark |
| | `castle_fortification` | Замок / фортификация | Castle |
| | `monument_public_art` | Памятник / публичное искусство | Monument |
| природа | `park_garden` | Сад / парк | Park & Garden |
| | `natural_landmark` | Природная достопримечательность | Natural Landmark |
| | `viewpoint` | Смотровая точка | Viewpoint |
| город | `tourist_district` | Туристический район | Tourist District |
| | `market` | Рынок | Market |
| | `shopping_street` | Торговая улица / квартал | Shopping Street |
| досуг | `amusement_park` | Парк развлечений | Amusement Park |
| | `zoo_aquarium` | Зоопарк / аквариум | Zoo & Aquarium |
| купание | `public_onsen` | Общественный онсэн / купальня | Public Onsen |
| резерв | `other_tourist_poi` | Прочий туристический объект | Other Tourist POI |

В реестре 20 кодов: 19 рабочих типов и один резервный. Резервный полноценным типом не является — он означает «типа нет», и правило маршрута обходится с ним отдельно (§3).

`other_tourist_poi` — **только ручное решение с обязательной заметкой**, автоимпорт с ним запрещён.

Неизвестный код → `needs_review`. Агентам запрещено добавлять типы автоматически.

**Тип — слабый сигнал дедупликации, но не доказательство идентичности.** Совпадение типа повышает вес пары в матчере; расхождение типа не опровергает дубль (один и тот же объект люди классифицируют по-разному). Тождество по-прежнему устанавливают идентификаторы и расстояние.

---

## 5. Коды и подписи: разный режим изменения

| Что | Режим |
|---|---|
| код (`buddhist_temple`) | **неизменен навсегда** |
| подпись RU/EN | меняется **только с новой версией реестра** и записью в `labelHistory` |

Свободная правка подписей запрещена: отчёты и eval-фикстуры ссылаются на код, и если подпись под тем же кодом начнёт означать другое, старые наборы молча изменят смысл. Смена подписи — событие с версией, а не редактура строки.

### Версия и digest — разные вещи

Зафиксировано 12.08.2026, после того как в реестр добавили `languages`, `defaultLanguage` и `routingVocabulary` без повышения версии.

| Изменение | Версия | Digest |
|---|---|---|
| семантическое: новый код, изменённая подпись, изменённое правило маршрута | **обязана вырасти** | меняется |
| несемантическое: объявление того, что и так подразумевалось; переформатирование; правка примечания | остаётся прежней | **меняется всё равно** |

Digest считается по спецификации `raw-file-bytes/v1`: SHA-256 от точных байтов файла `config/poi-taxonomy.v1.json`, идентификатор — `sha256:<64 hex>`. Формат файла поэтому входит в контракт наравне с содержимым: UTF-8 без BOM, только LF, ровно один завершающий перевод строки — закреплено тестом. BOM или CRLF изменят digest, не изменив ни одного значения.

При импорте:

- версия не совпала — остановка;
- версия совпала, digest различается — остановка с причиной «реестр изменён без смены версии»;
- совпали оба значения — импорт разрешён.

Считать digest обязан один общий Node-модуль: отдельное хеширование `JSON.stringify` в HTML и в XLSX завело бы две реализации. Модуль появится вместе с первым артефактом, который digest записывает, — тогда у него сразу будет два потребителя и проверка сравнения. Внутрь самого реестра digest не пишется: получилась бы самоссылка.

Контрольное значение на 12.08.2026 — **сверка, а не запись в продуктовые артефакты**:
`sha256:57b9c2b6d95d8b79b8bc24cd8db4ccac1156a889d208967441511c01643f2305`.

---

## 6. `facets` v1 — только то, у чего нет своего поля

`hot_spring` (природный источник) · `onsen` (есть онсэн) · `spa` (есть СПА) · `night_view`

Из первой редакции **убраны**: `free_entry` (это билетная политика), `seasonal_bloom` (для этого есть `Season Window`), `indoor` (характеристика доступности). Facet не должен дублировать структурированное поле — иначе появляется второе место, где одно и то же может расходиться.

Facet — проверяемое по источнику свойство. На допуск не влияет и тип не заменяет.

---

## 7. `badges` — редакционный слой

`Знаковый вид` · `Выбор Jumbo` · `Сезонное место`

1. Бейдж **не тип и не facet**. Кинкакудзи — `buddhist_temple` + «Знаковый вид»; Shibuya Sky — `viewpoint` + тот же бейдж; красивый мост — `architectural_landmark` + тот же бейдж.
2. **Intake бейджи не присваивает.** Максимум — предлагает `badge_candidate` с обоснованием.
3. Присваивает редактор или отдельное правило публикации.
4. Не влияет на допуск, дедупликацию, маршрутизацию и тип.
5. Хранится в отдельном поле `POI Badges`.

«Сезонное место» как бейдж не подменяет `Season Window`: первое — решение показывать, второе — факт о работе объекта.

---

## 8. Онсэн и СПА — по сущности, а не по самоназванию

| Объект | `entity_kind` | `catalog_target` | Тип / признак |
|---|---|---|---|
| Природный источник | `tourist_poi` | `poi` | `natural_landmark` + `hot_spring` |
| Общественная купальня | `tourist_poi` | `poi` | `public_onsen` |
| Рёкан с онсэном | `accommodation` | `hotel` | признак `onsen` |
| Отель со СПА | `accommodation` | `hotel` | признак `spa` |
| Коммерческий day spa | `service_business` | `null` | `exclude` по умолчанию |

---

## 9. Проверка на живых данных: 141 решение в трёх очередях

Разметка Осаки — не один список. Из 381 импортной строки до решения человека доходит **141 запись в трёх очередях**:

| Очередь | Записей | Что решается |
|---|---|---|
| основная разметка | 132 | тип и фасеты по новой модели — таблица ниже |
| снято дедупом | 4 | коллизия с существующей записью: подтвердить дубль или развести |
| без географии | 5 | адрес не разобран, территория назначения не определена |

Редакция 2 считала только первую очередь. Остальные девять записей никуда не делись и требуют такого же ручного решения, поэтому счёт по Осаке — 141, а не 132. Оставшиеся 240 строк из 381 отсеяла география, и человека они не ждут.

Раскладка основной очереди:

| Сейчас | Записей | По новой модели |
|---|---|---|
| Синтоистское святилище | 29 | `poi` / `shinto_shrine` |
| Буддийский храм | 28 | `poi` / `buddhist_temple` |
| **Шоппинг** | **19** | 18 → `shopping_street` (все `商店街`), 1 → `market` (`大阪木津卸売市場`) |
| Ландшафтный сад / Парк | 18 | `poi` / `park_garden` |
| Архитектурный объект | 8 | `poi` / `architectural_landmark` |
| Арт-пространство / Галерея | 8 | `poi` / `art_venue` |
| Музей | 6 | `poi` / `museum` |
| **Транспортный узел** | **4** | 2 → `transport_infrastructure` / `exclude` (WILLER, OCAT), 1 → `experience` (корабль «Санта-Мария»), 1 → `poi` / `other_tourist_poi` (рыболовный пирс) |
| Смотровая площадка | 3 | `poi` / `viewpoint` |
| Парк развлечений | 3 | `poi` / `amusement_park` |
| Термальный Источник | 2 | по §8 |
| **Японский отель** | **2** | `hotel` — оба рёкана уходят из POI |
| Достопримечательность | 1 | `滝見小路` → `poi` / `tourist_district`, см. §10.3 |
| **СПА** | **1** | `OBP（大阪ビジネスパーク）` — бизнес-парк, не СПА |

Слой «Транспортный узел» раскладывается на **три разных исхода** — доказательство, что поле смешивало сущность с маршрутизацией.

---

## 10. Решения владельца по спорным случаям

### 10.1. Каталог инфраструктуры не создаём

Два автовокзала не оправдывают подсистему. Запись выглядит так:

```text
entity_kind        = transport_infrastructure
intake_disposition = exclude
catalog_target     = null
exclude_reason     = infrastructure_not_catalogued
```

Появится конкретный потребитель — построение маршрутов, трансферы, места встречи — каталог заводится отдельным ADR.

### 10.2. Рыболовный пирс — место, а не впечатление

У него нет отдельного поставщика, расписания сеанса и брони, значит это не `experience`:

```text
catalog_target   = poi
poi_primary_type = other_tourist_poi
note             = recreational fishing facility
```

Автоимпорт запрещён. Наберётся несколько таких объектов — обоснованно добавим `outdoor_recreation_site`.

### 10.3. `滝見小路` — не торговая улица

Основная функция не розничная торговля. Если пространство само входит в маршрут:

```text
catalog_target   = poi
poi_primary_type = tourist_district
relation         = part_of Umeda Sky Building
```

Отдельные рестораны остаются в ресторанном каталоге. Если туристической ценности у самого пространства нет — записи не нужно.

### 10.4. Синонимы — по явной карте, спорное вручную

| Старое значение | Записей | Решение |
|---|---|---|
| Ландшафтный сад | 7 | автоматически → `park_garden` |
| Парк | 6 | автоматически → `park_garden` |
| Историческая локация | 7 | автоматически → `historic_site` |
| **Городская достопримечательность** | **3** | **только вручную** |

Последнее слишком расплывчато: под ним может скрываться район, мост, памятник или смотровая площадка.

Миграция обязана начинаться с **read-only прогона** в формате `recordId → old → proposed → mode → reason`. Запись — только после проверки прогона человеком.

### 10.5. Порядок: сначала реестр

XLSX, собранный до реестра, немедленно стал бы **пятым** источником правды.

---

## 11. Единый машинный реестр

`config/poi-taxonomy.v1.json` — единственный источник, из которого строятся проверки Intake, промпт классификатора, выпадающие списки XLSX, переводы RU/EN и сверка опций Airtable. Читают его только через `src/lib/poi-taxonomy.ts`.

**Пример реестра из редакции 2 отсюда удалён.** Он был написан до реализации и разошёлся с ней в трёх местах: маршрутизация была вложена в `entityKinds`, хотя на деле это отдельная секция `routingPolicy`; фигурировало несуществующее поле `deprecated` вместо `legacyCategoryMigrations`; «храм» стоял точной подсказкой у типа, хотя на деле это `ambiguousHints` у ГРУППЫ `religion` и назначить тип он не может по определению. Документация не должна становиться второй машинной правдой, поэтому ниже — только перечень секций, а содержимое смотреть в файле.

| Секция | Что лежит | Раздел |
|---|---|---|
| `version` | версия реестра | §5 |
| `note` | назначение файла и режим правки | §5 |
| `languages`, `defaultLanguage` | объявленный набор языков подписей и язык по умолчанию | §5 |
| `routingVocabulary` | служебные слова политики: слово подстановки, два обобщённых состояния типа, допустимые источники классификации | §3 |
| `dispositions`, `catalogTargets` | допустимые исходы приёма и адреса каталогов | §2 |
| `entityKinds` | виды сущности с подписями | §3 |
| `excludeReasons` | причины отказа | §3 |
| `routingPolicy` | правила: вид сущности × состояние типа × источник классификации → исход, каталог, причина, требование заметки | §3 |
| `poiTypeGroups` | группы типов; у группы могут быть `ambiguousHints` — термины, уточняющие ГРУППУ и никогда не назначающие тип внутри неё | §4 |
| `poiPrimaryTypes` | 20 кодов с подписями, группой, признаком автоимпорта, подсказками и парой `include` / `exclude` | §4 |
| `facets` | независимый слой свойств | §6 |
| `badges` | редакционный слой | §7 |
| `legacyCategoryMigrations` | карта миграции старых значений `POI Category (RU)` | §10.4 |
| `labelHistory` | история переименования подписей под неизменным кодом | §5 |

Форму файла проверяет JSON Schema (`config/poi-taxonomy.schema.json`). Ссылочную целостность, согласованность языков и однозначность политики — `taxonomyProblems()` в loader'е: он перебирает все сочетания вида сущности, состояния типа и источника классификации и требует ровно одного подходящего правила на каждое. Проверка идёт при загрузке модуля, поэтому испорченный реестр роняет импорт, а не ждёт прогона тестов.

Поле называется `hints`, а не `synonyms`, и это не косметика: «храм» по-русски применяют и к синтоистскому святилищу. Точным соответствием такой термин быть не может — он подсказка классификатору и не более.

`include` / `exclude` пойдут в промпт вместо голого списка названий — из-за него ресторан `松阪牛焼肉M 法善寺横丁店` стал буддийским храмом по фрагменту `法善寺` в адресе.

---

## 12. Миграция «Знакового вида»

Измерено на живой базе: **16 записей**.

| Случай | Записей | Что делать |
|---|---|---|
| Есть и другая категория | 11 | перенести бейдж, тип остаётся |
| **«Знаковый вид» — единственная категория** | **5** | тип назначить вручную: `POI-000534`, `POI-000535`, `POI-000536`, `POI-000537`, `POI-000546` |

Пяти записям после переноса не останется ни одного типа, поэтому порядок жёсткий:

1. Завести поле `POI Badges`.
2. Перенести значение у всех 16.
3. Назначить тип пятерым вручную.
4. Только затем убрать опцию из поля категорий.

Отдельным долгом: **136 записей без категории** и **20 опций в Airtable против 18 в каноне**.

---

## 13. Порядок работ

| # | Шаг | Состояние |
|---|---|---|
| 1 | Исправить ADR | сделано, редакция 2 |
| 2 | Создать реестр и JSON Schema к нему | сделано — `a3f2d61`, `ac0f880` |
| 3 | Тесты реестра: уникальность кодов, ссылочная целостность, исчерпывающая таблица маршрутов | сделано — 86 проверок |
| 4 | Потребитель № 1: read-only loader и валидатор | сделано — `4912ad2`, `9df9dbb`, `2c4395a`, 144 проверки |
| 5 | Потребитель № 2: промпт классификатора и проверки Intake | не начат |
| 6 | Потребитель № 3: списки XLSX | не начат |
| 7 | Потребитель № 4: сверка опций Airtable | не начат |
| 8 | Миграция «Знакового вида» (§12) | не начата |
| 9 | Пересобрать XLSX из реестра | не начато |
| 10 | Разметить Осаку — 141 решение в трёх очередях (§9) | заморожено до шага 9 |
| 11 | **Только по размеченной выборке** менять правила классификатора | не начато |

Один потребитель — один коммит; поведение классификатора, Airtable и XLSX не меняется одновременно. После каждого потребителя — регрессионный тест, доказывающий, что локального списка больше нет.

Порядок между шагами 8, 9 и 10 может измениться: ранняя пересборка XLSX допустима, потому что файл размечает будущую схему и не обязан совпадать с текущими опциями Airtable. Безопасность обеспечивают версия таксономии и digest внутри XLSX (§5), запрет импорта до миграции Airtable и проверка версии при импорте. Перед пересборкой нужен новый dry-run Осаки с адресами — иначе колонка адреса снова окажется пустой.

---

## 14. Чего эта ADR не делает

Не меняет код, пороги классификатора и Intake. Не трогает существующие записи. Не описывает внутренние таксономии каталогов отелей, ресторанов, событий и впечатлений — только границу, по которой записи туда уходят.
