```
Status: proposed
Дата проверки: 5 сентября 2026 года
Canonical current status: docs/poi-intake/README.md
```

# Что осталось до готовности POI: граф зависимостей

Не план и не список задач. **Направленный ациклический граф**: узел можно начать, когда
закрыты те и только те узлы, без которых его нельзя сделать правильно. Номер слоя — имя,
а не очередь; внутри слоя и между слоями многое идёт параллельно.

Каждая строка проверена **по текущему коду, схеме живой базы и набору тестов**, а не
переписана из прежних документов. Там, где прежний документ расходится с кодом,
расхождение названо отдельно — см. «Что оказалось неверным в прежнем статусе».

**Числа блокеров здесь нет намеренно.** Назвать его можно только после доказательства
полноты списка; полнота слоя 4 сегодня не доказана, и об этом сказано в самом слое.

---

## Слой 1. Завершение кода парсера

| Узел | Состояние | Чем проверено | Зависит от |
|---|---|---|---|
| 1.1 Guarded production entrypoint исполнителя модели: fail-closed путь вызова | **реализован; принят независимым аудитом 03.09.2026** | `--model-execute` в `collect-pois.mjs` → `scripts/poi-portals/lib/model-run.mjs` → `executeModelPlan`; `tests/poi-model-entrypoint.mjs` — 448 проверок на композицию и отказы, `tests/poi-model-reachability.mjs` — 119 проверок, из них требование РОВНО ОДНОЙ production-ссылки на исполнитель. Платного запуска не было: policy источников запрещает обработку, разрешения владельца нет, переменная секрета не задана | **ничего**: сам путь построен и проверен без единого платного запуска |
| 1.2 Потребитель № 3: списки XLSX из реестра | не начат | в `scripts/` нет ни одного построителя XLSX; упоминания формата в `collect-pois.mjs` и `src/lib/poi-taxonomy.ts` — это чтение | 1.4 |
| 1.3 Потребитель № 4: сверка и миграция опций Airtable | не начат | в живой таблице `POI Category (RU)` — 24 значения, `POI Category (EN)` — 18; в реестре `poiPrimaryTypes` — 20 кодов | 3.1 |
| 1.4 Единая граница `resolvePlace` для всех путей создания | **закрыт**: оба пути идут через один `resolvePlace`; реализовано, закоммичено и **принято независимым аудитом 02.09.2026** вместе с P05 | `src/lib/poi-portal-place.ts` — граница портального пути; `collect-pois.mjs` наполняет `request.poi.resolved` только подтверждённой точкой резолвера; `tests/poi-portal-place.mjs` — 231 проверка, `tests/place-resolve.mjs` — 118 | ничего |
| 1.5 Запись `Coordinate Policy` для каждого нового POI (долг B) | **закрыт; принят владельцем 03.09.2026** (P07 3/3): машинная часть закрыта, авторизованный контур решений построен (узел 1.9) | `src/lib/poi-coordinate-policy.ts` — единственное вычисление; `poi-ingest.ts` пишет политику тем же объектом полей, что координаты; `tests/poi-coordinate-policy.mjs` — 84 проверки; матрица мутаций круга R5 — 28 из 28 убиты | 1.9 |
| 1.9 Авторизованный контур решений `representativePoint` и `notApplicable` | **закрыт; принят владельцем 03.09.2026** (P07.3 после пяти кругов аудита Codex; переоткрытие — только по новому production-контрпримеру) | форма — не карточка, а реестр под git: `config/poi-coordinate-decisions.v1.json` (`poi-coordinate-decision/v1`), читается `src/lib/poi-coordinate-decision.ts` статическим импортом по каноническому пути, запись с `subject` (город + имя), `decisionRef`, `approver`, `integrityDigest`; полномочие — процесс owner review (change-policy § 24); `ingestPoi` применяет решение только при совпадении предмета (`decisionSubjectMismatch`), `writeRun` консультирует реестр до резолвера; refresh-писатели подчинены политике (`src/lib/poi-coordinate-refresh.ts`: свежая предзаписная сверка, итог только чтением после PATCH, `recoveryRequired`); первое production-решение принято 05.09.2026 для «Музея Фудзита» и исполнено 06.09.2026; `tests/poi-coordinate-decision.mjs` 168, `tests/poi-coordinate-contract.mjs` 123, `tests/poi-coordinate-refresh.mjs` 302 | 4.9 |
| 1.6 Fail-closed `--existing` | **закрыт** | контракт вынесен в `scripts/poi-portals/lib/existing-file.mjs`; файл читается и проверяется в `main()` до модели, записи, мониторинга и создания артефакта; `tests/existing-file.mjs` — 105 проверок, из них композиционные (loadExistingBase → matchAgainstExisting) и процессные на настоящем CLI; матрица мутаций круга 10f-M — 17 из 17 убиты при зелёном самоконтроле раннера | — |
| 1.7 Ненулевой код возврата при перехваченной ошибке прогона | **закрыт** | обе ветки исполняются процессными тестами в `tests/existing-file.mjs`: перехваченная ошибка `main()` и `monitorFailure` — каждая на настоящем процессе, с проверкой побочных действий; мутации N11 и N12 убиты своими утверждениями | — |
| 1.8 Ось `place_id` в портальном пути | **закрыт**: Place ID доезжает до полей записи, отказ именован и терминален; **принята вместе с P05 независимым аудитом 02.09.2026** | офлайн-приёмка на замороженном корпусе Осаки (`tests/poi-canary-osaka-offline.mjs`, 44 проверки) доводит Place ID и `exactObjectPoint` до полей; разовый живой canary 02.09.2026 подтвердил это на настоящей выдаче Google | 1.4 |

**Разделение 1.1 и 4.1 существенно.** Прежняя редакция держала в одном узле три разных
вещи: код вызова исполнителя, разрешение владельца на конкретный платный прогон и
разрешающую policy источника. Из-за этого выходило, что код нельзя написать, пока нет
разрешения, — а это неправда. Fail-closed entrypoint строится, покрывается тестами и
доказывается мутациями **без единого платного запуска**: он обязан отказывать при
отсутствующем разрешении, и именно этот отказ и проверяется. Разрешение конкретного
прогона — узел 4.1, и он держит только сам прогон.

**Почему 1.9 отдельный узел.** В первой редакции решение приходило тем же объектом
запроса, что и всё остальное, и портальный коллектор объявлял себя человеком: обе
«требующие решения» политики проставлялись машиной. Значение `source.kind` доказательством
тоже не является — его задаёт тот же вызывающий. Поэтому решения на общей границе нет
вовсе, и контур для них — самостоятельная работа с собственной формой доказательства.

**Обновление координат подчинено политике во всех писателях (10f-P, 03.09.2026).** Узлы 1.4/1.5 закрывают политику на ПРИЁМЕ; отдельно закрыто её сохранение при ОБНОВЛЕНИИ: ежедневный крон `api/cron/refresh-coords` и ручной `scripts/refresh-google-coords.mjs` читают `Coordinate Policy` через общий контракт `src/lib/poi-coordinate-refresh.ts`, строят план по свежему предзаписному чтению (`RECORD_ID()`) и не затирают решение владельца, принятое после первоначального чтения (`changedSinceRead`); повреждённая политика — `corruptPolicy`, не legacy. Это не новый узел готовности парсера, а закрытие долга по существующим писателям обновления; журналирование правок (узел 2.8) остаётся открытым. Открытая граница: legacy-запись с полной парой обновляется без явного решения владельца — до миграции 444 записей.

**Узлы 1.4 и 1.5 связаны в одну сторону.** Политика выводится из происхождения точки,
поэтому портальный путь, не проходящий `resolvePlace`, **останавливается**, а не создаёт
запись с пустой политикой. Это не регресс коллектора: с закрытием 1.4 остановка стала
не ценой долга, а штатным терминальным отказом — запись без опознанного места не
создаётся ни одним путём.

## Слой 2. Инфраструктура безопасности до первой production-записи

Слой неоднороден, и прежний общий заголовок «до production-записи» вводил в заблуждение:
часть узлов сторожит **создание**, часть — **обновление и удаление**, которых при
первом create-only импорте не будет вовсе. Поэтому у каждого узла своя колонка.

| Узел | Состояние | Блокирует первый create-only импорт | Чем проверено |
|---|---|---|---|
| 2.1 `run-manifest/v2`, ядро | **ПРИНЯТ независимым аудитом 04.09.2026** (10f-Q, P08.1 — засчитан; P08 `ACCEPTED` 3/3) | нет | `scripts/poi-portals/lib/run-manifest.mjs`: один детерминированный объект на прогон в `report.manifest` — режим, коммит кода, версия контракта приёма, версия и digest таксономии, политика матчера с отпечатком словарей, порталы, база, имена; строгая форма, пересчёт `manifestDigest`, версия проверяется ПЕРВОЙ, до состава полей (`tests/poi-run-manifest.mjs` 112). Код подписан четырьмя независимыми осями: коммит, `code.graph` — SHA-256 байтов модулей, достижимых импортами от точки входа (`poi-code-graph/v1`; правило по каталогам заменено обходом графа, 10f-Q R2), признак грязного дерева и `code.deps` (замок зависимостей). Снимок кода снимается дважды — до первого адаптера и перед gate — и расхождение отказывает прогон (10f-Q R3). Реестры — списком `registries`, подписанным по ЗНАЧЕНИЮ, которое загрузил процесс (`poi-registry-value/v1`): таксономия и решения владельца о координатах. Fingerprint схемы источника в манифест не входит — см. 2.2 |
| 2.2 Манифест: тождество входа | **ПРИНЯТО независимым аудитом 04.09.2026** (10f-Q, P08.2 — засчитан) | нет | тождество только digest'ами: SHA-256 сырых байтов выгрузки (`portals[].input.rawPayload`, до декодирования и разбора) и SHA-256 канонического набора кандидатов и строк без ключа (`portals[].input.canonical`, сравним при совпадении `adapter.version`); база и имена — SHA-256 файлов; сырая и каноническая оси сверяются НЕЗАВИСИМО (`inputDrift`/`canonicalDrift`, `inputIncomparable`/`canonicalIncomparable`) — совпадение одной не отменяет сравнение другой, и дрейф адаптера тоже ничего не отменяет: сырые байты сравниваются, канонический набор получает названную несравнимость (10f-Q R2); каждый компонент даёт свой именованный дрейф (`tests/poi-prewrite-gate.mjs` 208). Контрактный fingerprint схемы входа (ADR § 8.1) не реализован целиком: сырые заголовки и колонка идентификатора едут диагностикой в `meta`, но неоднозначность заголовков (точный повтор и эквивалентность после складывания ширины) — уже отказ выгрузки на границе адаптера (10f-Q R1) |
| 2.3 Манифест: снимок базы | контракт и хранилище готовы, экспортёра нет | **да** | `lib/base-snapshot.mjs`, `tests/base-snapshot.mjs` — 69 проверок; команды, порождающей файл снимка, в `package.json` нет |
| 2.4 Манифест: версия политики матчера | **политика версионирована и имеет отпечаток**, манифест-носителя (2.1) нет | **да** (вместе с 2.1) | `MATCHER_POLICY` (`src/lib/poi-matching.ts`, `poi-matcher-policy/v3`): 34 числовых параметра + глубоко замороженные словари `MATCHER_LEXICON` (родовые слова, классы, Поливанов, складывания) в одном SHA-256-отпечатке; отчёт коллектора несёт `matcherPolicy {version, digest}` (`collect-pois.mjs`), eval-фикстура привязана к версии, отпечатку и отпечатку словарей; полнота проверяется инвентарём по AST (`tests/poi-matching.mjs`); правило версии — change-policy § 23 (узел 4.4 ждёт принятия владельцем) |
| 2.5 Авторитетный baseline Осаки | не начат | **да** | снимок существует, но манифеста не имеет и в репозиторий не входит |
| 2.6 Classification eval и matching eval | matching eval построен, фикстура `complete`, **принят независимым аудитом Codex 04.09.2026** (P06.3 `ACCEPTED` 3/3); classification eval не начат | **да** (classification eval) | `tests/poi-matching-eval.mjs` (323) на `tests/fixtures/poi-matching-eval/v1.json`: 21 эмпирическая пара + 1 производный контроль (различимы в контракте и отчёте, `kindCoverage`/`metricsByKind`), привязка к `poi-matcher-policy/v3` по версии, отпечатку и отпечатку словарей, пять метрик ADR § 9.2 закреплены baseline, все шесть классов и три действия покрыты; фикстуры классификации в `tests/fixtures/` нет |
| 2.7 Pre-write drift gate | **ПРИНЯТ независимым аудитом 04.09.2026** (10f-Q, P08.3 — засчитан) | нет | `preWriteGate` вызывается в `collect-pois.mjs` до `writeRun`, до создания store и до резолвера; `--write` требует эталонный прогон (`--monitor`) и `--existing`; `BLOCK` даёт `write.refused = preWriteGate` и ненулевой код возврата. Порядок доказан хранилищем и резолвером, падающими на любом обращении; прежний порядок воспроизведён в `tmp/10f-q-r0-repro-p08-OLD-2026-09-04.json`. Gate и writer пользуются ОДНИМ прочитанным содержимым каждого входного файла: файлы читаются один раз до сборки манифеста, `writeRun` их не перечитывает — подмена между `PASS` и записью закрыта (10f-Q R1). Post-run `--monitor` остался отчётом и допуска не даёт |
| 2.8 Журнал редакторских правок | не начат | **нет** | восемь активных путей **обновления** POI не журналируют изменение; create-only импорт ни одного из них не исполняет. Журнал 10f-R (`poi-write-journal/v1`) сюда НЕ относится: он покрывает эффекты парсера (create и внутренний PATCH номера), а не редакторские правки |
| 2.10 Журнал и проверка исхода записи парсера | **ПРИНЯТ независимым аудитом Codex 05.09.2026** (10f-R закрыт по кругу R8; P09 — `ACCEPTED` 3/3, засчитан). После 10f-S счёт **30/30, 10 вех из 10**. Покрывает parser-owned create и внутренний PATCH номера; редакторские update-пути — узел 2.8, в P09 не входят | нет | `lib/write-journal.mjs` + `lib/verified-write.mjs`: намерение в NDJSON тремя шагами (`prepare` со входом, `create` с полной нагрузкой POST включая поля хранилища, `rename` перед каждым PATCH переименования) — хранилище объявляет каждый эффект и дожидается записи намерения (`onEffect`); через дескриптор и `fsync`, каждое вновь созданное имя каталога фиксируется в содержащем каталоге (полная цепочка, без `recursive mkdir`); исход — после ДВУХ независимых чтений мимо кэша (`readFreshBySourceKey` — содержание, `readFreshByPoiId` — постинвариант уникальности номера) со сверкой `recordId`, `POI ID` и каждого поля полного ожидаемого итога (нагрузка `create` с номером последнего `rename`); отказ внутренней проверки уникальности writer'а после POST — `unknown`, не `verified`; тождество только из базы; ответ чтения обязан нести запрошенный номер/ключ (R4); строка журнала подтверждается только после долговечной записи и `close()`, после любого отказа дозаписи журнал запечатывается — по любому брошенному значению, причина от общего безопасного описателя, код ошибки читается безопасно на всех путях open/write/sync/close и в цепочке каталогов (R6, R7); оборванный хвост незавершённого журнала распознаётся и называется, пустая физическая строка и байты после `runFinished` — повреждение (R4, R5, R6); момент в сверке полей календарно строгий (R5); отказавший PATCH с оставшимся старым номером — `mismatch`/`recoveryRequired`, хвост остановлен, код 1; закрытый список состояний, успех ровно один; грамматика одна у писателя (до записи) и читателя: одна попытка на ключ, `verified` только после эффекта, закрывающая строка выводится; закрытые схемы строк, закрытый способ проверки, календарно строгий момент; поздняя сверка `npm run poi:reconcile` разрешает intent-only по полному итогу двумя чтениями, без повтора, отката и удаления (`tests/poi-write-journal.mjs` 383, `tests/poi-verified-write.mjs` 279). Узел больше не ждёт аудита и первый create-only импорт не блокирует |
| 2.9 Guard на удаление POI | не начат | **нет** | `deleteAirtablePoi` шлёт `DELETE` без проверки входящих ссылок; create-only импорт не удаляет ничего |

**Почему 2.8 и 2.9 всё же обязательны — но не здесь.** Журнал правок держит первый
**update-run** (узел 5.5): как только после импорта начнётся редактирование, вопрос «кто
и когда изменил поле» станет неотвечаемым, и восстановить прошлое значение будет можно
только руками из истории ревизий Airtable. Guard на удаление держит момент, когда
появится первая **операция удаления** — в том числе разбор неудачного импорта, если его
решат чистить сносом записей. Ставить их в предусловия create-only импорта значит
задерживать импорт ради сторожей, которым при нём нечего сторожить.

Узел 2.6 — это и есть требование «правила меняются по размеченной выборке, а не по одному
примеру». Пока обеих фикстур нет, любая правка порогов сопоставления или правил
классификации обосновывается единичным наблюдением.
| 2.11 Разрешение владельца на ограниченную живую запись | **ПРИНЯТ независимым аудитом Codex 05.09.2026** вместе с P10 (10f-S закрыт; счёт 30/30, 10 вех из 10) | нет | `lib/write-approval.mjs` — контракт `poi-write-approval/v2`: версия читается собственным data-дескриптором ДО состава полей (документ прежней версии отвергается как прежняя версия, а не как повреждённый формат); разрешение называет допустимые `Source Key` поимённо и объявляет ВЕСЬ бюджет эффектов двумя числами (`maxCreates`, `maxRenames`; ноль переименований означает остановку до PATCH при коллизии номера); привязка — к БАЙТАМ эталонного отчёта (`--monitor`), поэтому подмена значимых входов вместе с пересборкой эталона не проходит; одноразовость — по неизменяемой идентичности разрешения (SHA-256 канонического значения), отметка `tmp/poi-write-approvals/used/<отпечаток>.json` создаётся эксклюзивно и долговечно (цепочка каталогов, `fsync` файла и каталога) ДО первого платного обращения, поэтому копия и переименование того же разрешения исполнению не подлежат; порядок сторожей — ворота дрейфа → применимость → отметка → резолвер и хранилище (`tests/poi-write-approval.mjs` 178). Исполнено ровно один раз 05.09.2026 — см. слой 5 |

## Слой 3. Миграции схемы

| Узел | Состояние | Чем проверено | Зависит от |
|---|---|---|---|
| 3.1 Поля taxonomy v2 в таблице POI | **закрыт 03.09.2026** | карточка `poi-taxonomy-schema-card/v5` (`5fbdc2bd…`) исполнена один раз по разрешению владельца: gate `verifiedSuccess`, в таблице POI 57 полей, четыре из них — `POI Type`, `POI Facets`, `Type Source`, `Taxonomy Version` (`tmp/10f-p-r5-schema-after-2026-09-03.json`, `check:poi` чист) | 4.5 |
| 3.2 Схема для `Coordinate Policy` | **не требуется** | поле уже существует как `singleSelect` ровно с тремя допустимыми значениями | — |
| 3.3 Приведение опций `POI Category` к реестру | не начат | 24 значения RU против 20 кодов реестра; legacy-мост представляет 9 кодов, 11 непредставимы | 3.1, 1.3 |

## Слой 4. Решения владельца и очереди данных

| Узел | Состояние | Чем проверено | Держит |
|---|---|---|---|
| 4.1 Разрешение владельца на **конкретный** платный модельный прогон | нет в репозитории | `tests/poi-model-reachability.mjs` | сам прогон, но **не** код узла 1.1 |
| 4.2 Policy источника, допускающая модельную обработку | запрещена у всех двенадцати источников | реестр источников | выполнение модельной обработки |
| 4.3 Retention-матрица | не принята: ADR-0002 в статусе `proposed` | заголовок ADR | 2.2, 2.3, режим baseline 2.5 |
| 4.4 Правило версии политики матчера | не принято | ADR § 12 | 2.4 и полноту атрибуции 2.5 |
| 4.5 Состав полей taxonomy v2 в Airtable | **принят 03.09.2026** | замороженная карточка R5 (`5fbdc2bd…`) и L3-разрешение владельца `owner/2026-09-03#10f-P-taxonomy-schema` (`tmp/10f-p-r5-approval-2026-09-03.json`); поля созданы — см. 3.1 | 3.1 |
| 4.9 Форма авторизации решения о политике координат | **принята владельцем 03.09.2026** вместе с P07 (change-policy § 24: файл реестра под git по каноническому пути, `integrityDigest` как контрольная сумма, а не подпись; полномочие — owner review); первый production-пример принят 05.09.2026 | контур есть (узел 1.9); решение для «Музея Фудзита» записано в реестр и исполнено 06.09.2026 | 1.9 |
| 4.6 141 решение по Осаке — **по новому артефакту, а не по нынешнему** | ни одного не принято | `docs/osaka-review-2026-08-11.xlsx`: лист «Разбор» — 132 строки, колонка «Решение» пуста во всех 132; «Снято дедупом» — 4 кандидата; «Без географии» — 5. Итого 132 + 4 + 5 = 141 | 5.2 |
| 4.7 Долг A: политика для 444 координатированных legacy-записей | не начат | baseline `10556767…`: 450 несистемных записей с координатами, у 444 политика пуста | предусловия 5.2 — см. оговорку |
| 4.10 Выбор места для «Музея Фудзита» (`bodik-osaka-tourism:OSAKA0000061`) | **закрыт 06.09.2026** | владелец выбрал точку Google `34.695007, 135.525015` с официальной страницы музея как `representativePoint`; решение `owner/2026-09-05#fujita-google-point` записано в реестр; создана и независимо перечитана одна запись `POI-000579` (`recnkVNqCFzWcTBHl`), Google Place ID не назначался; эффекты успешного прогона: 1 POST, 0 PATCH, 0 DELETE, 0 обращений к Google Places | ничего |
| 4.8 Редакционные очереди | открыты | baseline: 30 без русского описания, 13 с Draft в ожидании, 124 без legacy-категории RU и 126 без EN, 404 без `Source Key` | публикацию затронутых записей |

**Полнота этого слоя не доказана.** Узлы найдены обходом кода, схемы и документов, но
решения владельца по своей природе не обязаны оставлять след в репозитории, и отсутствие
файла не доказывает отсутствия решения. Поэтому общее число блокеров не называется.

**Оговорка по 4.7.** Правило раздела 2 канонизации — «отсутствующие координаты и
`Coordinate Policy` закрываются до первого write-run нового корпуса» — не отменяется и не
ослабляется. Закрытие долга B (узел 1.5) означает, что новых записей без политики больше
не появится; про 444 старые оно не говорит ничего. Если владелец решит, что долг A не
держит первый импорт, это отдельное решение владельца.

## Слой 5. Первый ограниченный импорт

Цепочка Осаки строго последовательна, и порядок в ней содержательный, а не
организационный.

| Узел | Состояние | Зависит от | Почему именно так |
|---|---|---|---|
| 5.1 **Пересборка артефакта разбора Осаки** из текущего реестра и текущей базы | не начат | 1.2, 1.3, 3.1, 3.3 | нынешний файл собран 11 августа 2026 года — до taxonomy v2, до координатных партий 10f-K и до закрытия долга B |
| 5.2 **Решения владельца по новому артефакту** (те самые 141, но уже по свежим строкам) | не начат | 5.1 | решения по устаревшему файлу — это решения о состоянии, которого уже нет; принятые по нему, они потребуют повторного разбора |
| 5.3 **Ограниченный write-run** на явно названном подмножестве | не начат — канареечный прогон P10 (05.09.2026) им НЕ является: три названные строки по разрешению владельца, а не корпус Осаки | 5.2, весь слой 1, узлы 2.1–2.7, 2.11, слой 3, решения 4.1–4.5 | — |
| 5.4 Независимая сверка результата импорта с намерением | не начат | 5.3, 2.3 | сверка нужна до, а не после включения расписания |
| 5.5 Первый update-run по итогам сверки | не начат | 5.4, **2.8** | здесь журнал редакторских правок становится обязательным |

**Канареечный прогон P10, 05.09.2026.** Один авторизованный `--write` по разрешению
`canary-osaka-v2-2026-09-05`: gate `PASS`, 3 обращения к Google Places, **2 POST, 0 PATCH,
0 DELETE**, код 0. Создано две записи — `POI-000577` «Художественный музей Накано́сима»
(`OSAKA0000060`) и `POI-000578` «Парк Нагаи» (`OSAKA0000143`), обе `verified` независимым
перечитыванием; «Музей Фудзита» остановлен неоднозначностью места (узел 4.10), «Парк
Уцубо» и ещё 112 строк — `notAllowed`. База 471 → 473, `Source Key` 56 → 58, прежние
записи не изменены; поздняя сверка — `verified` 2, `recoveryRequired` 0, код 0. Этим
закрыт критерий **P10**, а не узел 5.3: массовый импорт Осаки по-прежнему ждёт 5.1 и 5.2.

Прежняя редакция ставила решения по Осаке в слой 4 и объявляла, что они держат пересборку
XLSX. Это была перевёрнутая зависимость: пересборка ничего не ждёт от решений, а решения
без пересборки принимаются по устаревшим строкам. Порядок восстановлен: 5.1 → 5.2 → 5.3.

## Слой 6. Cron и мониторинг — только после импорта

| Узел | Состояние | Зависит от |
|---|---|---|
| 6.1 Calibration series на неизменной политике | не начат | 2.1, 2.5, 2.7 |
| 6.2 Вывод порогов на серии | не начат | 6.1 |
| 6.3 Независимая проверка порогов на прогонах вне серии | не начат | 6.2 |
| 6.4 Cron регулярного обхода порталов | не начат: расписания нет, запуск ручной | 5.4, 2.7 |
| 6.5 Shadow/challenger для политики | не начат | 2.6, 2.1 |
| 6.6 Guard на удаление как условие эксплуатации | не начат | **2.9** |

Порядок 5 → 6 не стилистический. Cron, включённый до сверки первого импорта, смешивает
новый корпус со старым долгом, и отчёт перестаёт отвечать, что появилось в этом прогоне.

---

## Что оказалось неверным в прежнем статусе

`docs/poi-intake/drift-roadmap.md` утверждал: «Ни один этап не начат». Проверка по коду
показала три расхождения, все в сторону «сделано больше, чем записано»:

1. **Подэтап 1а был сделан наполовину и закрыт 1 сентября 2026 года.** Ненулевой код
   возврата в коллекторе был написан давно; исполняющего теста на обе ветки не было ни
   одного. Теперь обе ветки проверяются на настоящем процессе.
2. **Подэтап 1б сделан наполовину.** Контракт снимка, его проверка и хранилище поверх
   проверенных строк реализованы и покрыты исполняющими проверками. Отсутствует ровно
   экспортёр — команда, порождающая файл снимка из живой базы.
3. **Чтение `Coordinate Policy` закрыто, а из записи закрыта машинная часть.**
   `exactObjectPoint` выводится из подтверждённой точки резолвера и пишется тем же
   объектом полей, что координаты. Весь долг B при этом остаётся **частичным** до узла
   1.9: `representativePoint` и `notApplicable` требуют авторизованного контура, которого
   нет, и потому не проставляются вовсе.

Подэтап 1в роадмапа (`fail-closed --existing`) закрыт 1 сентября 2026 года: недостоверный
вход роняет прогон именованной ошибкой до модели, записи, мониторинга и создания
артефакта, а нормальный путь корректного файла не изменился.

Остальные этапы роадмапа — 1г, 2, 3, 4, 5, 6, 7, 7а, 8, 9 — не начаты, и это
подтверждено отсутствием кода, а не отсутствием упоминаний.
