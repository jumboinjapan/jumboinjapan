```
Status: current
Owner: Eduard Revidovich
```

# POI Intake — каноническая точка входа

Этот файл отвечает на один вопрос: **что в подсистеме приёма POI работает сейчас, а что существует только на бумаге.** Если вы агент без исторического контекста — начните отсюда и не верьте другим документам про POI, пока не сверитесь с таблицей ниже.

Остальные документы по подсистеме описывают либо целевую архитектуру, либо состояние на свою дату. Ни один из них не переписывается при каждом изменении кода; этот — переписывается.

---

## 1. Зачем система

База POI — точки, из которых собираются маршруты и печатные программы. Всё остальное в проекте её только читает. Приём отвечает за то, чтобы в базу попадали записи, за которые не стыдно: с городом, координатами, непротиворечивой категорией и следом происхождения.

Источников поступления четыре: Telegram-бот владельца, коллектор с открытых японских порталов, админка и ручной ввод в интерфейсе Airtable. Первые три идут через код, последний — нет, и это известная дыра, а не недосмотр (см. §6).

---

## 2. Текущая стадия

**Критерий завершения POI-парсера выполнен: 30/30, принято 10 вех из 10 (`docs/poi-intake/parser-completion-ledger.md`).** Последняя веха P10 принята независимым аудитом Codex 05.09.2026 после одного авторизованного живого прогона: разрешение владельца `poi-write-approval/v2` `canary-osaka-v2-2026-09-05`, pre-write gate `PASS`, 3 обращения к Google Places, **2 POST, 0 PATCH, 0 DELETE**, код возврата 0. Создано две записи — `POI-000577` «Художественный музей Накано́сима» и `POI-000578` «Парк Нагаи», обе `verified` независимым перечитыванием базы; «Музей Фудзита» остановлен неоднозначностью места, «Парк Уцубо» и ещё 112 строк — `notAllowed`. База 471 → 473 записи, `Source Key` 56 → 58, ни одна прежняя запись не изменена. Это НЕ открывает массовый импорт Осаки: он ждёт пересборки артефакта разбора и решений владельца (DAG 5.1–5.2).

Идёт перевод классификации на машинный реестр таксономии — по одному потребителю за коммит. Пройдены реестр, loader и портальный классификатор; для потребителя № 2b собран офлайн-контур до границы транспорта включительно. Read-only discovery-контур Japan Guide реализован; **полный живой обход v3 выполнен 25.08.2026** — 208 целей, 1140 записей, 1293 обмена, ноль отказов, `complete: true`, записи в Airtable не было (подробности и артефакт — § 4). Не выполнялись cron и регулярный повтор обхода: расписания нет, запуск остаётся ручным. Production-исполнение модели **подключено к CLI** (`--model-execute`, пакет 10f-O, **принят независимым аудитом 03.09.2026**: P03 закрыт 3/3) и остаётся закрытым — но теперь воротами, а не отсутствием кода. Путь идёт через один оркестратор (`scripts/poi-portals/lib/model-run.mjs`), и ссылка на исполнитель из production-исходников ровно одна. Живой вызов сегодня не состоится по трём причинам: policy всех двенадцати источников запрещает модельную обработку и останавливает прогон на воротах P7, разрешения владельца в репозитории нет, а отдельная переменная `POI_MODEL_EXECUTION_OPENAI_API_KEY` не задана. До полного preflight не читаются ни секрет, ни журнал, ни провод; код возврата ворот доходит до процесса точным. Проверено исполнением: `tests/poi-model-entrypoint.mjs`. Массовый импорт Осаки **не открыт**: до него нужно завершить этот контур, привести текущую Airtable-базу к измеримому baseline, подключить оставшихся потребителей и мигрировать поля таксономии. Из этого списка на 1 сентября 2026 года закрыта одна позиция — координатная очередь: `coords_missing` и `coords_half` равны нулю, активных остановок без `POI ID` нет. Долг `Coordinate Policy` разделён. Запись политики для новых POI **реализована и принята владельцем 03.09.2026 (P07 закрыт 3/3 после пяти кругов независимого аудита Codex; переоткрытие — только по новому production-контрпримеру)**: машинная часть закрыта — без выводимой политики запись не создаётся ни одним путём; авторизованный контур для `representativePoint` и `notApplicable` построен — реестр решений владельца под git по каноническому пути (`config/poi-coordinate-decisions.v1.json`, статический импорт, `integrityDigest`, owner review как процесс полномочия; реестр пуст, ни одно решение ещё не принято), а все production-писатели координат — приём и оба refresh-пути — подчинены политике через общие контракты с предзаписной сверкой и итоговым чтением после PATCH (§ 4, строки 10f-P). Не построено: миграция legacy и журнал редакторских правок. **Миграция 444 координатированных legacy-записей не выполнена**: политика заполнена у 16 записей, координаты есть у 450. Свежие числа, правило вывода и видимая цена закрытия — `docs/poi-intake/airtable-canonicalization.md` § 7 и § 7.1; порядок оставшихся работ — `docs/poi-intake/poi-completion-dag.md`. **Портальный путь опознания места** (единый `resolvePlace` для Telegram и порталов, японский ключ поиска, Place ID и `exactObjectPoint` до полей записи, именованный терминальный отказ вместо записи без места) **принят независимым аудитом 02.09.2026**: P05 закрыт 3/3, расписания нет, в живую базу этим путём ничего не писалось. Приёмка — офлайн на замороженном корпусе Осаки (`tests/poi-canary-osaka-offline.mjs`) и разовое живое наблюдение 02.09.2026 по отдельному разрешению владельца с потолком в 20 обращений (`tmp/10f-n/live-canary-2026-09-02.json`).

Активная версия таксономии: **`poi-taxonomy/v2`**
Файл: `config/poi-taxonomy.v2.json`
Digest (`raw-file-bytes/v1`): `sha256:11bb3fef98373bd0029d75a97db7bba4dade5f919b56b910bcac2633646d2a6b`

`config/poi-taxonomy.v1.json` остаётся в репозитории неизменным как историческая версия. Его digest `sha256:57b9c2b6d95d8b79b8bc24cd8db4ccac1156a889d208967441511c01643f2305` закреплён тестом: v1 больше не читается кодом, но обязан лежать побайтно тем же.

---

## 3. Порядок чтения

Прогресс до завершения именно парсера, замороженный знаменатель и журнал изменения
области ведутся отдельно в `docs/poi-intake/parser-completion-ledger.md`. Архитектурный
DAG не заменяет этот реестр и не может молча увеличить число условий готовности.

| # | Документ | Что даёт | Статус |
|---|---|---|---|
| 1 | этот файл | что работает сейчас | current |
| 2 | `docs/poi-intake/change-policy.md` | что агенту можно и нельзя | current |
| 3 | `docs/poi-intake/runbook.md` | как запускать, ничего не сломав | current |
| 4 | `docs/poi-intake/airtable-canonicalization.md` | когда и как приводить живую базу к канону | current |
| 5 | `docs/adr/0001-poi-taxonomy-v1.md` | почему таксономия устроена так | current |
| 6 | `docs/poi-intake-contract.md` | действующая граница создания записи | current, transitional |
| 7 | `docs/poi-writers-registry.md` | кто вообще пишет в таблицу POI | current |
| 8 | `docs/adr/0002-poi-drift-control-v1.md` | как предлагается ловить дрейф данных и решений | **proposed** |
| 9 | `docs/poi-intake/drift-roadmap.md` | порядок внедрения контроля дрейфа | **proposed** |
| 10 | `docs/poi-standard.md` | что значит «запись готова» | transitional |
| 11 | `docs/handoff-poi-intake-v2-2026-08-11.md` | целевая архитектура V2 целиком | target |

Документы 12+ (`docs/poi-portal-collector.md`, `docs/poi-intake-agent.md`, прочие `handoff-*`) — исторические срезы. Читать как «так было на такую-то дату», не как инструкцию.

---

## 4. Реализовано / переходное / целевое / не начато

### Реализовано

| Что | Где | Подтверждено |
|---|---|---|
| Fail-closed `needs_review`: неопознанное останавливается, а не проходит | `src/lib/poi-ingest.ts` | `tests/poi-ingest.mjs` |
| Политика координат выводится машинно из подтверждённой точки резолвера и пишется тем же объектом полей, что координаты; без выводимой политики запись не создаётся ни одним путём | `src/lib/poi-coordinate-policy.ts`, `src/lib/poi-ingest.ts` | `tests/poi-coordinate-policy.mjs` |
| Intake-маркеры: у каждой созданной кодом записи есть происхождение и run id | `src/lib/poi-ingest.ts`, `scripts/check-poi-integrity.mjs` | `tests/poi-integrity.mjs` |
| Реестр писателей: все пути записи в таблицу POI перечислены | `docs/poi-writers-registry.md` | обход кода 11.08.2026 |
| Реестр таксономии v2 с тремя источниками классификации | `config/poi-taxonomy.v2.json` | `tests/poi-taxonomy.mjs` |
| Потребитель № 1: read-only loader и валидатор реестра | `src/lib/poi-taxonomy.ts` | `tests/poi-taxonomy-loader.mjs` |
| Потребитель № 2a: портальный классификатор на реестре | `scripts/poi-portals/lib/classification-contract.mjs`, `lib/scoring.mjs` | `tests/poi-classification-contract.mjs` |
| Происхождение назначает вызывающий код: правило → `rule`, модель → `model` | `classification-contract.mjs` | там же |
| Модель не выбирает маршрут: исход и каталог вычисляет только реестр | `classification-contract.mjs` | там же |
| Контракт профиля модельного провайдера `poi-model-provider-profile/v1`: строгая закрытая форма без поля для секретов, запрет wildcard и перечисленных плавающих псевдонимов версий, каноническое процентное кодирование endpoint и status path, чёрный список заголовков идемпотентности, отпечаток профиля. **Канонический профиль объявлен** (`openai-responses-luna@1.0.0`, модель `gpt-5.6-luna`): `resolveProviderProfile` разрешает эту пару и отказывает на любой другой. Исполнение модели по-прежнему невозможно, но держится это уже не пустотой реестра — см. § 7. Потребитель № 2b этим не завершён — см. «Не начато» | `scripts/poi-portals/lib/provider-profile.mjs` | `tests/poi-provider-profile.mjs` |
| Идентификатор записи запроса `poi-model-request-item/v1` и выборка кандидатов `poi-model-selection/v1`: HMAC-SHA-256 с ключом от отпечатка плана и разделителями полей, выборка покрывает план целиком и сверяется с ним тождеством, а не счётом. Ни провайдера, ни запроса это не создаёт — только детерминированные имена для будущего исполнения | `scripts/poi-portals/lib/model-plan.mjs`, `scripts/lib/byte-digest.mjs` | `tests/poi-model-plan.mjs` |
| Механика исполняемого плана `poi-model-plan/v2`: флаг `--model-provider-profile <id>@<version>` разрешает профиль **только** через канонический реестр и **до первого адаптера**; один парсер выбирает правила по версии контракта; каждый фрагмент v2 назван профилем, под который вычислена policy, и разрешение между профилями не переносится; `executionPermitted` истинно лишь при разрешённом профиле и `allowed` у **каждого** портала. v1 не изменилась ни на байт и остаётся диагностической. Канонический профиль разрешается и даёт построить план v2, но `executionPermitted` остаётся `false`: policy источников запрещает обработку, production-вызова исполнителя нет, разрешение владельца отсутствует | `scripts/poi-portals/lib/model-plan.mjs`, `scripts/poi-portals/collect-pois.mjs` | `tests/poi-model-plan.mjs`, `tests/poi-model-reachability.mjs` |
| Контракт разрешения владельца `poi-model-approval/v1`: восемнадцать ключей верхнего уровня, двенадцать потолков, привязка к КОНКРЕТНОМУ проверенному плану v2 по каждому продублированному полю, пересчёт `planArtifactDigest` и отпечатка выборки, `deleteAfter` ровно `validUntil` плюс тридцать суток. Денежной арифметики нет: потолки объявляются и проверяются на форму и отношения, стоимость по ним считает preflight. Чистый `buildModelApproval` полномочий не выдаёт: решение владельца (`decisionRef`, `approver`, сроки, потолки) приходит снаружи целиком, функция считает только производное от плана и его отпечаток, файлов и сети не касается | `scripts/poi-portals/lib/model-approval.mjs` | `tests/poi-model-approval.mjs` |
| Корни артефактов и журнал упреждающей записи `poi-model-execution/v1`: два независимых фиксированных корня в `tmp/`, физическая граница пути отдельно от свободы leaf, детерминированный `executionId` из отпечатков разрешения и плана. Одноразовость проверяема: существование каталога исполнения — свидетельство потребления, и оно сильнее срока. Журнал append-only, у каждой строки собственный отпечаток, порядок закрыт, читатель fail-closed и файл не чинит. Запись `opened` самодостаточна — после удаления плана кандидаты опознаются по ней. Сессия открывается только по сохранённому и заново проверенному разрешению | `scripts/poi-portals/lib/model-execution.mjs`, `execution-journal.mjs`, `approval-store.mjs`, `scripts/lib/path-boundary.mjs` | `tests/poi-execution-journal.mjs`, `tests/poi-path-boundary.mjs` |
| Execution-preflight: двенадцать ворот all-or-nothing до первого платного обращения. У каждых ворот один предмет: P0 — повтор того же детерминированного `executionId` и ничего больше; P1–P2 — план как артефакт и его исполнимость; P3 — статические authority-входы (форма профиля, его отпечаток и пара `id@version`, строгая таблица цен, её отпечаток и ТОЧНАЯ строка модели) целиком **до первого адаптера**; P4 — файл разрешения от имени до отпечатка; P5 — идентичность кода двумя чтениями вокруг источников; P6 — время; P7 — policy на момент прогона; P8–P10 — исчезнувший или подменённый портал, набор `sourceKey`, содержательный вход модели; P11 — денежный итог. **Порядок исполнения не равен порядку номеров**: `executionId` выводится из двух проверенных отпечатков, поэтому P0 идёт после P1–P4 и до них честно остаётся `notRun`. Статусы правдивы: `notRun` означает только «проверка не исполнялась», пройденные ворота в него не сбрасываются, а при позднем drift P5 = `failed`, P6–P10 остаются `passed`, P11 = `notRun`, и фаза отказа (`beforeSources` / `afterSources`) машинно различима. Ожидаемый drift классифицируется по типу и названному классу, а не сплошным `catch`: отсутствующее разрешение — штатный отказ с кодом возврата 11, а `EIO`, `EACCES`, `EPERM` и программная ошибка пробрасываются и в `approvalRejected` не превращаются. Денежные значения считаются в `BigInt` с округлением вверх; токенные потолки — точным `safeMul` в безопасном `Number`, и выход за него даёт `budgetUnprovable`, а не большое число. Отказ любых ворот отменяет весь прогон: портал или кандидат молча не исключается. Preflight ничего не создаёт и чужие журналы только читает. Профиль и таблица цены объявлены, но платный production-путь по-прежнему недостижим: исполнитель ни одним production-путём не вызывается, policy всех двенадцати источников запрещает обработку, а разрешения владельца в репозитории нет | `scripts/poi-portals/lib/execution-preflight.mjs`, `execution-cost.mjs`, `model-pricing.mjs`, `approval-store.mjs` | `tests/poi-execution-preflight.mjs` |
| Provider-neutral запрос `poi-model-request/v1` и офлайн-исполнитель: спецификация связывает проверенные план, approval, профиль, prompt, schema и один готовый `requestItemId`; `requestSpecDigest` даёт намерению идентичность, но не выдаёт себя за отпечаток транспортных байтов. Исполнитель проводит полный preflight, заранее строит все запросы и подготавливает исходящие байты каждого, затем получает время каждой стадии из инъецируемых часов, пишет `opened`, а на каждый элемент — `prepared` и `dispatching`, каждую со своим fsync, вызывает только инъецируемый транспорт, пропускает ответ только через `classifyModelResponse`, закрывает журнал и сохраняет производный `report.json` через физическую границу каталога исполнения без recursive mkdir. Отчёт повторно сверяет элементы, счётчики и исход. Невалидный содержательный ответ становится терминальным `rejected` без повтора; исключение транспорта или чужой `requestItemId` после `dispatching` оставляют журнал незакрытым для reconciliation. Сырой ответ не хранится. Модуль не импортирует HTTP-клиент, endpoint или секрет и по умолчанию не имеет транспорта; production-сеть всё ещё невозможна. Отчёт исполнения — `poi-model-execution-report/v2`: у каждого элемента появились `outboundBytesDigest` и `outboundBytes`. Разбор первой версии сохранён, версия выбирается по `contractVersion`, и каждая проверка отвергает чужую версию поимённо | `scripts/poi-portals/lib/model-request.mjs`, `model-executor.mjs`, `model-execution.mjs`, `execution-journal.mjs` | `tests/poi-model-executor.mjs`, `tests/poi-execution-journal.mjs` |
| Read-only сверка исполнения `poi-model-reconciliation/v1`: разбирает НЕЗАКРЫТЫЙ журнал и дописывает только локальные свидетельства. Ни провайдера, ни транспорта, ни адаптеров, ни Airtable, ни Intake, ни status-запроса; план и разрешение не нужны — кандидат опознаётся по самодостаточной записи `opened`. Журнал с одним `opened` закрывается как `abortedBeforeDispatch`, и доказательство здесь не решение владельца, а сам write-ahead журнал. Для неопределённого запроса решение владельца оформляется отдельным контрактом `poi-model-reconciliation-evidence/v1` с собственным `evidenceDigest`: закрытые списки вердикта и основания, обязательные `decisionRef` и `approver`, привязка к `executionId`, `requestItemId` и `requestSpecDigest` того самого `dispatching`, и `dispatching.at ≤ observedAt ≤` момент записи. `evidenceDigest` — SHA-256-отпечаток решения, дающий его идентичность; криптографической подписью владельца он не является и аутентификации решения не даёт. Свидетельство ничего не доказывает машинно — оно оформляет наблюдение владельца. Доказанное `no-charge` обязательно перед возможным повтором, но право на новый запрос отдельно ограничено approval и лимитами исполнения: элемент остаётся `unknown`, журнал открыт, код 40, а повторный `dispatching` грамматикой по-прежнему запрещён. Доказанное списание даёт терминальный `settled:lost` с `charged === true`; закрытие `withLoss` с кодом 60 происходит **только** когда все остальные элементы уже терминальны — если после потерянного остались неотправленные или неопределённые кандидаты, запись `settled:lost` появляется, а журнал остаётся открытым с кодом 40. Второго свидетельства, повторного `settled` и повторного закрытия не бывает; закрытый журнал на дозапись не открывается. Повреждённый журнал и оборванный хвост не меняются ни на байт, и системная ошибка чтения повреждением не объявляется | `scripts/poi-portals/lib/execution-reconciler.mjs`, `model-execution.mjs`, `execution-journal.mjs` | `tests/poi-model-reconciliation.mjs`, `tests/poi-execution-journal.mjs` |
| Протокол владения исполнением: журнал — цепочка сегментов `journal.<поколение>.e<N>.jsonl`, захват эпохи есть `ax`-создание её файла, номера эпох непрерывны, `EEXIST` — отказ, а не следующий номер. Эпоха завершается записью `released` либо `closed`; дозапись после незакрытой эпохи возможна только по решению владельца `poi-model-takeover/v1`, связанному отпечатками с точным хвостом (`fromRecordDigest`, `fromSegmentBytes`, `fromSegmentRawDigest`) и перечнем перешагнутых сегментов, при `tail.at ≤ observedAt ≤ claimed.at`. Основания перехвата закрыты тремя значениями; граница проверяет форму утверждения и его привязку, но не истинность. Fencing стоит до каждой семантической записи и повторно после её fsync; расхождение отравляет ручку навсегда. Чтение связывает имя сегмента с поколением и номером эпохи внутри его единственной записи `claimed`, исключает перешагнутые сегменты из логической цепочки независимо от поздней дозаписи и всегда сверяет отпечаток объявленного префикса. Записи за границей перехвата дают `journalForked` со своим кодом возврата и полным перечнем сиротских сегментов и байтов. Журналы прежнего формата читаются и сохраняют свой бизнес-итог, но дозаписи не принимают: протокол и право дозаписи — ортогональные поля, они не подменяют состояние, счётчики и исход. Поколение стоит в имени сегмента и повторяется отпечатком внутри записи `claimed`, поэтому переименование файла становится отказом, а не тихой сменой грамматики; сегменты разных поколений в одном каталоге исполнения — повреждение, а не смешанный журнал. **Файловый протокол не отменяет байтов, уже отданных чужим живым процессом в сокет** — их исключает только правдивое утверждение владельца | `scripts/poi-portals/lib/execution-journal.mjs`, `model-execution.mjs`, `execution-reconciler.mjs` | `tests/poi-execution-journal.mjs`, `tests/poi-model-reconciliation.mjs` |
| Канонический сериализатор `poi-model-serializer/v1` и граница транспорта `poi-model-transport/v1`: проверенная provider-neutral спецификация превращается в ОДИН подготовленный буфер. Реестр сериализаторов глубоко заморожен, разрешение строгое по `id@version`, отсутствие и неоднозначность — отказ; параметра `registry`, `descriptor` или `serialize` ни у одной публичной границы нет, test-only записи не предусмотрено. В хешируемую часть дескриптора входит `implementationDigest` — отпечаток исходного текста функции, собирающей тело, поэтому дескриптор, совпавший по полям при другом теле, невозможен. Оба значения — SHA-256-отпечатки, а не криптографические подписи: они дают идентичность и никого не аутентифицируют. Первый дескриптор — OpenAI Responses: `POST`, суффикс пути `/v1/responses`, `application/json`, заголовок `authorization` со схемой `Bearer`, `json-schema-strict`, `idempotencyPolicy: unsupported`, свои `maxOutboundBytes` и `maxResponseBytes`. Адрес дескриптор не объявляет: запрос уходит ровно по `profile.endpoint`, который целиком входит в `providerProfileDigest`, а дескриптор лишь требует, чтобы путь этого адреса оканчивался объявленным суффиксом. Тело: `model` из проверенного профиля, `input` массивом элементов-сообщений, Structured Outputs через `text.format`, `max_output_tokens` верхним уровнем и явное `store: false` — ответы Responses API хранятся по умолчанию, и молчание означало бы согласие. `temperature` и `top_p` не отправляются: официальная документация говорит, что их поддержка зависит от модели, а класса модели профиль не объявляет. Сериализация одна, копия байтов собственная и в собственном `ArrayBuffer`, отпечаток и длина считаются с копии, сверка с пределом — до открытия журнала; на границе транспорта длина и отпечаток пересчитываются заново. Журнал поколения `g2` требует записи `prepared` перед каждым `dispatching`: `requestItemId`, `requestSpecDigest`, `serializerDescriptorDigest`, `providerProfileDigest`, `outboundBytesDigest`, `outboundBytes` — самих байтов в журнале нет. Транспорт не импортирует ни `fetch`, ни `node:http`/`node:https`, ни SDK и не читает окружение: провод и резолвер учётных данных инъецируются В ИСПОЛНИТЕЛЬ, а транспорт вокруг них собирается им же — произвольной функции `transport` в публичном входе нет, и обойти границу вызывающему нечем. Резолвер получает перепроверенный профиль и разрешённый дескриптор и вызывается один раз перед отправкой; значение не сохраняется, не хешируется и не печатается. Между резолвером и проводом лежит `await`, поэтому вплотную к вызову провода повторяются обе проверки: владение эпохой (четвёртая проверка fencing, уже про сетевой эффект, а не про запись) и целостность буфера; после них до вызова нет ни одного `await`. Полномочие на эффект — метод ручки журнала, и его происхождение проверяется приватным реестром модуля журнала: хранилище должно быть создано настоящей фабрикой **и с настоящим `FILE_IO`**, ручка — этим хранилищем, для этого исполнения, этого поколения и всё ещё владеющей эпохой. Одной фабрики мало: `io` — законный параметр для отказных сценариев, и хранилище без настоящего `fsync` фабрика создаёт честно, но платного эффекта ему не даёт — иначе запись о намерении отправить могла бы не пережить обрыв, тогда как запрос уже уйдёт. Отказы ввода-вывода проверяются ниже границы исполнителя, на самой ручке журнала. Ту же проверку — включая требование настоящего `io` — делает и сверка, до первого чтения журнала: она дозаписывает свидетельство, потерю и закрытие, то есть решение владельца о потраченных деньгах. Подставное хранилище возвращало бы «применено», не тронув настоящий журнал; хранилище без fsync записало бы решение, которое может не пережить обрыв, а отчёт о нём уже выдан. Отказные сценарии с подменным вводом-выводом остаются законными ниже этой границы — на самой ручке журнала. Обёртка `{ ...handle, assertOwnedForEffect: async () => {} }` — другой объект, и до эффекта она не доходит; экспортируется только проверяющая функция, канала регистрации нет, а экспортированного символа было бы недостаточно — символ копируется, идентичность объекта нет. Предел принимаемых байт применяется ДО `JSON.parse`. Любой полученный HTTP-ответ, включая 4xx и 5xx, означает списание, и `charged: false` в результате транспорта невыразим: до ответа функция бросает исключение и результата не возвращает вовсе. Превышение предела ответа даёт терминальный `rejected`; отказ до ответа оставляет журнал незакрытым для сверки. Очищается не только исключение, брошенное самим callback, но и всё ленивое поведение возвращённого им объекта: getter'ы `status` и `body`, `Symbol.asyncIterator`, `iterator.next`, `step.done` и `step.value` — одна внешняя граница. Форма ответа спрашивается по дескрипторам свойств, а значения берутся из самих дескрипторов, поэтому accessor не исполняется вовсе; вся рефлексия — включая `getPrototypeOf`, `ownKeys` и `getOwnPropertyDescriptor`, которые `Proxy` тоже перехватывает, — стоит внутри очищаемой границы, и негодные значения в текст отказа не воспроизводятся. Статус подтверждается первым и отдельно: пока он не подтверждён, дефект — неопределённость; после подтверждения дефект формы или тела становится терминальным `charged: true`, а не превращает известное списание в «неизвестно». Ранний выход из чтения тела отменяет поток `return()` — предел, после которого мы перестали читать, но не перестали держать, пределом не является. Ответа отмены при этом не ждут: `return()` пишет чужая сторона, и она вправе вернуть промис, который не завершится никогда, — тогда уже известный терминальный вердикт повис бы навсегда. Отмену запрашивают и отпускают, а возможное отклонение гасят на месте, иначе оно всплыло бы как `unhandledRejection` уже после возврата вердикта. Класс исключения выбирает тот, кто его бросает, поэтому доказательством происхождения он не служит; собственные проверки формы стоят вне `catch` вокруг чужого вызова. После подтверждённого HTTP-статуса любой отказ тела — терминальный `charged: true`, а не неопределённость: ответ был, и деньги потрачены. Отказ ДО первого касания провода реконсиляцией не объявляется: итог берётся у самого журнала. Результат классификации в `g2` — `poi-model-execution/v1:classification-result/v2`: проблемы не строки, а ограниченные записи с видом, полной длиной, отпечатком полного текста, безопасным по UTF-8 началом и признаком обрезки; версия выбирается поколением журнала, а не набором ключей. Ни учётных данных, ни заголовков, ни полного адреса, ни сырого тела ответа, ни неограниченного модельного фрагмента в журнал, отчёт, текст исключения и диагностику не попадает | `scripts/poi-portals/lib/model-serializers.mjs`, `model-transport.mjs`, `model-executor.mjs`, `model-execution.mjs`, `execution-journal.mjs` | `tests/poi-model-serializers.mjs`, `tests/poi-model-transport.mjs`, `tests/poi-model-executor.mjs`, `tests/poi-execution-journal.mjs` |
| Девять терминальных исходов, сумма сходится с числом выгруженных записей | `scripts/poi-portals/collect-pois.mjs` | инвариант в коде |
| Режим `--base-snapshot`: контракт снимка проверяется до сети, идемпотентность по `Source Key` реально исполняется | `scripts/poi-portals/lib/base-snapshot.mjs` | `tests/base-snapshot.mjs` |
| Read-only discovery Japan Guide: read-only обход классифицирует страницы по структуре, сохраняет доказательства и строит версионированный снимок; неоднозначность, неподдержанная форма и потеря страницы дают явный неполный исход. **Текущая версия снимка и порядка — v3**: обход стал детерминированным BFS по графу коллекций, элемент порядка несёт роль (`poi` / `collection`), вложенные коллекции получили собственный список свидетельств, а узел с неустановленной ролью — четвёртый канал отказа `rejected.nodes`. Формат ЗАПИСИ при этом остался v2 и не менялся: на одну спецификацию записи ссылаются две политики снимка. У v3 число сетевых обменов выводится из состава снимка нижней границей, и отвергнутая ПОСЛЕ обмена страница входит в неё наравне с полученной. v1 и v2 заморожены и читаются только политикой своей версии; сравнение разных версий без мигратора запрещено, границу обменов задним числом к ним не применяют. Контур не вызывает модель, Intake или Airtable. **Живой полный обход v3 выполнен 25.08.2026**: 208 целей, 150 коллекций каталога, 1 вложенная, 58 прямых объектов, 1140 записей, 1293 обмена, ноль отказов, `complete: true`. Снимок закреплён в Git манифестом и gzip-артефактом, мониторинг `v3` → `v3` проверен на нём | `scripts/poi-portals/lib/japan-guide-html.mjs`, `discovery-contract.mjs`, `html-fetch.mjs`, `discovery-baseline.mjs` | `tests/poi-japan-guide-html.mjs`, `tests/poi-discovery-contract.mjs`, `tests/poi-discovery-baseline.mjs` |
| Baseline живого обхода лежит в Git ЦЕЛИКОМ: манифест плюс gzip-артефакт (683 037 байт против 6 507 108 распакованных). Контейнер собран вручную, чтобы в файл не попали метаданные машины: `gzipSync` пишет в шапку байт ОС и время. ВОСПРОИЗВОДИМОСТЬ БАЙТ МЕЖДУ СРЕДАМИ НЕ ОБЕЩАЕТСЯ — тело потока пишет `deflateRawSync`, и его вывод зависит от сборки zlib: аудит предъявил пересборку того же отчёта в 714 522 байта. Tracked-байты неизменяемы и доказываются ТРЕМЯ независимыми отпечатками — сжатого файла, распакованного отчёта и снимка, — а не пересжатием. Сверка читает артефакт из репозитория, распаковывает ПОД ПОТОЛКОМ, сверяет оба представления, выбирает портал ПОИМЁННО и проверяет снимок производственным контрактом ДО сравнения описаний. `poi-discovery-baseline/v1` описывает ровно один формат снимка — `poi-discovery-snapshot/v3`; метка `v1`, `v2` или `v4` отвергается поимённо, будущему формату нужен baseline `v2` либо мигратор. Сверяется и фактический формат ВНУТРИ архива — после контракта и до описания; та же проверка стоит на `describeDiscoveryBaseline`. Манифест валидируется точно, ДО ЛИСТА и по версии формата: у `scope`, `networkPolicy` и причин неполноты — точные ключи и области значений из политики версии, у отпечатков — синтаксис, у чисел — область, у пути артефакта — каноничность и принадлежность каталогу baseline; разрешённый путь проверяется второй раз файловой системой, поэтому символьная ссылка наружу не читается. Отсутствие артефакта — порча клона, а не «локально не скачан»: кода возврата «сверка не выполнялась» больше нет, `npm test` выполняет настоящую сверку в свежем клоне и ничего не берёт из `tmp/` | `docs/poi-intake/baselines/*.json`, `*.artifact.json.gz`, `scripts/poi-portals/verify-discovery-baseline.mjs`, `lib/discovery-baseline.mjs` | `tests/poi-discovery-baseline.mjs` |
| Read-only сопоставление корпуса обхода с таблицей POI: исходы разной силы, совпадение имени остаётся кандидатом и не превращается в связь, омоним уходит в неоднозначность. ПУБЛИЧНАЯ ГРАНИЦА ПРИНИМАЕТ БАЙТЫ выгрузки: она сама разбирает их, сама проверяет контракт `poi-airtable-export/v1` и сама считает SHA-256, поэтому отпечаток в отчёте — доказательство, а не объявление вызывающего; чистое ядро раскладки приватно. Снимок на той же границе проверяется ПОЛНЫМ производственным контрактом: публичная граница не может быть слабее скрипта, который её зовёт, иначе подделанная запись со старым `snapshotDigest` проходит и отпечаток публикуется как привязка. Каноническая пара база/таблица берётся из `src/lib/airtable-schema.ts` и публично не подменяется. Проекция выгрузки обязана доказать, что каждое используемое поле запрошено: на выгрузке `fields: ['poiId']` «не найдено» означало бы «мы не спрашивали». Области значений строк проверяются. `Source Key` индексируется ОДИН-КО-МНОГИМ: дубль ключа — дефект базы, о котором надо сказать. Любой непустой ключ, не согласный с кандидатом по `Website` — включая неизвестный обходу, исчезнувший и чужой портал, — даёт названный конфликт, а не откат к связи по адресу. Несколько записей на одну страницу — неоднозначность; обратной ветви нет, потому что контракт снимка требует выводимости `sourceKey` из адреса и одна страница даёт ровно один объект. Идентичность страницы разбирается СВОИМ строгим выражением, без `new URL` (тот чинит обратные слэши и лишние слэши раньше любой проверки): на вход принимается только примитивная строка и не подрезается; приводятся схема, регистр хоста, точный `www.` и хвостовой слэш, РЕГИСТР ПУТИ СОХРАНЯЕТСЯ, а обратный слэш, лишние слэши, запрос, фрагмент, учётные данные, порт, точечные сегменты, процентное кодирование и хост с хвостовой точкой идентичности не дают вовсе; автоматическая связь обязана быть один-к-одному, и это проверяется сторожем. Портал выбирается ПОИМЁННО. Скрипт не умеет писать в Airtable и не ходит в сеть | `scripts/poi-portals/match-discovery-to-airtable.mjs`, `lib/discovery-airtable-match.mjs` | `tests/poi-airtable-match.mjs` |
| Структурная идентичность составных POI: коллекция, объект и уточнение сравниваются раздельно, и спорный кандидат не участвует в автоматическом решении о дубле или `Parent POI`. При дедупе неопровергнутое расстоянием расхождение даёт `needs_review`, только если нет более сильного терминального исхода. При поиске родителя конфликт запрещает автоматическую связь и создание заглушки, но сам по себе не останавливает создание основной записи. Свидетельство сохраняется в структурированных причинах и, где применимо, в Notes, Telegram-отчёте и отчёте целостности. Прямой `Parent POI` снимает identity-предупреждение только для непосредственно связанной пары; общий родитель дубль не скрывает | `src/lib/poi-matching.ts`, `src/lib/poi-ingest.ts`, `src/lib/poi-intake.ts`, `scripts/check-poi-integrity.mjs` | `tests/poi-matching.mjs`, `tests/poi-ingest.mjs`, `tests/poi-intake.mjs`, `tests/poi-integrity.mjs` |
| **10f-R ЗАКРЫТ 05.09.2026: P02.2 и P09 ПРИНЯТЫ независимым аудитом Codex по кругу R8. P09 — `ACCEPTED` 3/3; итог 28/30, принято 9 вех из 10. Следующая и последняя веха — P10; пакет 10f-S не начат. Граница принятия: P09 покрывает parser-owned create и внутренний PATCH номера; редакторские update-пути (DAG 2.8) в P09 не входят. Ниже — как критерии были закрыты.** Воспроизведено до правки на production-композиции (`tmp/10f-r-repro-OLD-2026-09-05.log`): ошибка портала перехватывалась в отчёт и давала нулевой код возврата; отказ записи ПОСЛЕ состоявшегося POST схлопывал отчёт в `report.write = { error }` — созданная запись в отчёте не называлась; исход записи объявлялся по телу POST, а поиск по ключу отвечал из кэша, который наполнил тот же writer. Фактический граф эффектов от `poi:collect -- --write` — POST создания и ВНУТРЕННИЙ PATCH переименования при коллизии `POI ID` (его ответ не читался вовсе); оба живут в `createAirtablePoiStore`, который production собирает ровно в одном месте. Сделано: `poi-write-journal/v1` — намерение ДО первого сетевого вызова, исход ПОСЛЕ независимого чтения, эксклюзивный файл прогона, закрытый список состояний с единственным успехом; `withVerifiedWrites` — граница вокруг ХРАНИЛИЩА (общая точка приёма `ingestPoi` не тронута: ею пользуется и Telegram-бот), исход устанавливается `readFreshBySourceKey` мимо кэша, первый неподтверждённый исход останавливает хвост и сохраняет доказанный префикс поимённо; отказы прогона собраны в `runFailures` и доходят до кода возврата, а терминальный отказ отдельной POI по правилам приёма — нет; `npm run poi:reconcile` — поздняя сверка по журналу без повтора, отката и удаления; ответ PATCH переименования теперь проверяется. 15 мутаций: 14 убиты поимённо, одна названа избыточной и снята вместе с проверкой (`tmp/10f-r-mutations-2026-09-05.md`). **Аудит 05.09.2026 принял P02.2 и не принял P09: ложный `verified` (сравнивались только число записей и `recordId` — PATCH, заявивший `POI-000002` при `POI-000001` в базе, проходил, чужие поля тоже), `intent` без значений (intent-only журнал не восстанавливался), читатель журнала fail-open (одинокая строка `outcome verified` с `seq 999` принималась с кодом 0), `fsync` не вызывался. Круг R1 закрыл все четыре: `verified` требует того же `recordId`, того же `POI ID` и совпадения каждого обещанного поля с прочитанным (внутренний PATCH — фактический эффект); намерение несёт поля целиком и их digest, intent-only разрешается по нему; читатель — строгая грамматика (`runStarted` первой, сплошной `seq`, один `runId`, `outcome` только после своего `intent`, digest намерения сходится); каждая строка через дескриптор и `handle.sync()`. Ветка `storeRead` снята: за границу ставится только хранилище с независимым чтением. 13 мутаций R1 убиты поимённо (`tmp/10f-r-r1-mutations-2026-09-05.md`). Аудит R1 (05.09.2026) закрыл исходные четыре и открыл три: P0 — отказ внутреннего PATCH превращался в успех (намерение знало только вход `store.create`, а не `POI ID`/`Last Seeded At`/новый номер, добавляемые хранилищем; после исключения сравнение номера пропускалось — PATCH 500 с дублирующим `POI-000001` в базе давал `verified`); P1 — грамматика всё ещё fail-open (`verification: trusted-cache` и момент `2026-02-31T25:61:61.999Z` принимались с кодом 0); P1 — `fsync` файла не фиксирует запись о нём в каталоге. Круг R2 закрыл все три: хранилище объявляет границе каждый эффект с точной нагрузкой и дожидается намерения (`create` перед POST с полями хранилища, `rename` перед PATCH), ожидаемый итог — нагрузка `create` с номером последнего `rename`, и сверяется он целиком (PATCH 500 → `mismatch`/`recoveryRequired`, хвост остановлен, код 1; исключение после возможного эффекта — не успех, пока чтение не доказало полный итог); читатель fail-closed (закрытые схемы строк, закрытый способ проверки, календарно строгий момент, порядок шагов); каталоги синхронизируются при создании журнала. 17 мутаций R2 убиты поимённо, одна названа эквивалентной при одноэлементном списке способов (`tmp/10f-r-r2-mutations-2026-09-05.md`). Аудит R2 (05.09.2026) открыл четыре: постусловие уникальности `POI ID` отказало 503 после POST, а граница дала `verified` по совпадению полей; строка без `recordId` давала `verified` с id из заявки writer'а; грамматика принимала `verified` после одного `prepare`, две завершённые попытки одного ключа (сводка скрывала первый `mismatch`, `recoveryRequired 0`) и противоречивую `runFinished`; `recursive mkdir` на первом запуске оставлял промежуточные каталоги без `fsync`. Круг R3 закрыл все четыре: постинвариант уникальности граница устанавливает сама вторым чтением `readFreshByPoiId` (без него или при отказе — `unknown`, две записи с номером — `mismatch`), тождество только из базы (строка без id — `unknown`), одна грамматика у писателя и читателя (одна попытка на ключ, `verified` только после эффекта, `runFinished` выводится), цепочка долговечности по одному каталогу с `fsync` содержащего; хвост после неподтверждённого исхода не исполняется — проверено на двух записях через production. 19 мутаций R3 убиты поимённо, одна названа эквивалентной при контракте `verified` (`tmp/10f-r-r3-mutations-2026-09-05.md`). Аудит R3 (05.09.2026) открыл две: строка `outcome` подтверждалась грамматикой и `seq` до долговечной записи — отказ записи до байтов и последующий `finish()` давали seq 1,2,3,5 с `failed:false`, журнал непригоден для сверки; чтение по номеру, вернувшее верный `recordId` с `POI ID = POI-999999` или без номера, давало `verified`. Круг R4 закрыл обе: подтверждение строки только после `writeFile` → `sync` → синхронизации каталога, печать журнала после любого отказа дозаписи (`journal.sealed`, `evidence.journalSealed`), читатель распознаёт и называет оборванный хвост (`tornTail`) и отвергает обрывок посреди файла; ответ чтения обязан нести запрошенный номер (и ключ источника) — иначе `unknown`, фильтру сервера не верят. 17 мутаций R4 убиты поимённо, эквивалентных нет (`tmp/10f-r-r4-mutations-2026-09-05.md`). Аудит R4 (05.09.2026) открыл три: `close()` бросал после удачного `sync` — журнал не запечатывался, `finish()` дописывал строку с тем же seq, журнал нечитаем; `fieldEquals` через `Date.parse` равнял `2026-02-31` с `2026-03-03`; фрагмент после законного `runFinished` принимался как `tornTail`, `poi:reconcile` — код 0. Круг R5 закрыл все три: отказ `close()` — отказ дозаписи и печать, подтверждение строки после `close()`; момент — календарно строгий `parseMoment`, невозможный момент не равен ничему; байты после `runFinished` — повреждение, хвост допустим только у незавершённого журнала. 7 мутаций R5 убиты поимённо, одна названа эквивалентной (`tmp/10f-r-r5-mutations-2026-09-05.md`). Аудит R5 (05.09.2026) открыл две: `seal` сам рефлексировал брошенное значение (`instanceof`/`.message`/`String`) — отозванный Proxy из `close()` ронял саму печать, `sealed` оставался null, `finish()` дописывал строку с тем же seq; читатель отбрасывал пустые физические строки, из-за чего лишний `\n` после `runFinished` принимался, а `poi:reconcile` давал код 0. Круг R6 закрыл обе по свойству: причина печати строится общим безопасным описателем `describeThrownSafely`, признак отказа — факт, а не истинность значения (найдено сверх находок: `null`/`undefined`/`0` из `close()` проходили как успех), наружу уходит обычная `Error`; физическая грамматика NDJSON fail-closed — пустая строка в любом месте и любые байты после `runFinished` дают именованный отказ, `tornTail` остаётся только у незавершённого журнала. 10 мутаций R6 убиты поимённо, эквивалентных нет (`tmp/10f-r-r6-mutations-2026-09-05.md`). Аудит R6 (05.09.2026) открыл остаточную: `catch` в `append` и в цепочке каталогов читал `error?.code` — на отозванном Proxy из `io.open` это ловушка `get`, вторичный `TypeError`, `sealed` оставался null, `finish()` дописывал `runFinished`, и журнал `[1,2,3,4]` читался как ЗАКОНЧЕННЫЙ, хотя исход записан не был. Круг R7 закрыл весь класс: код ошибки читается безопасной процедурой `thrownCode` (собственный строковый дескриптор, рядом с описателем, без второй копии), опознаются ровно `ENOENT`/`ENOTDIR`/`EEXIST`, любое иное значение — обычная `Error` с устойчивым описанием, исходное наружу не выходит; штатные именные диагностики сохранены. 7 мутаций R7 убиты поимённо, эквивалентных нет (`tmp/10f-r-r7-mutations-2026-09-05.md`). Аудит R7 (05.09.2026) открыл узкую находку композиции диагностики: вложенная причина повторяла полный путь, и обрезка описателя (200 кодовых точек) на длинном каталоге съедала признаки «уже существует» и «дозапись в чужой журнал запрещена» — на macOS `node tests/poi-write-journal.mjs` давал 369/370 против 370/370 с `TMPDIR=/tmp`. Круг R8 закрыл её одной правкой: вложенное сообщение EEXIST не повторяет путь (файл называет внешний текст печати, обрезке не подверженный), признаки стоят первыми и от длины пути не зависят; добавлен детерминированный тест с намеренно длинным каталогом внутри обычного временного каталога сюиты — без опоры на TMPDIR; различающий мутант убит поимённо (`tmp/10f-r-r8-checkpoint-2026-09-05.md`). Аудит R8 (05.09.2026) ПРИНЯТ: P09 — `ACCEPTED` 3/3, счёт 28/30, принято 9 вех из 10; пакет 10f-R закрыт, 10f-S открыт** | `scripts/poi-portals/lib/write-journal.mjs`, `scripts/poi-portals/lib/verified-write.mjs`, `scripts/poi-portals/reconcile-writes.mjs`, `scripts/poi-portals/lib/airtable-store.mjs`, `scripts/poi-portals/collect-pois.mjs` | `tests/poi-write-journal.mjs` (383), `tests/poi-verified-write.mjs` (279), `tests/poi-coordinate-contract.mjs` (120) |
| **10f-S ЗАКРЫТ 05.09.2026: P10 ПРИНЯТ независимым аудитом Codex — `ACCEPTED` 3/3; итог 30/30, принято 10 вех из 10. Критерий завершения парсера выполнен целиком.** Предмет пакета — ограниченный живой запуск, который записывает ТОЛЬКО явно разрешённые результаты. Воспроизведено до правки (`tmp/10f-s-repro-OLD-2026-09-05.log`): состав эффектов определяли источник и позиционный `--limit`, ни одна строка не была названа заранее, максимум эффектов до исполнения был неизвестен; там же найден и исправлен дефект адаптера — `sourceUrl: undefined` убивал весь портал на настоящем корпусе BODIK. Сделано: контракт `poi-write-approval/v2` (`lib/write-approval.mjs`) — поимённый состав `Source Key`, весь бюджет эффектов двумя числами (`maxCreates`, `maxRenames`), привязка к БАЙТАМ эталонного отчёта, полный интервал действия `issuedAt <= now < expiresAt`, одноразовость по неизменяемой идентичности разрешения (отметка `used/<отпечаток>.json`, эксклюзивно и долговечно, до первого платного обращения), версия читается до состава полей; строка вне разрешения получает терминальный исход `notAllowed` и считается в раскладке; бюджет переименований доведён до границы записи — при исчерпании PATCH не отправляется вовсе. Круги: R0 и R1 не приняты (привязка к результату владельца, фактическая одноразовость, полный интервал, PATCH вне бюджета, подстановка разрешения в тестах вместо настоящего direct-entry; затем — одноразовость по имени файла вместо идентичности, недолговечная отметка и непеределанная версия контракта), R2 принят без блокирующих находок; мутации 9/9 и 10/10 убиты поимённо. **Живой прогон 05.09.2026 12:54:41Z, ровно один, код 0**: gate `PASS` (8 компонентов), Google Places 3 из 3, Airtable 2 POST / 0 PATCH / 0 DELETE; `POI-000577` (`OSAKA0000060`) и `POI-000578` (`OSAKA0000143`) — `verified`; `OSAKA0000061` «Музей Фудзита» — `placeUnresolved: ambiguous` (два кандидата «藤田美術館»), записи нет, долг DAG 4.10; `OSAKA0000186` «Парк Уцубо» и ещё 112 строк — `notAllowed`. Журнал 8 строк, `runFinished` `attempts: 2, failed: false`; поздняя сверка — `verified` 2, `recoveryRequired` 0, код 0. База 471 → 473, `Source Key` 56 → 58, прежние записи не изменены; `check:poi` — 473 точки, поломок нет | `scripts/poi-portals/lib/write-approval.mjs`, `scripts/poi-portals/collect-pois.mjs`, `scripts/poi-portals/lib/verified-write.mjs`, `scripts/poi-portals/lib/opendata-csv.mjs` | `tests/poi-write-approval.mjs` (178), `tests/poi-verified-write.mjs` (279), `tests/poi-prewrite-gate.mjs` (228), `tests/poi-portal-place.mjs` (316) |
| **10f-Q ЗАКРЫТ 04.09.2026: P01.2 и P08.1–P08.3 приняты независимым аудитом Codex. P08 — `ACCEPTED` 3/3; счёт 25/30, принято 7 вех из 10. Ниже — как критерии закрывались, включая промежуточные статусы кругов.** Ключ источника перестал зависеть от позиции строки: `opendata-csv/v2` строит `sourceKey` только из идентификатора самого источника и читает его сырым, а строка без ключа получает именованный терминальный исход (`sourceIdColumnMissing`, `sourceIdColumnAmbiguous`, `sourceIdEmpty`, `sourceIdInvalid`, `sourceKeyCollision`), считается в `finalTally.sourceKeyRefused` и целиком попадает в очередь отчёта; закон сохранения проверяется и на границе адаптера, и в коллекторе; сторож ключей действует для любого адаптера. Прежнее поведение воспроизведено на production-композиции (`tmp/10f-q-r0-repro-p01-OLD-2026-09-04.log`): после перестановки строк «通天閣» под ключом `row-1` совпадал с записью базы «Замок Осака» по `source_key` с уверенностью 1. Каждый прогон собирает `run-manifest/v1` (`report.manifest`): сырые байты входа каждого портала, канонический набор кандидатов и отказов, файлы `--existing`, `--base-snapshot` и `--names`, политика матчера с отпечатком словарей, реестр таксономии, версия контракта приёма, коммит кода — тождество только digest'ами. Pre-write drift gate исполняется **до store, до резолвера и до первой записи** и сверяет манифест с манифестом эталонного прогона (`--monitor`): любой дрейф — `BLOCK`, `write.refused = preWriteGate`, ненулевой код возврата; `--write` без эталона и без `--existing` не запускается. Прежний порядок (сравнение после `writeRun`) воспроизведён: `tmp/10f-q-r0-repro-p08-OLD-2026-09-04.json` — запись состоялась, дрейф назван после неё. **R1 (04.09.2026), четыре находки аудита Codex воспроизведены на текущих байтах и закрыты:** (1) TOCTOU файла `--names` после `PASS` — теперь каждый входной файл читается один раз в `main`, `writeRun` файлы не перечитывает и отвергает отсутствующее или чужое прочитанное содержимое (`tmp/10f-q-r1-repro-toctou-OLD-2026-09-04.log`); (2) изменённый production-код проходил при том же коммите — в манифест добавлен `code.tree` (`poi-code-tree/v1`, обход по правилу, 64 файла), коммит, отпечаток дерева и грязь сверяются тремя независимыми осями (`tmp/10f-q-r1-repro-code-canonical-OLD-2026-09-04.log`); (3) разные canonical при одинаковом raw проходили — оси входа разведены (`inputDrift`/`canonicalDrift`, `inputIncomparable`/`canonicalIncomparable`); (4) CSV с дублирующимися или эквивалентными по ширине ID-заголовками принимался — `assertHeaderRow` проверяет сырую строку заголовков до сборки объектов, адаптер `opendata-csv/v3` (`tmp/10f-q-r1-repro-headers-OLD-2026-09-04.log`). Дополнительно найдено разбором мутаций и исправлено: двухпроходный разбор по ФИЗИЧЕСКИМ строкам съедал заголовки у файла с пустой первой строкой и у заголовка с переносом внутри кавычек — вся выгрузка молча превращалась в `sourceIdColumnMissing`; строка заголовков берётся из первой записи. 12 мутаций убиты поимённо, одна названа эквивалентной (`tmp/10f-q-r1-mutations-2026-09-04.md`). **Аудит 04.09.2026: P01.2 принят; P08 не принят — `code.tree` не покрывает всю исполняемую цепочку (`src/lib/prefectures.ts`, `config/poi-coordinate-decisions.v1.json`), манифест не связывает фактически использованные реестры (реестр координат без digest-компонента; байты таксономии читаются отдельно и позднее решений), обязательный `code.tree` добавлен под прежней версией `run-manifest/v1`, `adapterDrift` маскирует сравнение raw и canonical. **Круг R2 (04.09.2026) закрыл все четыре: состав исполняемого кода ВЫВОДИТСЯ обходом графа импортов от точки входа (`code.graph`, `poi-code-graph/v1`) вместо правила по каталогам — `src/lib/prefectures.ts` и оба JSON-реестра теперь в отпечатке; голые спецификаторы подписаны замком `code.deps`; реестры таксономии и решений владельца о координатах подписаны ЗНАЧЕНИЕМ, которое загрузил процесс, списком `registries`; контракт поднят до `run-manifest/v2`, версия проверяется до состава полей; `adapterDrift` больше не отменяет сравнение сырых байтов и даёт названную `canonicalIncomparable`. 13 мутаций убиты поимённо, эквивалентных нет (`tmp/10f-q-r2-mutations-2026-09-04.md`). **Аудит 04.09.2026 принял P08.2 и P08.3 (граф совпал с независимым разбором TypeScript AST — 55/55 файлов) и оставил P08.1 открытым: граф снимался один раз и поздно; сканер пропускал исполняемый `import()` внутри шаблонной строки; байтовое тождество JSON в графе спорило с ценностным тождеством реестра. Круг R3 (04.09.2026) закрыл все три: снимков кода два — до первого адаптера и перед gate, расхождение отказывает прогон до store и резолвера; шаблонные строки разбираются (текст отбрасывается, `${…}` читается как код, вложенность любая), а полнота разбора проверяется по хвосту — незнакомая форма `import` отказывает прогон; байтовая и ценностная оси объявлены раздельно и больше не спорят. 10 мутаций убиты поимённо (`tmp/10f-q-r3-mutations-2026-09-04.md`). **Аудит 04.09.2026 принял P08.1: двойной снимок расположен до адаптеров и перед gate, расхождение останавливает writer; шаблонные интерполяции учитываются; неизвестные формы `import` закрыты fail-closed; разделение осей описано честно. Одна находка по тестам — контрпример подмены правил настоящий `src/lib/prefectures.ts` — исправлена переносом в песочницу-копию (`tests/support/code-swap-counterexample.mjs`), рабочее дерево контрпримером не затрагивается. P08 `ACCEPTED` 3/3, счёт 25/30, пакет 10f-Q закрыт**  | `scripts/poi-portals/lib/opendata-csv.mjs`, `scripts/poi-portals/lib/run-manifest.mjs`, `scripts/poi-portals/collect-pois.mjs` | `tests/opendata-csv.mjs` (86), `tests/poi-code-graph.mjs` (48), `tests/poi-run-manifest.mjs` (112), `tests/poi-prewrite-gate.mjs` (228), `tests/base-snapshot.mjs` (78), `tests/names-file.mjs` (29) |
| **10f-P, P06.3 — ПРИНЯТ независимым аудитом Codex 04.09.2026 (3/3, счёт 21/30); переоткрытие — только по новому контрпримеру. Ниже — как критерий закрывался, включая промежуточные статусы кругов.** Политика матчера `poi-matcher-policy/v1` — одна замороженная запись всех порогов гейта и пакетного дедупа, версия полем и SHA-256-отпечаток числами; `dedupe.mjs` читает пороги оттуда. Потребители версии: `policyVersion` в вердикте гейта; `report.matcherPolicy` в отчёте коллектора; `--monitor` разводит `changed` (данные) и `decisionDrift` (решение); eval-фикстура `tests/fixtures/poi-matching-eval/v1.json` привязана к версии и отпечатку — порог без новой версии роняет `npm test`. Семнадцать пар только из доказанных решений (аудит 06.08.2026, замер 11.08.2026, связи `Parent POI` живой базы); пять метрик ADR §9.2 закреплены baseline'ом (coverage 0,76, abstention 0,24, overall P/R/F1 0/0/0, exact 0,65 — числа честные). **Покрытие неполное**: классы `same_physical_place` и `co_located` и действие `needs_review` без доказанных пар; метки не выдумываются — фикстура несёт `status: BLOCKED_OWNER` и пять поимённых вопросов владельцу (Q1–Q5), eval требует, чтобы каждый пробел был назван, и отвергает статус `complete` при пробелах. **03.09.2026: владелец решил Q1–Q5 = A; решения внесены в фикстуру** (четыре новые пары с `owner_decision` и дословной цитатой, `fuji-5th-station`/`sendai-castle-ruins` → `needs_review`), baseline переснят штатно при той же политике v1 (21 пара, coverage 0,619, abstention 0,381). **`complete` пока недостижим**: Q4 = A оставил действие `duplicate` без единой доказанной пары (все подтверждённые дубли слиты, у вторых записей координат не было) — ошибка моего «минимального набора», не решения; фикстура `BLOCKED_OWNER` с одним вопросом Q6 (варианты E/C/D) — `docs/poi-intake/p06-matching-decisions-2026-09-03.md`. **04.09.2026: владелец выбрал Q6 = C (read-only поиск пары-дубля с координатами у обеих записей); поиск по артефактам и GET-скан таблицы POI (5 GET, 471 запись, 24 кандидата матчера: 0 с общим Place ID, 0 с равным именем, 3 blockingDuplicate — все связаны Parent POI) доказанной пары не дал — итог BLOCKED_EVIDENCE_GAP**, зафиксирован в том же документе и в вопросе Q6 фикстуры; Q4 = A, требование покрытия `duplicate`, политика v1 и живая база не менялись; автоматического перехода к E/D не было. **04.09.2026, Q6 = F:** добавлен производный конструированный контроль `fuji-5th-station-derived-control` (`kind = derived_control`, та же доказанная идентичность Фудзи, входящей записи подставлена точка POI-000239; `expectedDecision = duplicate`, `expectedVerdict = blocked_duplicate`, `historicalClaim = false`, ссылки на доказательство тождества, источник координаты и решение); эмпирическая пара Фудзи не изменена (Q4 = A); eval различает роды пар в контракте и отчёте (`kindCoverage`, `metricsByKind`: действие `duplicate` закрыто только контролем); baseline переснят штатно при той же политике v1 (22 пары = 21 эмпирическая + 1 контроль); фикстура `complete`, `tests/poi-matching-eval.mjs` 322; 13 мутаций фикстуры и мутация гео-ветки матчера убиты (`tmp/10f-p-p06-q6f-mutations-2026-09-04.md`). **P06.3 — READY_FOR_CODEX_AUDIT; 2/3 и счёт до аудита не повышаются.** 04.09.2026, инвентарь: воспроизведено, что порог «часть — целое» 0,95 и разрыв кандидатов в родители 0,1 (числа в теле `screenNewPoi`) меняли вердикт и выбор Parent POI при прежних версии и отпечатке; 15 параметров алгоритма перенесены в `MATCHER_POLICY` (34 числовых поля), версия `poi-matcher-policy/v2`, baseline переснят без единого сдвига; `tests/poi-matching.mjs` (368) снимает с AST все числовые литералы `poi-matching.ts` и `dedupe.mjs` — каждый либо поле политики, либо поимённое исключение (math / identity / report) с точным числом покрытий; 11 мутаций значений убиты отпечатком и поведением, 11 мутаций обхода — инвентарём (`tmp/10f-p-p06-inv-mutations-2026-09-04.md`); аудит, круг 2 (P1): нечисловые таблицы — родовые слова, классы, стоп-слова RU/EN, Поливанов, складывания букв/макронов, артикли, разделитель коллекции, скобки уточнения, гласные — собраны в `MATCHER_LEXICON`, их SHA-256 входит в отпечаток политики и отдельно в фикстуру (`lexiconDigest`); регулярки и множества строятся из записи, инвентарь таблиц и регулярок по AST (`tests/poi-matching.mjs` 431); эквивалентность до/после — 0 расхождений на 88 тыс. пар, baseline без сдвигов; 9 правок словаря убиты отпечатком (8 — и поведением), 6 обходов — инвентарём (`tmp/10f-p-p06-lex-mutations-2026-09-04.md`); DAG 2.1/2.4/2.6 приведены к фактическому состоянию (P2); круг 3: словари заморожены глубоко (`deepFreeze`, 98 узлов, 14 отвергнутых вложенных правок — `tests/poi-matching.mjs` 451), версия `poi-matcher-policy/v3` (состав политики изменился после зафиксированной v2), baseline без сдвигов, 5 мутаций заморозки/версии убиты. **Принято 04.09.2026: P06 `ACCEPTED` 3/3, счёт 21/30, принято 5 вех из 10; пакет 10f-P закрыт — P04, P06 и P07 приняты. 10f-Q не начат** | `src/lib/poi-matching.ts`, `scripts/poi-portals/lib/dedupe.mjs`, `scripts/poi-portals/collect-pois.mjs` | `tests/poi-matching-eval.mjs`, `tests/poi-matching.mjs`, `tests/poi-model-plan.mjs` |
| **10f-P, остаточный аудит P07.3 — ПРИНЯТ владельцем 03.09.2026 (3/3, счёт 20/30) после пяти кругов находок Codex; переоткрытие — только по новому production-контрпримеру.** Найден и закрыт настоящий дефект: refresh-писатели координат обходили политику. Ежедневный крон `src/app/api/cron/refresh-coords/route.ts` и ручной `scripts/refresh-google-coords.mjs` не читали `Coordinate Policy` и перезаписывали точкой Google любую запись с `place_id`: у `representativePoint` точка владельца молча заменялась центроидом Google, у `notApplicable` появлялись координаты, половина пары дописывалась (крон) либо объявлялась сдвигом в 11 016 км (скрипт). Контрпример воспроизведён на production-коде обоих путей без сети (`tmp/10f-p-p07-refresh-repro-*-OLD-2026-09-03.*`). Исправление — один контракт `src/lib/poi-coordinate-refresh.ts`: явная fail-closed таблица допуска (`exactObjectPoint` и legacy с полной парой — да; `representativePoint`, `notApplicable`, неизвестная политика, `exactObjectPoint` без пары, половина пары, legacy без координат — никогда), готовые `plan.fields` по каноническим ID (единственная копия ID), сдвиг > 3 км — только отметка; оба писателя запрашивают все поля контракта и шлют ровно `plan.fields`, не подлежащее обновлению не попадает в очередь и не спрашивается у Google. Политика legacy-записям НЕ назначается (refresh — не миграция). Состав писателей координат проверяется обходом всего `src/` и `scripts/` и сверкой с реестром писателей. Прежние свойства P07 сохранены (`tests/poi-coordinate-contract.mjs`, 114). Открытая граница: legacy с полной парой обновляется без явного решения владельца — до миграции 444 записей. Тот же тест на старом коде писателей проваливается на 37 проверках из 120 **Круг 2 (03.09.2026): три остаточных дефекта закрыты.** (1) Гонка: решение владельца (`representativePoint`/`notApplicable`), принятое после первоначального чтения писателя, старым планом затиралось — теперь план строится по СВЕЖЕМУ предзаписному чтению ровно тех записей, что идут в пачку (`filterByFormula` по `RECORD_ID()`), и при расхождении политики/пары/`place_id` со снимком запись откладывается (`changedSinceRead`), а не перезаписывается; недоступное свежее чтение — `freshReadUnavailable`. (2) Повреждённое (нестроковое, пробельное) значение `Coordinate Policy` теперь даёт `corruptPolicy`, а не `legacyGoogleCache`. (3) `hold` и неизменившаяся пара исполняются одной семантикой у обоих писателей — PATCH ровно `plan.fields`. Три случая воспроизведены на обоих настоящих писателях (`tmp/10f-p-p07-refresh2-repro-OLD/NEW-2026-09-03.*`); мутации обеих writer-границ и контракта — 14/14 жизнеспособных убиты. Граница честна: условной записи у Airtable нет, окно свежее-чтение→PATCH — один запрос, оно названо. **Круг 3 (03.09.2026, по находкам Codex): остаточная гонка обнаруживается, а не молчит.** Владелец, успевший изменить запись в окне между свежим чтением и PATCH, получал смешанное состояние (`representativePoint` + пара Google) при `ok: true`. Теперь после КАЖДОЙ пачки PATCH — независимое чтение и сверка итога (`classifyPatchOutcome`): политика и `place_id` как до записи, пара как в плане (`write`) или как до записи (`hold`); расхождение или недоступное чтение — `recoveryRequired` (`outcomeMismatch`/`outcomeUnverified`), крон отвечает `ok: false`, HTTP 500 и полным списком record ID в `требуетВосстановления`, скрипт — «ТРЕБУЕТСЯ ВОССТАНОВЛЕНИЕ» и код 1; ровно один PATCH на пачку — без повтора, отката и исправляющей записи. Все брошенные значения на границах чтения/записи (null, Symbol, бросающий getter, отозванный Proxy) описываются `describeThrownSafely` и писатель не покидают. Воспроизведено до/после на обоих настоящих писателях (`tmp/10f-p-p07-refresh3-repro-OLD/NEW-2026-09-03.log`); мутации контракта и обеих writer-границ — 13/13 убиты. Не устранено и названо: CAS у Airtable нет — гонка не исключена физически, но не может пройти молча. **Круг 4 (03.09.2026, по находкам Codex): два блокирующих дефекта закрыты.** P0 — после `recoveryRequired` в первой пачке оба писателя отправляли PATCH следующих пачек (11 записей: одиннадцатая уходила вторым PATCH) — теперь пачка с расхождением классифицируется целиком, а оставшиеся пачки не отправляются вовсе (крон — `остановлено` с хвостом POI ID; скрипт — «ОСТАНОВЛЕНО после пачки N»). P1 — `Coords Checked At` не сверялась: старая отметка после PATCH объявлялась подтверждённой (у `hold` это единственный записываемый эффект) — теперь сверяется каждое поле `plan.fields`, отметка по значению момента после строгого разбора ISO 8601 (`parseMoment`), писатель ставит отметку целыми секундами (`checkedAtMoment`); старая отметка — `outcomeMismatch [checkedAt]`. Воспроизведено до/после на обоих настоящих писателях (`tmp/10f-p-p07-refresh4-repro-OLD/NEW-2026-09-03.log`); мутации 11/11 (в т.ч. удаление остановки хвоста, исключение отметки из сверки, сравнение строкой, нестрогий разбор, отметка с миллисекундами — на обеих границах). **Круг 5 (03.09.2026, по находке Codex): `parseMoment` календарно строг.** `Date.parse` нормализовал «2026-02-31» в 3 марта, и невозможная дата после PATCH совпадала с ожидаемым моментом (`verified`), а план с такой датой строился. Теперь компоненты проверяются по календарю (день по месяцу, високосный год 4/100/400, время 00–23:00–59:00–59, смещение 00–23:00–59), момент собирается арифметикой `Date.UTC` без `Date.parse`; невозможная дата — `null` → `outcomeMismatch [checkedAt]` / `checkedAtInvalid`. Репро до/после `tmp/10f-p-p07-refresh5-repro-OLD/NEW-2026-09-03.log`; мутации 7/7 (снятие календарной проверки, правило 100/400, февраль=29, время, смещение, месяц, возврат к `Date.parse`). | `src/lib/poi-coordinate-refresh.ts`, `src/app/api/cron/refresh-coords/route.ts`, `scripts/refresh-google-coords.mjs`, `src/lib/poi-coordinate-decision.ts`, `src/lib/poi-ingest.ts`, `scripts/poi-portals/collect-pois.mjs` | `tests/poi-coordinate-refresh.mjs` (302), `tests/poi-coordinate-contract.mjs` (114), `tests/poi-coordinate-decision.mjs`, `tests/poi-portal-place.mjs`, `tests/support/coords-refresh-harness.mjs`, `tests/support/ts-alias-hooks.mjs`, `tests/support/production-sandbox.mjs` |
| **10f-P R5, P07.3 (тогда кандидат; принят 03.09.2026 — см. строку остаточного аудита выше).** Полный production-контур трёх политик координат: `exactObjectPoint` выводится машинно из подтверждённой точки резолвера; `representativePoint` и `notApplicable` доходят до записи ЕДИНСТВЕННЫМ каналом — реестр `config/poi-coordinate-decisions.v1.json` под git, читаемый writer'ом статическим импортом по каноническому пути. Фабрики с аргументом-реестром нет, `options`/`deps`/поля запроса не читаются, объект той же формы брендом не обладает. **Честно о полномочии:** статический импорт и контрольная сумма `integrityDigest` не доказывают авторство — импорт лишь закрывает обходные каналы в коде, а сумму пересчитает кто угодно; полномочие задаётся процессом owner review — правка реестра попадает в main только через ревью владельца, и именно этот процесс, а не код, удостоверяет решение. Решение привязано к предмету (`subject`: город и хотя бы одно имя) и применяется только при совпадении с записываемой записью — перестановка строк с нестабильным ключом `row-N` даёт именованный отказ `decisionSubjectMismatch`, а не перенос решения на другой POI. `writeRun` консультирует реестр до резолвера; `ingestPoi` перепроверяет предмет и согласие точки/пустой пары тем же `classifyCoordinatePolicy`; след решения — в Notes. Композиция проверяется песочницей-копией дерева с фикстурным файлом по каноническому пути, без второго канала в коде. Реестр пуст; путь Telegram проходит через тот же `ingestPoi` | `src/lib/poi-coordinate-decision.ts`, `src/lib/poi-ingest.ts`, `scripts/poi-portals/collect-pois.mjs`, `scripts/poi-coordinate-decision.mjs` | `tests/poi-coordinate-decision.mjs`, `tests/poi-portal-place.mjs`, `tests/support/production-sandbox.mjs` |
| **10f-P R5, P04.3 — ACCEPTED 3/3: аудит Codex R5 принят 03.09.2026; карточка `5fbdc2bd…` исполнена один раз 03.09.2026 по L3-разрешению владельца — `allApplied`, gate `verifiedSuccess: true`, четыре поля созданы в `tblVCmFcHRpXUT24y` (`fldbb1QTJBWYeK5hd`, `fldBfAn5QYSKcurip`, `fldtUtysGHkzZZyOT`, `fldH4rDk8pfKcl8le`), схема 29/449 → 29/453 без иных изменений, `check:poi` чист; журнал `tmp/poi-schema-executions/5fbdc2bd…/journal.jsonl`.** Четыре канонических поля `POI Type`, `POI Facets`, `Type Source`, `Taxonomy Version` — коды, не подписи; единственная связь реестр↔схема↔writer — `src/lib/poi-taxonomy-airtable.ts`. Схемная операция — execution-grade цепочка `scripts/poi-schema/*` (15 модулей) с bootstrap. **Каноническая область проверяется ДО credentials, сети и маршрута** (10f-P R5, находка 2): свидетель и CLI сверяют карточку каноническим контрактом (база, таблица, поля из loader) прежде, чем строить URL или читать токен; baseId/tableId/поля не переопределяются карточкой, не прошедшей контракт — URL и поиск таблицы идут от КОНСТАНТ, а gate требует у свидетеля каноническую таблицу; карточка с валидными отпечатками модулей, но чужим baseId (bootstrap сверяет лишь модули) отвергается до запроса — credentialed GET в чужую базу невозможен. **Любое брошенное значение на КАЖДОЙ границе `readSchema`/`createField` — включая СЫРОЙ отозванный Proxy прямо на потребляемой исполнителем границе — описывается безопасно и не покидает исполнителя** (10f-P R5, находка 1): после потенциального POST исход только свежим чтением, недоступность по любой причине → `unknown` + `recoveryRequired` + закрытый журнал, без повтора; `error.*` читается безопасными обёртками, не через `?.`. **Транспорт сам не выпускает неизвестное исключение** (находка 3): враждебный `fetchImpl`, в т.ч. отклоняющийся отозванным Proxy, даёт только `{ kind }` или `SchemaReadError`. Диагностика печатает `sent.response`, не `sent.kind` (находка 4). Прежние свойства сохранены: срок на каждый обмен, полная валидация формы, полное состояние четвёрки перед POST, `pendingFinalWitness` и поздний вердикт, независимый свидетель со счётом «ровно по одному», gate требует ровно четыре fieldId. Карточки R0–R4 объявлены устаревшими; карточка R5 заморожена и исполнена ровно один раз (см. выше); повторный `--execute` запрещён | `src/lib/poi-taxonomy-airtable.ts`, `src/lib/poi-memory-store.ts`, `src/lib/poi-ingest.ts`, `scripts/poi-portals/collect-pois.mjs`, `scripts/poi-portals/lib/airtable-store.mjs`, `scripts/poi-portals/lib/base-snapshot.mjs`, `scripts/check-poi-integrity.mjs`, `scripts/poi-schema/*` | `tests/poi-taxonomy-airtable.mjs`, `tests/poi-schema-chain.mjs`, `tests/poi-portal-place.mjs`, `tests/base-snapshot.mjs`, `tests/poi-integrity.mjs` |

### Переходное

| Что | Почему переходное | Когда закончится |
|---|---|---|
| Legacy-мост старых русских категорий | Сайт фильтрует по старому полю (потребитель № 4 не начат); с 10f-P мост не останавливает запись — заполняется где перевод точен, иначе пусто с очередью и Notes, истина о типе — в `POI Type` | потребитель № 4 и миграция полей |
| Поля `categoriesRu` и `POI Category (RU)` | это совместимость, а не новая таксономия | там же |
| `docs/poi-intake-contract.md` | описывает действующую границу create, не всю целевую V2 | когда целевая V2 будет реализована |
| `docs/poi-standard.md` | эталон записи писался до таксономии | после потребителя № 4 |

### Целевое (описано, не реализовано)

- Claim Ledger и происхождение каждого факта — `docs/handoff-poi-intake-v2-2026-08-11.md`.
- Единая граница `resolvePlace` для всех путей создания.
- Журнал редакторских правок: сейчас девять путей правят POI и ни один не журналирует изменение.
- Guard на удаление POI: удаление не смотрит на входящие ссылки.

### Не начато

- Потребитель № 2b: **production-подключение исполнителя реализовано и принято независимым аудитом 03.09.2026** (10f-O, P03 — 3/3) — `npm run poi:collect -- --model-execute --model-plan-file <name>.envelope.json --model-approval <name>`; остаются owner-approval конкретного прогона и разрешающая policy источника. Профиль, модель, таблица цен и офлайн-контур были реализованы раньше.
- Потребитель № 3: списки XLSX из реестра.
- Потребитель № 4: сверка и миграция опций Airtable.
- ~~Четыре поля таксономии v2 в живой схеме Airtable~~ — **сделано 03.09.2026**: карточка R5
  (`5fbdc2bd…`) исполнена один раз по разрешению владельца, gate `verifiedSuccess`, четыре поля есть
  в таблице POI (57 полей), `check:poi` чист; preflight схемы у `writeRun` теперь проходит по
  живой базе (см. строку 10f-P R5 в § 4). Повторное исполнение карточки запрещено.
- Миграция 444 legacy-записей на явную политику координат — реестр решений есть, записей в нём нет.
- Production-импорт Осаки.
- Контроль дрейфа данных и решений. Архитектура предложена — `docs/adr/0002-poi-drift-control-v1.md`,
  порядок работ — `docs/poi-intake/drift-roadmap.md`; ADR не принят. «Не реализовано ничего» —
  неверно и исправлено 1 сентября 2026 года. Подэтапы **1а и 1в закрыты** — обе ветки кода
  возврата исполняются процессными тестами, а `--existing` стал fail-closed; подэтап **1б
  сделан наполовину**: контракт снимка есть, экспортёра нет. Остальное не начато.
  Проверенный по коду статус каждого этапа — в самом роадмапе, полный граф оставшихся работ —
  `docs/poi-intake/poi-completion-dag.md`.

---

## 5. Что показывает baseline Осаки

Снимок `tmp/dry-osaka-pre-taxonomy-e4bc27d.json`, digest `sha256:abb8afb31fa1ed7347d5bfbabfa1931605ea033efe1aa93682c640bd5f4f82db`. Файл в репозиторий не входит; если его нет, профильный тест пропускает сверку и говорит об этом вслух.

| Величина | Значение | Что означает |
|---|---|---|
| `poiEligible` | 336 | таксономия и качество пропускают дальше |
| `poiWritable` | 116 | те же плюс определён маршрутный город и не снят дедупом |
| `autoImportable` | 116 | тождественно `poiWritable`, считается той же функцией |

Разница между 336 и 116 — не потеря: география и дедуп отсеивают записи после классификации. Раньше эти два понятия были одним, и отчёт называл автоимпортируемыми 336 объектов, из которых до записи доходило 116.

---

## 6. Защищённые границы

Обойти любую из них — регрессия, а не оптимизация.

1. **Создание записи POI кодом идёт только через `ingestPoi()`.** Прямой `POST` в таблицу POI из нового кода запрещён.
2. **Ручной ввод в интерфейсе Airtable этой границей не закрыт.** Так появились 24 пустые записи 10.08.2026. Закрыть кодом нельзя; сделать видимым — можно, запись без Intake-маркера отличима.
3. **Реестр таксономии читается только через `src/lib/poi-taxonomy.ts`.** Второй читатель JSON — это второй реестр.
4. **Локальных списков типов, подписей, фасетов и маршрутов не существует.** Проверяется тестом: строковые литералы модулей пересекаются со значениями реестра, пересечение обязано быть пустым.
5. **Источник классификации `human` машинным вызовом не имитируется.** В портальном классификаторе такого канала нет вовсе.
6. **Семантическое изменение реестра требует новой версии; любое изменение байтов меняет digest.**
7. **`--write` в коллекторе — только по явному решению владельца.**

Полный список правил для агентов: `docs/poi-intake/change-policy.md`.

---

## 7. Следующий разрешённый этап

**Owner-конфигурация: выбор провайдера, модели и таблицы цен — выполнено (этап 10a).** В обоих реестрах по одной записи: профиль `openai-responses-luna@1.0.0` с моделью `gpt-5.6-luna` и таблица цен, сверенная 17 августа 2026. Платный путь остаётся недостижимым, но уже не пустотой реестра, а четырьмя другими обстоятельствами — см. `tests/poi-model-reachability.mjs`.

**Повтор запроса после подтверждённого `noCharge` не открывается ни в № 9a, ни в № 9b.** Контракт запроса и контракт разрешения оба несут `maxRetries === 0`, и снятая неопределённость о списании права на новый платный запрос не выдаёт. Повтор потребует нового решения владельца, оформленного отдельным контрактом. Прежняя формулировка этого раздела обещала обратное — она была неверна.

Неоплачиваемый status-запрос появится позже и только для профиля, который явно объявляет такую capability.

Этапы № 9a (протокол владения исполнением) и № 9b (граница транспорта) выполнены; см. строки о них в § 4.

Что уже готово: контракт профиля с объявленной записью, идентификатор записи запроса и выборка кандидатов, механика исполняемого плана v2, контракт разрешения владельца с потолками, корни артефактов и журнал упреждающей записи, execution-preflight с денежной арифметикой, provider-neutral запрос, канонический сериализатор с отпечатком исходящих байтов, граница транспорта с инъецируемыми проводом и резолвером учётных данных и протокол владения исполнением. Потребитель № 2b этим не завершён.

Чего по-прежнему нет и кодом не делается: разрешённые источники и финансовый потолок. Провайдер, модель и таблица цен выбраны владельцем в этапе 10a. `modelProcessing` у всех двенадцати источников остаётся запрещающей, исполнитель ни одним production-путём не вызывается, approval в репозитории отсутствует — поэтому `executionPermitted` остаётся `false`, а запрос отправить нечем. Реальный runtime-план v2 и approval создаются локально уже после owner-коммита, а не сейчас.

Что делать **до** него нельзя: трогать XLSX, Airtable, production Intake и правила классификации. Правила меняются только по eval-фикстуре, а её ещё нет (§8).

---

## 8. Известные ошибки классификации, оставленные намеренно

Две находки из сравнения с baseline. Обе оставлены как есть — материал для будущей eval-фикстуры, а не повод править правило по единичному примеру.

| Объект | Что происходит | Почему так |
|---|---|---|
| `大阪港帆船型観光船 サンタマリア` | шаблон `港` уводит в `excludedByTaxonomy` | по ADR это `experience`, но правило транспорта грубое; менять его по одному случаю нельзя |
| `水上バス・アクアライナー（大阪城港）` | `城` в названии причала даёт `castle_fortification` | порядок шаблонов достался от легаси; ошибка не новая |

---

## 9. Открытые риски

Известные и непокрытые. Не дефекты конкретного коммита — свойства системы на сегодня.

| Риск | В чём | Что делать до того, как закроем |
|---|---|---|
| **Пакетная запись не атомарна** | `ingestPoiBatch()` — цикл по `ingestPoi()`. Транзакций Airtable не даёт, компенсирующего удаления у нас нет. Падение на сотой записи оставляет первые девяносто девять в базе | не считать базу неизменённой при сбое; порядок действий — `runbook.md` §9 |
| **Сбой записи не виден по коду возврата** | исключение из `writeRun` перехватывается, попадает в отчёт как `write.error`, прогон завершается нулём. Списка созданного в отчёте при этом нет | смотреть `report.write`, а не код возврата; что создано — выяснять по базе, выборкой по `Intake Run ID` |
| **Отката записи нет** | удаление POI не смотрит на входящие ссылки; `Parent POI` и Route Stops оборвутся молча | удалять руками, поштучно, сверяясь со связями |
| **Обновления не журналируются** | девять путей правят POI, ни один не пишет, кто и когда | на вопрос «кто поменял» ответа нет; восстанавливать из истории ревизий Airtable |
| **Ручной ввод в Airtable не закрыт** | так появились 24 пустые записи 10.08.2026 | записи без Intake-маркера отличимы — это единственная защита |
| **Legacy-мост покрывает 9 кодов из 20** | для остальных запись останавливается целиком | ожидаемо до потребителя № 4 |
| **Файловый протокол не отменяет уже отправленных байтов** | владение исполнением защищено сегментами и fencing'ом: после перехвата ни одна запись прежнего владельца не входит в логический журнал. Но отрезать живой процесс от его собственного сокета локальными средствами нельзя | перехват выполнять только по правдивому утверждению владельца `operatorConfirmedStopped`, `processExited` или `hostRestarted`; код проверяет форму утверждения и его привязку к точному хвосту, но не истинность |
| **Незавершённая эпоха требует решения владельца** | существование сегмента резервирует эпоху немедленно; пустой или оборванный сегмент не пропускается автоматически, потому что вывод «создатель умер» — тот же запрет, что и вывод по времени | снимать неопределённость полномочием `poi-model-takeover/v1`, связанным с именем, длиной и сырым отпечатком незавершённого сегмента |
| **Расщеплённый журнал итога не даёт** | записи-сироты за границей перехвата описывают настоящие эффекты, включая возможную отправку платного запроса. Отбросить их молча значило бы спрятать списание | разбирать `journalForked` вручную: код возврата отдельный, счётчики не считаются, дозапись закрыта |

---

## 10. Как проверить, что этот файл не устарел

```bash
git log -1 --format=%H -- docs/poi-intake/README.md   # чем этот файл в последний раз подтверждён
git log --oneline -1                                  # где HEAD
npm test                                              # состав наборов — в package.json
shasum -a 256 config/poi-taxonomy.v2.json
```

Границей актуальности служит коммит, содержащий текущую версию **самого этого файла**, а не поле внутри него. Поле `Last verified commit` убрано намеренно: коммит, обновляющий документ, не может содержать собственный hash, поэтому такая отметка всегда отстаёт ровно на один коммит и выглядит просроченной, даже когда документ верен.

Если между `git log -1 -- docs/poi-intake/README.md` и `HEAD` есть коммиты, менявшие подсистему POI, документ обязан быть обновлён — тем же коммитом, что и код.
