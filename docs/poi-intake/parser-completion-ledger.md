```
Status: current
Scope ID: poi-parser-v1
Scope frozen: 2026-09-01
Owner: Eduard Revidovich
Milestones: 10 (immutable without an approved scope change)
Acceptance checks: 30
Checks met at freeze: 18
Checks actually met (after 10f-Q R3 P08 acceptance, 2026-09-04): 25
Milestones accepted at freeze: 2
Milestones accepted now: 7
Current work package: 10f-Q closed (P01.2 and P08.1-P08.3 ACCEPTED 2026-09-04; 25/30, 7/10). Next: 10f-R (P02.2, P09.2-P09.3). 10f-P closed (P04.3, P06.3, P07.3 ACCEPTED)
Task identity: every work package must close named P01–P10 checks
```

# POI-парсер: реестр завершения и дрейфа области

Этот файл отвечает на один вопрос: **насколько близок к завершению POI-парсер**.
Он не заменяет канонический статус подсистемы в `README.md`, архитектурный DAG или
операционный runbook. Их объём может расти при обнаружении новых рисков; знаменатель
этого реестра без решения владельца не меняется.

## 1. Что считается готовым парсером

Готовый парсер одним разрешённым запуском проводит запись по цепочке:

`источник → извлечение → нормализация → география → классификация → сопоставление → проверка → Airtable → обратное чтение → отчёт`

Для доказанной записи результатом становится автоматическое создание или обновление
разрешённых полей в Airtable. Неоднозначная запись заканчивается `needs_review` и не
пишется. Ошибка, дрейф или неизвестный исход останавливают запись. Автоматическое
удаление в определение готовности не входит.

Парсер принят только после закрытия всех десяти вех ниже. Архитектурная возможность,
документ, тест чистой функции или одноразовый исполнитель сами по себе веху не закрывают:
нужно доказательство на production-композиции, указанное в последней колонке.

### 1.1. Инвариант идентичности задачи

Задержка, новый дефект и повторный аудит допустимы. Незаметная смена предмета работы —
нет. До начала каждого пакета фиксируются четыре строки:

- `scope_id`: всегда `poi-parser-v1`;
- `target_checks`: конкретные критерии P01–P10;
- `allowed_work`: какие изменения нужны, чтобы закрыть эти критерии;
- `non_goals`: что в этом пакете запрещено начинать.

Если действие нельзя связать хотя бы с одним `target_checks`, оно не выполняется. Агент
останавливает работу и классифицирует его как `FOLLOW_UP_DEBT` или `SCOPE_CHANGE`.
Сложность исправления не меняет идентичность задачи: несколько кругов вокруг P05.3
остаются работой над P05.3, но работа над гостиницами, legacy-миграцией или cron — нет.

## 2. Замороженные десять вех

Статусы: `ACCEPTED`, `IN_PROGRESS`, `QUEUED`, `REOPENED`, `BLOCKED_OWNER`.
Внутри каждой вехи ровно три бинарных критерия. Процент ниже показывает покрытие
критериев, а не оценку оставшегося времени.

| ID | Веха | Критерии приёмки | Состояние на 2026-09-01 | Доказательство |
|---|---|---|---|---|
| P01 | Получение источников | ✓ обход всего заявленного корпуса; ✓ стабильные `sourceKey` и учёт каждой строки; ✓ полный read-only обход завершается отчётом | `ACCEPTED` — 3/3 | `README.md` § 2 и § 4: живой обход v3 от 25.08.2026. P01.2 опровергнут: `scripts/poi-portals/lib/opendata-csv.mjs:143` подставляет номер строки (`row-N`) при пустом источниковом ID — перестановка строк выгрузки даёт новому объекту старый ключ. Аудит Fable R2, F-07; 10f-Q R0 (04.09.2026, кандидат): ключ источника не зависит от позиции строки — `opendata-csv/v2` строит его только из идентификатора источника, строка без ключа получает именованный исход из закрытого списка и считается в `finalTally.sourceKeyRefused`, закон сохранения проверяется на границе адаптера и в коллекторе, сторож ключей общий для любого адаптера. R1 (04.09.2026): адаптер `opendata-csv/v3` — сырая строка заголовков проверяется на неоднозначность (точный повтор и эквивалентность после складывания ширины) ДО сборки объектов, иначе колонки схлопываются молча; строка заголовков берётся из первой ЗАПИСИ, а не первой физической строки (`tests/opendata-csv.mjs` 86). **P01.2 принят независимым аудитом Codex 04.09.2026 — `ACCEPTED` 3/3, счёт 22/30**; переоткрытие — только по новому производственному контрпримеру |
| P02 | Fail-closed вход и ошибки процесса | ✓ неверный `--existing` отвергается до эффектов; ☐ перехваченная ошибка даёт ненулевой код; ✓ корректный снимок действительно участвует в сопоставлении | `REOPENED` — 2/3 | `tests/existing-file.mjs`; 10f-M R2. P02.2 опровергнут: `collect-pois.mjs` перехватывает ошибку портала и ошибку записи в отчёт (`portals[].error`, `write.error`) и завершает процесс нулём — критерий был принят на узком чтении «ошибка, покинувшая `main`». Аудит Fable R2, F-03 |
| P03 | Модельное извлечение | ✓ план, профиль, разрешение и preflight связаны; ✓ офлайн-исполнитель и журнал проходят полный контракт; ✓ production-entrypoint реально вызывает исполнитель только после ворот | `ACCEPTED` — 3/3 | `scripts/poi-portals/collect-pois.mjs` (`--model-execute`); `scripts/poi-portals/lib/model-run.mjs` (конверт `poi-model-plan-envelope/v1`), `lib/model-wire.mjs`; `tests/poi-model-entrypoint.mjs` (448), `tests/poi-model-reachability.mjs` (119); независимый аудит Codex 03.09.2026 |
| P04 | Нормализация и taxonomy | ✓ реестр v2 и loader каноничны; ✓ портальный классификатор использует реестр; ✓ итоговые значения полностью представимы и передаются в действующую схему Airtable | `ACCEPTED` — 3/3 | `config/poi-taxonomy.v2.json`; `src/lib/poi-taxonomy-airtable.ts`; `scripts/poi-schema/*` (15 модулей, карточка `poi-taxonomy-schema-card/v5`); `tests/poi-taxonomy-airtable.mjs` (136), `tests/poi-schema-chain.mjs` (453); независимый аудит Codex R5 03.09.2026; одноразовое исполнение карточки `5fbdc2bd…` 03.09.2026 — gate `verifiedSuccess`, четыре поля созданы в `tblVCmFcHRpXUT24y`, `check:poi` чист |
| P05 | География и Place ID | ✓ единый контракт `resolvePlace` существует; ✓ подтверждённая точка канонизируется без допуска; ✓ портальный путь проходит эту границу и наполняет `resolved`/`place_id` | `ACCEPTED` — 3/3 | `src/lib/place-resolve.ts`; `src/lib/poi-portal-place.ts`; `tests/place-resolve.mjs`; `tests/poi-portal-place.mjs`; `tests/poi-canary-osaka-offline.mjs`; независимый аудит Codex 02.09.2026 |
| P06 | Сопоставление и дедупликация | ✓ контракт снимка и хранилище готовы; ✓ проверенный снимок композиционно участвует в матчинге; ✓ версия matcher policy и различающая eval-фикстура закреплены | `ACCEPTED` — 3/3 | `tests/base-snapshot.mjs`; `tests/existing-file.mjs`; политика `poi-matcher-policy/v1` заморожена (`src/lib/poi-matching.ts`, версия + SHA-256), eval `tests/poi-matching-eval.mjs` (207) на фикстуре `tests/fixtures/poi-matching-eval/v1.json` — 17 доказанных пар, покрытие неполное (`same_physical_place`, `co_located`, действие `needs_review`); решения владельца Q1–Q5 = A (03.09.2026) внесены в фикстуру с `owner_decision` и дословной цитатой, baseline переснят штатно при той же политике v1 (21 пара); `complete` недостижим — действие `duplicate` осталось без доказанной пары после Q4 = A, открыт вопрос Q6 (`docs/poi-intake/p06-matching-decisions-2026-09-03.md`); Q6 = C (04.09.2026): артефакты и GET-скан таблицы POI (5 GET, 471 запись, 24 кандидата, 0 общих Place ID, 0 равных имён, 3 blockingDuplicate — все Parent POI) доказанной пары не дали — `BLOCKED_EVIDENCE_GAP`; Q6 = F (04.09.2026): производный конструированный контроль `fuji-5th-station-derived-control` (`kind = derived_control`, точка POI-000239 подставлена входящей записи, `duplicate`/`blocked_duplicate`, `historicalClaim = false`, три ссылки), эмпирическая пара Фудзи не изменена; eval различает роды пар (`kindCoverage`, `metricsByKind`), baseline переснят при той же политике v1 — 22 пары, фикстура `complete`, eval 322, мутации убиты; 04.09.2026 — 15 параметров алгоритма вне политики (part/whole 0,95, parent gap 0,1, длины ядра/скелета/коллекции, пороги и резервные веса вхождения, n-граммы, округления, радиус Земли) перенесены в `MATCHER_POLICY` → `poi-matcher-policy/v2`, baseline без сдвигов; инвентарь всех числовых литералов по AST с поимёнными исключениями; аудит круг 2 — словари (`MATCHER_LEXICON`: родовые слова, классы, стоп-слова, Поливанов, складывания) в отпечатке политики и фикстуры, инвентарь таблиц и регулярок по AST (`tests/poi-matching.mjs` 431), эквивалентность 0 расхождений, baseline без сдвигов, 15 мутаций словарей/обходов убиты; круг 3 — глубокая заморозка словарей (`deepFreeze`, 98 узлов, 14 отвергнутых вложенных правок), версия `poi-matcher-policy/v3`, baseline без сдвигов (`tests/poi-matching.mjs` 451); **P06.3 принят независимым аудитом Codex 04.09.2026 — `ACCEPTED` 3/3, счёт 21/30** |
| P07 | Контракт записываемой POI | ✓ неизвестное заканчивается `needs_review`; ✓ `exactObjectPoint` пишется вместе с подтверждёнными координатами; ✓ авторизованы и реализованы `representativePoint`/`notApplicable` | `ACCEPTED` — 3/3 | `src/lib/poi-coordinate-policy.ts` (единственное вычисление); `src/lib/poi-coordinate-decision.ts` + `config/poi-coordinate-decisions.v1.json` (реестр решений владельца под git, статический импорт, `integrityDigest`, предмет решения); `src/lib/poi-ingest.ts` (единственный create-путь: решение → тот же `classifyCoordinatePolicy` → координаты и политика одним `store.create`); `src/lib/poi-coordinate-refresh.ts` + `src/app/api/cron/refresh-coords/route.ts` + `scripts/refresh-google-coords.mjs` (все писатели координат подчинены политике: свежая предзаписная сверка, итог только чтением после PATCH по каждому полю плана, `recoveryRequired`, остановка хвоста, безопасные границы); `tests/poi-coordinate-contract.mjs` (114), `tests/poi-coordinate-refresh.mjs` (302), `tests/poi-coordinate-decision.mjs` (167), `tests/poi-coordinate-policy.mjs` (84); мутации по кругам 10/10, 15/15, 14/14, 13/13, 11/11, 7/7; независимый аудит Codex — пять кругов находок закрыты, принята владельцем 03.09.2026. Переоткрытие — только по новому production-контрпримеру |
| P08 | Манифест и допуск до записи | ✓ `run-manifest/v2`; ✓ тождество входа, снимка и версии политики; ✓ pre-write drift gate исполняется до store и первой записи | `ACCEPTED` — 3/3 | Узлы 2.1–2.7 DAG; 10f-Q R0 (кандидат): `run-manifest/v1` собирается каждым прогоном (`scripts/poi-portals/lib/run-manifest.mjs`), тождество входа — SHA-256 сырых байтов выгрузки и канонического набора кандидатов, база и имена — SHA-256 файлов, политика матчера с отпечатком словарей, таксономия, контракт приёма и коммит кода; pre-write drift gate исполняется до store, до резолвера и до первой записи, каждый компонент даёт свой именованный дрейф, `--write` без эталона и без `--existing` не запускается. R1 (04.09.2026): каждый входной файл читается один раз и gate с writer'ом пользуются одним прочитанным содержимым (TOCTOU закрыт); `code.tree` — отпечаток фактических байтов цепочки, коммит/дерево/грязь сверяются независимо; сырые и канонические тождества входа разведены на две независимые оси. R2 (04.09.2026): состав исполняемого кода ВЫВОДИТСЯ обходом графа импортов от точки входа (`code.graph`, `poi-code-graph/v1`) вместо правила по каталогам, замок зависимостей — своя ось (`code.deps`); реестры (таксономия и решения владельца о координатах) подписаны ЗНАЧЕНИЕМ, которое загрузил процесс, списком `registries`; контракт манифеста поднят до `run-manifest/v2`, версия проверяется до состава полей; `adapterDrift` больше не отменяет сравнение сырых байтов и даёт названную `canonicalIncomparable` (`tests/poi-code-graph.mjs` 48, `tests/poi-run-manifest.mjs` 112, `tests/poi-prewrite-gate.mjs` 223). R3 (04.09.2026): снимков кода два — до первого адаптера и перед gate, расхождение отказывает прогон до store и резолвера; шаблонные строки разбираются, а полнота разбора импортов проверяется по хвосту; байтовая (`code.graph`) и ценностная (`registries`) оси объявлены раздельно |
| P09 | Автоматический перенос в Airtable | ✓ production-код имеет create-path; ☐ общий update-path журналирует и восстанавливает неизвестный исход; ☐ результат каждой записи устанавливается независимым перечитыванием | `IN_PROGRESS` — 1/3 | `writeRun`/Intake существуют; полная общая граница записи и reconciliation не собраны |
| P10 | Сквозная приёмка | ✓ read-only цепочка до кандидата и сравнения с базой исполнялась; ☐ ограниченный запуск автоматически записал разрешённые результаты; ☐ независимая сверка и production-гейты дали финальный успех | `QUEUED` — 1/3 | Нужен финальный end-to-end прогон самого парсера, не одноразовой карточки |

### Сводка

Счёт исправлен 2026-09-02 по аудиту Fable R2. Прежние значения (2 вехи, 18/30) были
завышены: два принятых критерия опровергнуты исполняемыми контрпримерами; после
коррекции осталось 0 вех и 16/30.

**Сегодняшние «5 вех, 21/30» не наследуют заморозке.** На заморозке принятыми
числились P01 и P02, оба переоткрыты. Сейчас приняты P05 (02.09.2026), P03
(03.09.2026), P04 (03.09.2026), P07 (03.09.2026) и P06 (04.09.2026), все пять — после
независимого аудита Codex; P04 дополнительно подтверждена исполнением схемной
карточки с `verifiedSuccess`, P07 — пятью кругами закрытых находок на production-коде
всех писателей координат, P06 — тремя кругами (числовой инвентарь калибровок,
словари в отпечатке, глубокая заморозка) при неизменных вердиктах 22 пар eval.
P02.2 по-прежнему открыт, и путь 16 → 17 → 18 → 19 → 20 → 21 → 22 → 24 → 25 виден
построчно в журнале § 8.

- Принято вех: **7 из 10**.
- Выполнено бинарных критериев: **25 из 30**.
- Осталось критериев: **5**.
- Следующая веха: **P02** (переоткрыта, критерий 2) и далее пакет **10f-R** (P02.2, P09.2–P09.3). Пакет **10f-Q закрыт 04.09.2026**: P01.2 принят после круга R1, P08.2 и P08.3 — после R2, P08.1 — после R3; **P08 `ACCEPTED` 3/3, счёт 25/30, принято 7 вех из 10**. Плановые для 10f-Q 25/30 достигнуты.
- Переоткрыто: **P02** (критерий 2).
- Неподтверждённых расширений области: **0**.

`25 из 30` нельзя выдавать за «парсер готов наполовину по времени»: критерии разного
размера. Это только проверяемая мера покрытия. Финиш — исключительно `30 из 30` и
`10 из 10 ACCEPTED`.

## 3. Очередь до финиша

Каждый пакет обязан закрыть перечисленные критерии и не вправе добавлять новую веху.

Отправная точка после коррекции по Fable R2 — **16 из 30**. Колонка «после пакета»
показывает счёт ПОСЛЕ независимого аудита соответствующего пакета, а не до него.

| Пакет | Закрывает | Результат | После пакета |
|---|---|---|---|
| 10f-N | P05.3 | Портальный путь использует общий `resolvePlace`, сохраняет Place ID либо заканчивается `needs_review` | 17/30 |
| 10f-O | P03.3 | Production-entrypoint модели подключён и остаётся fail-closed без точного разрешения | 18/30 |
| 10f-P | P04.3, P06.3, P07.3 | Выход парсера представим в Airtable; matcher версионирован и проверен eval; завершён контур Coordinate Policy | 21/30 |
| 10f-Q | P01.2, P08.1–P08.3 | Стабильный `sourceKey` fail-closed; манифест, тождество входа и pre-write drift gate работают до эффектов | 25/30 |
| 10f-R | P02.2, P09.2–P09.3 | Перехваченная ошибка даёт ненулевой код; общая create/update-граница журналирует попытки и устанавливает исход перечитыванием | 28/30 |
| 10f-S | P10.2–P10.3 | Ограниченный живой запуск от источника до Airtable получает независимый финальный вердикт | 30/30 |

После 10f-S парсер либо принят, либо конкретная веха получает `REOPENED`. Следующей
«фазы парсера» после S нет.

## 4. Что не входит в знаменатель

- массовый импорт Осаки после приёмочного подмножества;
- миграция `Coordinate Policy` у 444 legacy-записей;
- редакционные очереди, переписывание описаний и чистка старых категорий;
- cron, calibration series и shadow/challenger;
- автоматическое удаление POI;
- гостиничный трек;
- любые улучшения, не необходимые для одного из тридцати критериев.

Такой долг можно записать в DAG или backlog, но он не блокирует P01–P10 и не меняет
их число.

## 5. Правило против дрейфа области

Каждая новая находка до начала работы получает один класс:

1. `IN_SCOPE_DEFECT` — прямо нарушает существующий критерий P01–P10. Исправляется
   внутри той же вехи; новая веха не появляется.
2. `FOLLOW_UP_DEBT` — полезна, но не нужна для P01–P10. Записывается вне этого реестра
   и не задерживает текущий пакет.
3. `SCOPE_CHANGE` — меняет определение готовности, число вех или критерий приёмки.
   Без отдельной записи владельца в таблице ниже не действует.

Аудит может отвергнуть доказательство и вернуть веху в `REOPENED`, но не может молча
расширить её предмет. Если третий круг аудита подряд не закрывает ни одного бинарного
критерия, работа останавливается: вместо четвёртого круга составляется разбор причины
и два варианта — минимальное исправление или отдельный долг.

## 6. Утверждённые изменения области

| ID | Дата | Решение владельца | Влияние на знаменатель |
|---|---|---|---|
| SC-001 | 2026-09-01 | Гостиницы — отдельный припаркованный трек | нет |
| SC-002 | 2026-09-01 | Legacy-миграция 444 политик не определяет готовность нового парсера | нет |
| SC-003 | 2026-09-01 | Готовый парсер включает автоматический перенос доказанных данных в Airtable | P09 и P10 явно входят в исходные 10 вех; число не изменилось |
| SC-004 | 2026-09-02 | Задержки и новые находки допустимы, но вся работа обязана оставаться внутри явно названных P01–P10 | нет; вводит обязательный контроль идентичности задачи |

Новая строка добавляется только после однозначного решения владельца. Существующие
строки не переписываются; отмена оформляется новой строкой.

## 7. Ретроспектива выполненной работы

| Круг | Что делалось | Вклад в P01–P10 | Классификация |
|---|---|---|---|
| 10f-A–10f-K | аудит и канонизация живых POI, координаты, города, Coordinate Policy, безопасные разовые L3-карточки | подготовил данные и доказал часть P07; большинство работы не закрывало парсерные вехи | prerequisite/data cleanup |
| 10f-L R2–R5 | детерминированный baseline, канонизация координат, семантика машинной Coordinate Policy, безопасная запись артефакта | P05.1–P05.2, P07.1–P07.2 | parser progress |
| 10f-M–10f-M R2 | fail-closed `--existing`, композиция с matcher, коды возврата | закрыл P02, подтвердил P06.1–P06.2 | parser progress |
| Гостиничный эпизод | ссылки Trip.com и `check:docs` | 0 критериев | parked foreign track |

Эта таблица объясняет, почему объём выполненной работы велик, а принята пока одна
веха: значительная часть месяца ушла на состояние базы и безопасность разовых записей,
а не на замыкание production-композиции парсера.

## 8. Журнал прогресса

Только добавление строк. Любая строка, меняющая статус, обязана называть критерий и
проверяемое доказательство в репозитории; ссылка только на файл в `tmp/` недостаточна.

| Дата | Пакет | Критерий | Было → стало | Доказательство | Изменение области |
|---|---|---|---|---|---|
| 2026-09-01 | freeze | P01–P10 | незамороженный DAG → 10 вех / 30 критериев | `README.md`, `poi-completion-dag.md`, код и тесты, перечисленные в § 2 | SC-001–SC-003 |
| 2026-09-02 | governance | P01–P10 | неявная связь работы с целью → обязательные `scope_id`, `target_checks`, `allowed_work`, `non_goals` | § 1.1 и решение владельца | SC-004 |
| 2026-09-02 | коррекция по аудиту Fable R2 | P01.2 | `ACCEPTED` → `REOPENED`: критерий был закрыт неверно | `scripts/poi-portals/lib/opendata-csv.mjs:143` — `get('sourceId') \|\| 'row-' + (i+1)`; аудит `docs/poi-intake/audits/fable-5.1-poi-system-audit-2026-09-02-r2.md` (sha256 `c3fbbd7d…`), F-07 | нет: знаменатель прежний |
| 2026-09-02 | коррекция по аудиту Fable R2 | P02.2 | `ACCEPTED` → `REOPENED`: критерий был закрыт неверно | `scripts/poi-portals/collect-pois.mjs` — `portals[].error` и `write.error` не меняют кода возврата; тот же аудит, F-03 | нет: знаменатель прежний |
| 2026-09-02 | коррекция по аудиту Fable R2 | сводка | принято вех 2 → **0**; критериев 18/30 → **16/30** | обе строки выше; обе принятые вехи и были P01 и P02 | нет |
| 2026-09-02 | 10f-N FINAL R1 | P05.3 | `IN_PROGRESS` 2/3 → `ACCEPTED` 3/3; всего 16/30 → **17/30** | общий `resolvePlace`, портальная граница, offline-canary; 118/118, 229/229, 94/94 и 44/44; мутации 25/25; независимый аудит Codex | нет |
| 2026-09-03 | 10f-O (R0–R3) | P03.3 | `IN_PROGRESS` 2/3 → `ACCEPTED` 3/3; всего 17/30 → **18/30**; принято вех 1 → **2** | `--model-execute` → `scripts/poi-portals/lib/model-run.mjs` → `executeModelPlan` за двенадцатью воротами; исполняемый артефакт — конверт `poi-model-plan-envelope/v1`, отчёт прогона полномочий не несёт; `runCli` fail-closed на любом брошенном значении через `src/lib/thrown-value.ts`; `tests/poi-model-entrypoint.mjs` 448/448, `tests/poi-model-reachability.mjs` 119/119 (ровно одна production-ссылка на исполнитель); мутации 29/29; независимый аудит Codex 03.09.2026 | нет |
| 2026-09-03 | 10f-P R5 + исполнение | P04.3 | `IN_PROGRESS` 2/3 → `ACCEPTED` 3/3; всего 18/30 → **19/30**; принято вех 2 → **3** | схемная цепочка `scripts/poi-schema/*` (15 модулей: карточка `poi-taxonomy-schema-card/v5` c `CANONICAL_BASE_ID`/`POI_TABLE_ID`, bootstrap с проверкой цепочки против карточки, транспорт с единым deadline, исполнитель с исходом только по перечитыванию, независимый свидетель, gate с грамматикой журнала); `src/lib/poi-taxonomy-airtable.ts` — значения taxonomy представимы в схеме POI; `tests/poi-taxonomy-airtable.mjs` 136/136, `tests/poi-schema-chain.mjs` 453/453; мутации R5 10/10; независимый аудит Codex R5 03.09.2026 (принят); одноразовое исполнение карточки `5fbdc2bd…` по L3-разрешению владельца 03.09.2026 13:15:24Z–13:15:30Z: `allApplied`, gate `verifiedSuccess: true`, четыре поля созданы в `tblVCmFcHRpXUT24y` (`fldbb1QTJBWYeK5hd`, `fldBfAn5QYSKcurip`, `fldtUtysGHkzZZyOT`, `fldH4rDk8pfKcl8le`), снимок схемы 29/449 → 29/453 таблиц/полей без иных изменений, `check:poi` чист; журнал `tmp/poi-schema-executions/5fbdc2bd…/journal.jsonl` (19 записей) | нет |
| 2026-09-03 | 10f-P, остаточный аудит P07 | P07.3 | `IN_PROGRESS` 2/3 → без изменения (кандидат 3/3, `READY_FOR_CODEX_AUDIT`); счёт 19/30 не меняется | найден и закрыт дефект вне пути приёма: refresh-писатели координат (`src/app/api/cron/refresh-coords/route.ts`, `scripts/refresh-google-coords.mjs`) не читали `Coordinate Policy` и перезаписывали точку `representativePoint` точкой Google, заводили координаты у `notApplicable`, дописывали половину пары — контрпример на production-коде обоих путей `tmp/10f-p-p07-refresh-repro-*-OLD-2026-09-03.*`; исправление — общий контракт `src/lib/poi-coordinate-refresh.ts` (явная fail-closed таблица допуска, `plan.fields` по каноническим ID, политика legacy не назначается), оба писателя шлют ровно `plan.fields`; `tests/poi-coordinate-refresh.mjs` 120/120 (настоящий route.ts под Node, настоящий скрипт дочерним процессом, состав писателей обходом всего `src/` и `scripts/` со сверкой с реестром); тот же тест на старом коде — 37 провалов (`tmp/10f-p-p07-refresh-old-vs-new-2026-09-03.log`); мутации 15/15 (`tmp/10f-p-p07-refresh-mutations-2026-09-03.log`); `tests/poi-coordinate-contract.mjs` 114/114, мутации 10/10; открытая граница — legacy с полной парой обновляется без явного решения (до миграции 444 записей) | нет |
| 2026-09-03 | 10f-P, остаточный аудит P07 (круг 2) | P07.3 | `IN_PROGRESS` 2/3 → без изменения (кандидат 3/3, `READY_FOR_CODEX_AUDIT`); счёт 19/30 не меняется | закрыты три остаточных дефекта refresh-писателей координат, воспроизведённые на обоих настоящих путях (`tmp/10f-p-p07-refresh2-repro-OLD-2026-09-03.log`): (1) решение владельца (`representativePoint`/`notApplicable`), принятое после первоначального чтения, затиралось старым планом — теперь план строится по свежему предзаписному чтению ровно записей пачки (`filterByFormula` по `RECORD_ID()`) и при расхождении со снимком откладывается (`changedSinceRead`); (2) повреждённое нестроковое/пробельное значение политики → `corruptPolicy`, не `legacyGoogleCache`; (3) единая семантика `hold`/неизменившейся пары — оба писателя шлют ровно `plan.fields`. Общий контракт `src/lib/poi-coordinate-refresh.ts` (`planCoordinateRefresh({selected,fresh,…})`, `readPolicyField`, `ownerStateDifference`, `freshReadFormula`); `tests/poi-coordinate-refresh.mjs` 185/185 (настоящий route.ts под Node с гонкой через `onRequest`, настоящий скрипт дочерним процессом), `tests/poi-coordinate-contract.mjs` 114/114; мутации обеих writer-границ и контракта 14/14 жизнеспособных (`tmp/10f-p-p07-refresh2-mutations-2026-09-03.log`, один эквивалентный subsumed); тест на коде до аудита — провал (`tmp/10f-p-p07-refresh2-old-vs-new-2026-09-03.log`). Граница: у Airtable нет условной записи, окно свежее-чтение→PATCH — один запрос | нет |
| 2026-09-03 | 10f-P, остаточный аудит P07 (круг 3, по находкам Codex) | P07.3 | `IN_PROGRESS` 2/3 → без изменения (кандидат 3/3, `READY_FOR_CODEX_AUDIT`); счёт 19/30 не меняется | P1 закрыт как обнаружимая гонка: владелец, изменивший запись между свежим чтением и PATCH, получал смешанное состояние при `ok: true`/коде 0 (`tmp/10f-p-p07-refresh3-repro-OLD-2026-09-03.log`) — теперь после каждой пачки независимое чтение и `classifyPatchOutcome` (`verified`/`notApplied`/`recoveryRequired: outcomeMismatch|outcomeUnverified`), крон `ok: false` + HTTP 500 + `требуетВосстановления` с record ID, скрипт код 1; ровно один PATCH, без повтора/отката/исправления. P2 (границы): все брошенные значения (null, Symbol, бросающий getter, отозванный Proxy) на первичном, свежем, итоговом чтении и PATCH описываются `describeThrownSafely` — писатель не покидают. P2 (документы): README § 2, DAG 1.5/1.9/4.9, счётчики ledger приведены к реализации. `tests/poi-coordinate-refresh.mjs` 239/239 (секция 3в на обоих настоящих писателях), `tests/poi-coordinate-contract.mjs` 114/114; мутации контракта и обеих writer-границ 13/13 (`tmp/10f-p-p07-refresh3-mutations-2026-09-03.log`). Не устранено и названо: CAS у Airtable нет — гонка не исключена физически, только обнаруживается | нет |
| 2026-09-03 | 10f-P, остаточный аудит P07 (круг 4, по находкам Codex) | P07.3 | `IN_PROGRESS` 2/3 → без изменения (кандидат 3/3, `READY_FOR_CODEX_AUDIT`); счёт 19/30 не меняется | P0 — оба писателя продолжали PATCH следующих пачек после `recoveryRequired` (репро на 11 записях: одиннадцатая уходила вторым PATCH, `tmp/10f-p-p07-refresh4-repro-OLD-2026-09-03.log`) — теперь пачка с расхождением классифицируется целиком и хвост не пишется (крон `остановлено`, скрипт «ОСТАНОВЛЕНО после пачки N»); P1 — `Coords Checked At` не сверялась, старая отметка после PATCH считалась подтверждённой — теперь сверяется каждое поле плана, отметка по значению момента (`parseMoment`, строгий ISO 8601 → epoch ms), писатель ставит отметку целыми секундами (`checkedAtMoment`), `notApplied` требует и прежней отметки. `tests/poi-coordinate-refresh.mjs` 282/282 (секция 3г: 11 записей на обоих писателях, старая отметка при write и hold, контроль честного хранилища), `tests/poi-coordinate-contract.mjs` 114/114; мутации 11/11 (`tmp/10f-p-p07-refresh4-mutations-2026-09-03.log`): удаление остановки хвоста и break (крон/скрипт), исключение отметки из сверки, сравнение строкой, нестрогий разбор, отметка с миллисекундами (контракт/крон/скрипт), `notApplied` без отметки. Harness теперь хранит dateTime с секундной точностью, как Airtable | нет |
| 2026-09-03 | 10f-P, остаточный аудит P07 (круг 5, по находке Codex) | P07.3 | `IN_PROGRESS` 2/3 → без изменения (кандидат 3/3, `READY_FOR_CODEX_AUDIT`); счёт 19/30 не меняется | остаточный дефект временного контракта: `parseMoment` опирался на `Date.parse`, который нормализует переполнение — `parseMoment('2026-02-31T12:00:00Z')` совпадал с `2026-03-03T12:00:00Z`, `classifyPatchOutcome` давал `verified`, план с невозможной датой строился (`tmp/10f-p-p07-refresh5-repro-OLD-2026-09-03.log`). Теперь разбор календарно строгий без `Date.parse`: день по месяцу, високосный год 4/100/400, время и смещение зоны в диапазонах, момент — арифметикой `Date.UTC` из проверенных компонентов. `tests/poi-coordinate-refresh.mjs` 302/302 (+20: 31 февраля/апреля, 29.02.2025 и 2100 → null, 29.02.2024 и 2000 проходят, месяц 13, день 00, час 24, минута/секунда 60, смещение +24:00/+00:60, невозможная дата после PATCH → `outcomeMismatch`, план → `checkedAtInvalid`); мутации 7/7 жизнеспособных (`tmp/10f-p-p07-refresh5-mutations-2026-09-03.log`; один эквивалентный отдельно). Остальные исправления круга 4 не менялись | нет |
| 2026-09-03 | 10f-P — принятие P07 владельцем | P07.3 | `IN_PROGRESS` 2/3 → `ACCEPTED` 3/3; всего 19/30 → **20/30**; принято вех 3 → **4** | полный контур Coordinate Policy на всех production-писателях: реестр решений владельца под git (`config/poi-coordinate-decisions.v1.json`, `src/lib/poi-coordinate-decision.ts`), единственный create-путь `ingestPoi`, refresh-контракт `src/lib/poi-coordinate-refresh.ts` для крона и ручного скрипта (предзаписная сверка, итог только чтением после PATCH по каждому полю плана, `recoveryRequired`, остановка хвоста, безопасные границы, календарно строгий `parseMoment`); `tests/poi-coordinate-contract.mjs` 114/114, `tests/poi-coordinate-refresh.mjs` 302/302, `tests/poi-coordinate-decision.mjs` 167/167, `tests/poi-coordinate-policy.mjs` 84/84; мутации по кругам 10/10, 15/15, 14/14, 13/13, 11/11, 7/7 (`tmp/10f-p-p07-*mutations*`); независимый аудит Codex — пять кругов находок (расхождение семантики писателей, гонка после чтения, гонка при PATCH и границы, продолжение после `recoveryRequired` и отметка, календарная строгость) закрыты; принято владельцем 03.09.2026. Правило: переоткрытие P07 — только по новому production-контрпримеру | нет |
| 2026-09-03 | 10f-P — P06.3 вынесен владельцу | P06.3 | `IN_PROGRESS` 2/3 → без изменения (`BLOCKED_OWNER`); счёт 20/30 не меняется | пять заблокированных решений сопоставления оформлены как решения владельца с фактами из замороженной фикстуры (чтение 03.09.2026), определениями классов ADR § 9.2, вариантами и следствиями для eval: Q1 `same_physical_place` (GLION ⟷ склад), Q2/Q3 `co_located` (дома-проекты Наосимы / ворота Тосёгу), Q4/Q5 действие `needs_review` (дубль без координат / арт-поле ⟷ Нобутай) — `docs/poi-intake/p06-matching-decisions-2026-09-03.md`; минимальный закрывающий набор назван; смежное непринятое правило версии политики (DAG 4.4) отмечено; ни код, ни фикстура, ни живая база не менялись | нет |
| 2026-09-03 | 10f-P — решения владельца по P06.3 применены | P06.3 | `IN_PROGRESS` 2/3 → без изменения (`BLOCKED_OWNER`); счёт 20/30 не меняется | Q1–Q5 = A внесены в `tests/fixtures/poi-matching-eval/v1.json`: четыре новые пары (`glion-red-brick-warehouse` same_physical_place, `art-house-kinza-gokaisho` и `yomeimon-karamon` co_located, `echigo-tsumari-nohbutai` part_whole/needs_review) с `provenance.kind = owner_decision`, ссылкой на документ решений и дословной цитатой; `fuji-5th-station`/`sendai-castle-ruins` → `needs_review` (истина same_product_poi не менялась); baseline переснят штатно (`JJ_EVAL_REBASELINE=1`), политика `poi-matcher-policy/v1` и отпечаток прежние, 21 пара, coverage 0,619, abstention 0,381, exact 0,667; `tests/poi-matching-eval.mjs` 233/233. Координаты POI-000345 (0 м → как у POI-000340) и POI-000449 (11 м от POI-000445, сдвиг на север) восстановлены по замороженным расстояниям — в репозитории не сохранены, помечено в парах. Обнаружено при применении: Q4 = A оставил действие `duplicate` без единой доказанной пары — `complete` недостижим, поправка исполнителя и вопрос Q6 (E/C/D) в документе решений; живая база не менялась | нет |
| 2026-09-04 | 10f-P — Q6 = C: поиск пары-дубля исчерпан | P06.3 | `IN_PROGRESS` 2/3 → без изменения (`BLOCKED_OWNER` → `BLOCKED_EVIDENCE_GAP`); счёт 20/30 не меняется | по решению владельца Q6 = C (04.09.2026) выполнен ограниченный read-only поиск пары-дубля с координатами у обеих записей: артефакты на диске (аудит § 2, nearPairs baseline 01.09, корпуса 26.08/11.08, отчёты check:poi 26.08 и 03.09) пары не содержат; GET-скан только таблицы POI (5 GET, все 200, 471 запись, 460 живых, 450 с координатами; скрипт из песочницы вне репозитория) — 24 кандидата матчера, 0 с общим Google Place ID, 0 с равным русским именем, 3 blockingDuplicate — все связаны Parent POI; ни один не доказан как same_product_poi. Итог BLOCKED_EVIDENCE_GAP записан в `docs/poi-intake/p06-matching-decisions-2026-09-03.md` (таблица 24 кандидатов) и в вопрос Q6 фикстуры (ответ C, исход, оставшиеся варианты E/D/иное); в eval пар не добавлено; Q4 = A, `requiredCoverage`, политика v1 и живая база не менялись; `tests/poi-matching-eval.mjs` 233/233; журнал скана `tmp/10f-p-p06-q6c-scan-2026-09-04.{json,log}` | нет |
| 2026-09-04 | 10f-P — Q6 = F: производный контроль на паре Фудзи | P06.3 | `IN_PROGRESS` 2/3 → без изменения (`BLOCKED_EVIDENCE_GAP` → `READY_FOR_CODEX_AUDIT`); счёт 20/30 не меняется | по решению владельца Q6 = F (04.09.2026) в `tests/fixtures/poi-matching-eval/v1.json` добавлен производный конструированный контроль `fuji-5th-station-derived-control`: `kind = derived_control`, `constructed = true`, `derivedFrom = fuji-5th-station`, та же доказанная идентичность (аудит § 2, 06.08.2026), входящей записи подставлена подтверждённая точка POI-000239 (35.394895, 138.73258; источник — блок `existing` пары, liveReadAt 03.09.2026, корпус 26.08.2026), `expectedDecision = duplicate`, `expectedVerdict = blocked_duplicate`, `historicalClaim = false`, решение дословно в `provenance.quote`; эмпирическая пара Фудзи не изменена (needs_review, Q4 = A); всем 21 прежним парам проставлен `kind = empirical`; eval (`poi-matching-eval/v1`) различает роды пар в контракте (обязательные поля контроля, вердикт = `expectedVerdict`, дубли обязаны быть и среди эмпирических) и в отчёте (`kindCoverage`: действие `duplicate` закрыто только контролем; `metricsByKind`); Q6 перенесён в `ownerDecisions`/`resolvedOwnerQuestions`, `ownerQuestions` пуст, статус `complete`; baseline переснят штатно при той же политике v1 (`sha256:8a701717…`), 22 пары, единственное изменение — `observedBaseline` контроля; eval 322/322; 13 мутаций фикстуры и мутация гео-ветки матчера убиты, мутация ветки «имена + город без гео» эквивалентна для фикстуры и ловится `tests/poi-matching.mjs` (`tmp/10f-p-p06-q6f-mutations-2026-09-04.md`); Q4, `requiredCoverage`, политика, живая база не менялись | нет |
| 2026-09-04 | 10f-P — инвентарь калибровочных параметров матчера, политика v2 | P06.3 | `IN_PROGRESS` 2/3 → без изменения (`READY_FOR_CODEX_AUDIT`); счёт 20/30 не меняется | воспроизведён дефект (песочница вне репозитория, `tmp/10f-p-p06-inv-repro-OLD/MUT-2026-09-04.json`): part/whole cutoff 0,95 → 0,7 и parent ambiguity gap 0,1 → 0,2 в теле `screenNewPoi` меняют вердикт («Роппонги Хиллз» ⟷ «Роппонги» clear → needs_review) и выбор Parent POI (POI-A → null/ambiguous) при прежних `poi-matcher-policy/v1` и `sha256:8a701717…`; eval 322/322 и `tests/poi-matching.mjs` не ловили ни одну из правок по отдельности. Инвентарь всех числовых литералов `src/lib/poi-matching.ts` и `scripts/poi-portals/lib/dedupe.mjs` по AST (`typescript`): 15 параметров алгоритма перенесены в `MATCHER_POLICY` (nameCoreMinLength, namespacePartMinLength, skeletonMinLength, skeletonContainmentMinShort/MinExtra/Fallback, headUnknownFloor, containmentCoreMinLength, cityTokenMinLength, partWholeCutoff, parentAmbiguityGap, ngramSize, scoreDecimals, distanceDecimals, earthRadiusM) — значения прежние; остальные литералы — 21 поимённое исключение трёх категорий (math / identity / report) с обоснованием и точным числом покрытий, 0 незаявленных; каждое поле политики читается кодом. Версия `poi-matcher-policy/v1` → `v2`, отпечаток `sha256:554a9cfd…`; baseline eval переснят штатно — изменений 0, фикстура кроме `matcherPolicy` побайтно прежняя, empirical/derived_control сохранены. `tests/poi-matching.mjs` 279 → 368 (инвентарь + поведенческий пин каждого параметра), `tests/poi-model-plan.mjs` ожидает v2. Мутации (`tmp/10f-p-p06-inv-mutations-2026-09-04.md`): 11 значений — каждая убита отпечатком и поимённой проверкой; 11 обходов (число зашито обратно при том же значении, новый литерал, новая константа, лишний элемент отчёта, `dedupe.mjs`) — убиты инвентарём; контроль Фудзи на v2 различающий (гео-ветка → needs_review роняет его вердикт и baseline). `npm test` 104 сюиты, tsc 0, eslint 0 ошибок; change-policy § 23 дополнен правилом инвентаря. Q1–Q6, expectedDecision, requiredCoverage, живая база, Git — без изменений | нет |
| 2026-09-04 | 10f-P — аудит P06.3, круг 2: словари матчера в отпечатке, DAG | P06.3 | `IN_PROGRESS` 2/3 → без изменения (`READY_FOR_CODEX_AUDIT`, круг 2); счёт 20/30 не меняется | по замечаниям аудита (P1 нечисловая политика вне отпечатка; P2 DAG устарел): `GENERIC_HEAD`, `GENERIC_TAIL`, `GENERIC_CLASS`, `EN_GENERIC`, `RU_GENERIC`, `POLIVANOV` и найденные аналоги (складывания ё/э → е, артикли, скобки/кавычки/точки, разделитель коллекции, скобки уточнения, макроны, гласные ромадзи) собраны в замороженную запись `MATCHER_LEXICON` (`src/lib/poi-matching.ts`); регулярки и множества строятся из неё; `matcherLexiconDigest` (SHA-256, порядок значим) входит строкой в `matcherPolicyDigest`, фикстура несёт `matcherPolicy.lexiconDigest`. Эквивалентность до/после: 6 448 унарных и 87 990 парных проверок на 806 именах — 0 расхождений (`tmp/10f-p-p06-lex-equivalence-2026-09-04.json`); baseline переснят штатно — изменений 0, фикстура кроме `matcherPolicy` побайтно прежняя; версия `poi-matcher-policy/v2` сохранена (не была ни в коммите, ни в отчёте), отпечаток `sha256:554a9cfd…` → `sha256:6798f973…`, словари `sha256:0b6585f3…`. `tests/poi-matching.mjs` 368 → 431: отпечаток словарей меняется от каждого поля, порядка и одного слова; 17 поведенческих пинов словаря; инвентарь таблиц (каждая верхнеуровневая таблица и каждый `new RegExp/Set/Map` с литералом — из словаря) и регулярок-литералов (13, восемь поимённых исключений структуры). Мутации (`tmp/10f-p-p06-lex-mutations-2026-09-04.md`): 9 правок словаря убиты отпечатком (8 — и поведением; макрон — только отпечатком, после NFD он избыточен), 6 обходов — инвентарём. DAG: узлы 2.1, 2.4, 2.6 приведены к факту (политика версионирована с отпечатком чисел и словарей, носитель-манифест 2.1 не начат; eval `complete`, ждёт аудита). change-policy § 23 дополнен словарями. `npm test` 104 сюиты, tsc 0, eslint 0 ошибок. Q1–Q6, expectedDecision, requiredCoverage, живая база, Git — без изменений | нет |
| 2026-09-04 | 10f-P — аудит P06.3, круг 3: глубокая заморозка словарей, политика v3 | P06.3 | `IN_PROGRESS` 2/3 → без изменения (`READY_FOR_CODEX_AUDIT`, круг 3); счёт 20/30 не меняется | по замечанию аудита (заморозка `MATCHER_LEXICON` поверхностная; версию поднять): `deepFreeze` рекурсивно замораживает запись словарей и все вложенные массивы/объекты (98 узлов); `MATCHER_POLICY_SPEC` → `poi-matcher-policy/v3` (состав политики изменился после зафиксированной v2), отпечаток `sha256:43513158…`, словари `sha256:0b6585f3…` без изменений; baseline eval переснят штатно — изменений 0, фикстура кроме `matcherPolicy` побайтно прежняя, контроль Фудзи на v3 — `blocked_duplicate`. `tests/poi-matching.mjs` 431 → 451: каждый вложенный узел заморожен, 14 попыток вложенной правки бросают `TypeError`, словарь и отпечатки после попыток прежние, политика плоская; `tests/poi-model-plan.mjs` ожидает v3. Мутации (`tmp/10f-p-p06-deep-mutations-2026-09-04.md`): поверхностная заморозка, заморозка на один уровень, без массивов, пустышка, версия v2 при новом составе — все убиты поимённо. `npm test` 104 сюиты, tsc 0, eslint 0 ошибок; change-policy § 23, DAG 2.4/2.6, памятка P06 обновлены. Q1–Q6, expectedDecision, requiredCoverage, живая база, Git — без изменений | нет |
| 2026-09-04 | 10f-P — принятие P06.3 независимым аудитом | P06.3 | `IN_PROGRESS` 2/3 → `ACCEPTED` 3/3; всего 20/30 → **21/30**; принято вех 4 → **5**; пакет 10f-P закрыт (P04.3, P06.3, P07.3 приняты) | аудит Codex принял третий критерий P06 после трёх кругов: политика матчера `poi-matcher-policy/v3` (`src/lib/poi-matching.ts`) — версия, SHA-256-отпечаток 34 числовых параметров и глубоко замороженных словарей `MATCHER_LEXICON`, полнота закреплена AST-инвентарём (`tests/poi-matching.mjs` 451/451); различающая eval-фикстура `tests/fixtures/poi-matching-eval/v1.json` — `complete`, 22 пары (21 эмпирическая + 1 производный контроль, различимы в контракте и отчёте), привязана к версии, отпечатку и отпечатку словарей, пять метрик ADR § 9.2 в baseline (`tests/poi-matching-eval.mjs` 323/323, `tests/poi-model-plan.mjs` 669/669); вердикты 22 пар за все круги не менялись | нет |
| 2026-09-04 | 10f-Q R0 — стабильный ключ источника, манифест прогона и pre-write gate | P01.2, P08.1–P08.3 | `REOPENED` 2/3 и `QUEUED` 0/3 → без изменения счёта (21/30); оба критерия предъявлены как `READY_FOR_CODEX_AUDIT` | воспроизведено до правки на production-композиции: перестановка строк переносила ключ `row-N` на другой объект и давала `same` по `source_key` с уверенностью 1 (`tmp/10f-q-r0-repro-p01-OLD-2026-09-04.log`), а сравнение прогонов считалось после `writeRun` — запись состоялась при дрейфе входа и политики (`tmp/10f-q-r0-repro-p08-OLD-2026-09-04.json`). Сделано: `opendata-csv/v2` строит ключ только из идентификатора источника (сырым, без подрезки), пять именованных отказов, коллизия отнимает ключ у всех строк группы, поиск коллизий идёт по всей выгрузке до `--limit`; исход `sourceKeyRefused` в раскладке и полная очередь в отчёте; сторож ключей на границе любого адаптера. `run-manifest/v1` (`scripts/poi-portals/lib/run-manifest.mjs`): SHA-256 сырых байтов входа, канонического набора кандидатов и отказов, файлов `--existing`/`--base-snapshot`/`--names`, политика с отпечатком словарей, таксономия, контракт приёма, коммит кода; строгая форма, пересчёт digest, закрытый список версий. Pre-write gate — до store, до резолвера и до первой записи; `--write` требует эталон (`--monitor`) и `--existing`; отказ даёт `write.refused = preWriteGate` и ненулевой код возврата. Проверено: `tests/opendata-csv.mjs` 61, `tests/poi-run-manifest.mjs` 79, `tests/poi-prewrite-gate.mjs` 131 (хранилище и резолвер падают на любом вызове — доказано, что до них не дошли), `npm test` 107 сюит, `tsc` 0, `eslint` 0 ошибок; 19 мутаций убиты поимённо, одна названа эквивалентной (`tmp/10f-q-r0-mutations-2026-09-04.md`) | нет |
| 2026-09-04 | 10f-Q R1 — четыре находки аудита Codex закрыты | P01.2, P08.1–P08.3 | без изменения счёта (21/30); предъявлено повторно как `READY_FOR_CODEX_AUDIT` | Воспроизведено на текущих байтах до правки: (1) файл `--names`, подменённый между `PASS` gate и `writeRun`, применялся писателем — `writeRun` читал его вторично (`tmp/10f-q-r1-repro-toctou-OLD-2026-09-04.log`); (2) изменённые байты production-модуля проходили gate при том же коммите; (3) разные канонические наборы кандидатов проходили при одинаковом сыром digest — совпавший raw отменял сравнение canonical (`tmp/10f-q-r1-repro-code-canonical-OLD-2026-09-04.log`); (4) выгрузка с заголовками `ID,ID` принималась, ключ брался из второй колонки, а `meta.columns` показывал 15 при 16 колонках; `ID` и полноширинный `ＩＤ` не различались вовсе (`tmp/10f-q-r1-repro-headers-OLD-2026-09-04.log`). Сделано: каждый входной файл (`--names`, `--base-snapshot`, `--existing`) читается ОДИН раз в `main` до сборки манифеста, `writeRun` файлы не перечитывает и отвергает непрочитанное или принадлежащее другому пути содержимое; `code.tree` (`poi-code-tree/v1`) — SHA-256 фактических байтов модулей цепочки, обход по правилу (64 файла), три независимые оси кода `codeDrift`/`codeTreeDrift`/`codeDirtyDrift`; тождество входа разведено на независимые оси `inputDrift`/`canonicalDrift` и `inputIncomparable`/`canonicalIncomparable`; `assertHeaderRow` проверяет СЫРУЮ строку заголовков до сборки объектов (точный повтор и эквивалентность после складывания ширины — отказ всей выгрузки), адаптер `opendata-csv/v3`. Найдено разбором мутаций и исправлено сверх находок: двухпроходный разбор по физическим строкам (`to_line`/`from_line`) давал пустую строку заголовков у файла с пустой первой строкой и у заголовка с переносом внутри кавычек — вся выгрузка молча уходила в `sourceIdColumnMissing`; заголовок берётся из первой ЗАПИСИ. Проверено: `tests/opendata-csv.mjs` 86, `tests/poi-run-manifest.mjs` 96, `tests/poi-prewrite-gate.mjs` 166, `tests/base-snapshot.mjs` 78, `tests/names-file.mjs` 29; 50 сюит `npm test` зелены, `tsc` 0, `eslint` 0 ошибок, документные проверки — единственное расхождение чужое (`check:hotel-links` в verify и не в CI, гостиничный трек). 12 мутаций убиты поимённо, одна названа эквивалентной (`tmp/10f-q-r1-mutations-2026-09-04.md`) | нет |
| 2026-09-04 | Независимый аудит Codex: 10f-Q R1 принят частично | P01.2 | `REOPENED` 2/3 → `ACCEPTED` 3/3; счёт 21/30 → **22/30**, принято 6 вех из 10 | P01.2 принят: ключ источника не зависит от позиции строки, каждая строка учтена именованным исходом, неоднозначность заголовков отвергается до объектного разбора. P08 НЕ принят и остаётся 0/3 — четыре блокирующие находки: (1) P0 — `code.tree` не покрывает всю фактически исполняемую цепочку (`src/lib/prefectures.ts`, `config/poi-coordinate-decisions.v1.json` не входят в правило обхода: в песочнице аудитора правка обоих файлов сохранила digest `sha256:32e3439c…` и 64 файла, хотя `canonicalPrefecture` и загрузка решений изменились); (2) P0 — манифест не связывает фактически использованные реестры: реестр координат импортируется статически в `src/lib/poi-coordinate-decision.ts` и используется писателем, но своего digest-компонента не имеет, а байты таксономии читаются для манифеста позднее и отдельно — подписан может быть не тот экземпляр, по которому уже приняты решения; (3) P1 — обязательный `code.tree` добавлен под прежним селектором `run-manifest/v1`: два несовместимых формата под одной версией; (4) P1 — `adapterDrift` продолжает маскировать входные оси (`continue` пропускает сравнение raw и canonical); сырые байты сравнимы независимо от версии адаптера, а canonical при смене версии обязан дать хотя бы `canonicalIncomparable`. Подтверждено аудитом: TOCTOU закрыт для `--names`, `--existing` и `--base-snapshot`; дубли `ID,ID` и `ID,ＩＤ` отвергаются до объектного разбора; raw и canonical независимы при одной версии адаптера; gate расположен до writer/store/resolver; целевые сюиты 86/96/166/78/29, `npm test`, typecheck и `diff --check` зелены | нет |
| 2026-09-04 | 10f-Q R2 — четыре блокирующие находки P08 закрыты | P08.1–P08.3 | без изменения счёта (22/30); предъявлено как `READY_FOR_CODEX_AUDIT` | Воспроизведено на текущих байтах до правки: правка `src/lib/prefectures.ts` и обнуление реестра решений сохранили и число файлов (64), и digest `sha256:32e3439c…` (`tmp/10f-q-r2-repro-codetree-OLD-2026-09-04.log`); компоненты реестра решений в манифесте не было вовсе, а байты таксономии читались отдельным поздним чтением — переформатирование файла давало другой digest при том же значении; манифест формы R0 отвергался версией `run-manifest/v1` как «нет поля tree», то есть две формы носили одну версию; контрпример со сменой версии адаптера, другим raw и другим canonical возвращал ровно один `adapterDrift` (`tmp/10f-q-r2-repro-registries-version-adapter-OLD-2026-09-04.log`). Сделано: состав исполняемого кода ВЫВОДИТСЯ обходом графа импортов от точки входа (`scripts/poi-portals/lib/code-graph.mjs`, `poi-code-graph/v1`) — JSON-реестры входят импортом, границы названы (нечитаемый относительный импорт и нелитеральный `import()` — отказ), голые спецификаторы подписаны замком `code.deps`; реестры таксономии и решений владельца о координатах подписаны ЗНАЧЕНИЕМ, которое загрузил процесс, списком `registries` со своим `registryDrift`/`registrySetDrift`; контракт манифеста поднят до `run-manifest/v2`, а версия проверяется ПЕРВОЙ, до состава полей; `adapterDrift` больше ничего не отменяет — сырые байты сравниваются, канонический набор получает названную `canonicalIncomparable`. Проверено: `tests/poi-code-graph.mjs` 30 (новая сюита), `tests/poi-run-manifest.mjs` 112, `tests/poi-prewrite-gate.mjs` 208, `tests/poi-coordinate-decision.mjs` 168; 51 сюита `npm test` зелена, `tsc` 0, `eslint` 0 ошибок и прежние 184 предупреждения, документные проверки — единственное расхождение чужое (`check:hotel-links`). 13 мутаций убиты поимённо, эквивалентных нет (`tmp/10f-q-r2-mutations-2026-09-04.md`) | нет |
| 2026-09-04 | Независимый аудит Codex: 10f-Q R2 принят частично | P08.2, P08.3 | `IN_PROGRESS` 0/3 → 2/3; счёт 22/30 → **24/30**, принято 6 вех из 10 | Принято: тождество входа, снимка и версии политики (P08.2) и pre-write gate до store и первой записи (P08.3). Подтверждено независимым разбором TypeScript AST — граф совпал 55/55 файлов, без пропусков и лишних; `prefectures.ts` и оба JSON-реестра в графе; оба фактически загруженных реестра связаны с манифестом по значению; `run-manifest/v2` проверяется до формы; `adapterDrift` не маскирует raw/canonical; gate физически до writer/store/resolver; целевые наборы 30/112/208, `npm test`, typecheck и `diff --check` зелены. P08.1 ОСТАВЛЕН ОТКРЫТЫМ — три находки: (1) P0 — граф снимается один раз и поздно, после адаптеров; предварительный снимок есть только у `--model-plan`, поэтому процесс мог исполнять модуль A, а манифест удостоверить заменивший его B; нужны два сравнимых снимка (до адаптеров и перед gate) с остановкой записи при расхождении; (2) P1 — сканер целиком выбрасывает содержимое шаблонной строки, и исполняемый `` `${(await import('./hidden.mjs')).h}` `` в граф не входит: правка файла отпечаток не сдвинула; (3) P2 — JSON входит в граф сырыми байтами и одновременно подписывается по значению, поэтому переформатирование даёт `codeGraphDrift` без `registryDrift` — безопасный ложный блок, но семантика обязана быть одна и объявленная | нет |
| 2026-09-04 | 10f-Q R3 — три оставшиеся находки P08.1 закрыты | P08.1 | без изменения счёта (24/30); предъявлено как `READY_FOR_CODEX_AUDIT` | Воспроизведено на текущих байтах до правки: снимок графа снимался один раз и поздно — процесс продолжал исполнять уже загруженный модуль, а поздний снимок отдавал другой digest (`tmp/10f-q-r3-repro-graph-toctou-OLD-2026-09-04.log`); исполняемый `` `${(await import('./hidden.mjs')).h}` `` в граф не входил и правка файла отпечаток не двигала; переформатирование JSON-реестра давало `codeGraphDrift` без `registryDrift` (`tmp/10f-q-r3-repro-template-json-OLD-2026-09-04.log`). Сделано: снимок кода (граф + замок) снимается ДВАЖДЫ — до первого адаптера и непосредственно перед gate, — и расхождение отказывает прогон до store и до резолвера с названной причиной; шаблонные строки разбираются (текст отбрасывается, `${…}` читается как код, вложенность любая), а полнота разбора проверяется по хвосту: каждое ключевое слово `import` обязано оказаться одной из четырёх разобранных форм, иначе отказ с именем файла; байтовая ось `code.graph` и ценностная ось `registries` объявлены раздельно в трёх документах и закреплены проверкой. Проверено: `tests/poi-code-graph.mjs` 48, `tests/poi-prewrite-gate.mjs` 223, `tests/poi-run-manifest.mjs` 112; 51 сюита `npm test` зелена, `tsc` 0, `eslint` 0 ошибок, документные проверки — единственное расхождение чужое. 10 мутаций убиты поимённо, эквивалентных нет (`tmp/10f-q-r3-mutations-2026-09-04.md`) | нет |
| 2026-09-04 | Независимый аудит Codex: 10f-Q R3 принят; пакет 10f-Q закрыт | P08.1 | `IN_PROGRESS` 2/3 → `ACCEPTED` 3/3; счёт 24/30 → **25/30**, принято **7 вех из 10** | Принято: двойной снимок кода расположен до адаптеров и перед gate, расхождение останавливает writer; шаблонные интерполяции учитываются, неизвестные формы `import` закрыты fail-closed; разделение байтовой и ценностной осей JSON описано честно. Независимые проверки аудитора: `poi-code-graph` 48/48, `run-manifest` 112/112, `poi-prewrite-gate` 223/223 на временной копии, typecheck и `diff --check` зелены. Одна находка по ТЕСТАМ (не по production): контрпример подмены кода перезаписывал настоящий `src/lib/prefectures.ts` с восстановлением только в `finally` — прерванный процесс оставил бы подмену в рабочем дереве или затёр бы правку владельца. Исправлено: контрпример вынесен в `tests/support/code-swap-counterexample.mjs` и исполняется в песочнице-копии (`createProductionSandbox`; к копируемым путям добавлен `package-lock.json` для оси `code.deps`, добавлен `writeInSandbox` с отказом при выходе за пределы копии); в наборе остались только чтение рабочего файла для утверждения «не изменён» и положительный контроль. Дифференцирующая сила сохранена: мутации «первый снимок не снимается» и «расхождение не останавливает прогон» убиты поимённо (4 и 10 проверок). `tests/poi-prewrite-gate.mjs` 223 → 228 | нет |

## 9. Исправления неверных управленческих утверждений

Эти записи не удаляются при следующей редакции плана.

| Дата | Что было сказано неверно | Исправление |
|---|---|---|
| 2026-08-30 | Разрешение из телефона было представлено как недостаточное, а доступ к Mac — как второе разрешение владельца | Разрешение связано с карточкой и её отпечатком, а не с устройством. Доступ к Mac и системный prompt — только технические условия исполнения |
| 2026-09-01 | Автоматический перенос в Airtable был сначала вынесен за границу готового парсера | Без автоматического переноса доказанных данных готова лишь аналитическая половина. P09 и P10 входят в критерий завершения |
| 2026-09-01 | Полный архитектурный DAG был выдан за линейный остаток задачи парсера | Критический предмет ограничен P01–P10; остальные узлы — зависимости конкретных критериев или отдельный долг |
| 2026-09-02 | «18 из 30, принято 2 вехи» держалось на двух критериях, закрытых по узкому чтению: P01.2 проверялся на источниках со стабильным ID, P02.2 — только на ошибке, покинувшей `main` | Фактически **16 из 30, принято 0 вех**. Оба контрпримера исполнимы и подтверждены двумя аудиторами. Урок процедурный: критерий, доказанный на подмножестве входов, закрытым не считается — доказательство обязано покрывать то множество, которое критерий называет |

## 10. Как обновлять после каждого пакета

1. До работы записать четыре строки из § 1.1.
2. После аудита изменить только соответствующие статусы и счётчики в § 2.
3. Добавить одну строку в журнал § 8 — даже если прогресса не было.
4. Новую находку классифицировать по § 5.
5. При `SCOPE_CHANGE` сначала получить решение владельца и добавить строку в § 6.
6. Сверить арифметику: сумма критериев всегда 30, число закрытых совпадает со сводкой.
7. В отчёте пользователю первой строкой писать: `Pxx: n/3; всего m/30; scope delta 0`.
8. Следующей строкой писать: `task identity: preserved` либо честно называть отклонение.

Архитектурный DAG может уточняться независимо. Если его новый узел не сопоставлен с
одним из P01–P10, он по умолчанию считается `FOLLOW_UP_DEBT`, а не новым условием
готовности парсера.
