# Пять согласованных мест: результат 12.09.2026

Владелец утвердил все пять регистраций и дал команду «Действуй». Штатный
`poi:jg-intake` создал пять черновиков; независимое чтение подтвердило все
записанные поля. Снимок базы: 1055 → 1060 POI. Это результат конкретного
прогона, а не обещание текущего количества записей.

| POI | Название | Тип | Родитель |
|---|---|---|---|
| POI-001161 | Pier 7, Кэсэннума | `shopping_complex` | POI-001157 |
| POI-001162 | Mitsui Outlet Park Sendai Port | `shopping_complex` | POI-001158 |
| POI-001163 | Michi-no-Eki Shiretoko Rausu | `shopping_complex` | POI-001104 |
| POI-001164 | Пристань Нитода | `transport_hub` | POI-000979 |
| POI-001165 | Пристань Одомари | `transport_hub` | POI-000979 |

У всех пяти: `Copy Status: Draft`, `Fact Check Status: Todo`, собственные
RU/EN-описания, досье фактов и точка Google с `exactObjectPoint`. В досье
соответственно 7, 13, 6, 12 и 12 фактов. Опубликованные описания не записывались.
Предыдущие 1055 записей совпали с полным снимком до этой операции.

В очереди разбора e5031, e5036, e5160 и e6859 переведены в `done`; независимое
чтение подтвердило статусы и отсутствие ожидания ответа агента. Снимок очереди:
392 завершены, 47 отложены, 4 в работе. Эти четыре — Инаса, Фукуэ,
Накадори/Вакамацу и Момоти; они не входят в текущую пятёрку.

## Что учтено

- Для пристаней сохранены точные японские названия из страницы перевозчика.
  Остров, Нитода и Одомари явно различены в реестре согласованных объектов.
  Это исполнение принятого состава, а не общее отключение проверки дублей.
- Официальный перевозчик сообщает о заведениях питания на острове, в отличие
  от старого текста Japan Guide; возможность поесть зависит от их работы.
  Билеты оплачиваются наличными, без предварительного бронирования.
- У Раусу сохранены сезонные часы и выходные отдельных магазинов; комплекс
  не подменяет визит-центр на дороге к перевалу.
- В Pier 7 сообщения о прекращении и частичном возобновлении аренды помещений
  не трактуются как закрытие всего комплекса. Часы справочной не выданы за
  общий режим арендаторов.
- На официальной странице аутлета сведения о Wi-Fi противоречат друг другу.
  Расхождение сохранено в фактах, наличие Wi-Fi не обещается.
- Сокращены повторяющиеся служебные пояснения покрытия источников, чтобы досье
  помещались в поле Notes. Факты, копирайт и перечень блоков не удалялись.

## Доказательства и восстановление

Артефакты — `tmp/jg-five-2026-09-12/`:
`execution-contract.json`, `facts-storage.json`, `identification-final.json`,
`verification-full.json`, `base-after-full.json` и журналы проверок.
Первый `verification-five.json` сравнивал снимки с разным набором полей:
его список `concurrentChanges` недействителен. Исправленная проверка по полному
снимку — `verification-full.json`: изменений прежних записей нет.

Живой прогон — `tmp/poi-jg-runs/jg-five-20260912-live/`: пять исходов
`verified`, POST 5, PATCH 0, DELETE 0. Не повторять старое разрешение.
При продолжении сначала проверять Source Key; новые дубли не создавать.

Проверки: пять подготовленных запросов до записи, 48 проверок reviewed Intake,
46 сценариев фактов, `check:docs`, типографика, независимое чтение всех новых
полей и очереди. Живой `check:poi` завершился с кодом 0: поломок нет;
предупреждения о накопленных данных не означают провал проверки.
Production-код не менялся: применяется правило партий данных из AGENTS.md § 3;
полный verify уже пройден для кода e9c3d85, повторный build здесь не запускался.

За четыре диагностических прогона выполнены 22 вызова Google, максимум 7 за
один прогон. Повторения потребовались после уточнения реестра: отчёт связан
с его отпечатком. `actualCharge: null`; фактическое списание не установлено,
расчёт по тарифу за расход не выдаётся.
