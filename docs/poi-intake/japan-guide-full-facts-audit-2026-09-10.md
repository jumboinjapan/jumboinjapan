# Аудит полного чтения Japan Guide — 10.09.2026

Результат: код готов к следующему проходу по операционной карте
`japan-guide-operations.md`. Это внедрение полного чтения и досье, а не завершение
всего импорта или миграция всех старых карточек.

## Закрытые находки

1. Предупреждения в div/aside выпадали из чтения абзацев. Новый читатель сохраняет
   весь alert вместе с его датой; досье обязано сохранить его отдельным фактом.
2. Ограничения 13 абзацев и 12 фактов теряли содержание. Новый reader не обрезает
   текст, copy/v2 не имеет лимита количества фактов. Переполнение транспорта —
   явный отказ, не усечение. Старый copy/v1 остаётся для восстановления прежних запусков.
3. Вложенные ссылки разрывали условия часов; смена кодировки портила японский текст.
   Текст собирается по блокам с сохранением коротких фрагментов. Перекодирование
   использует исходные байты, ошибки видны. Разметка и извлечение не удостоверяют смысл.
4. Адрес оставался в очереди, но не попадал в Google-запрос. Он передаётся в общий
   resolvePlace. Центр встроенной карты с совпадающим названием служит только поисковой
   подсказкой; не становится точкой POI. Чужая карта не используется.
5. Будущее досье не входило бы в отпечаток обычного исполнения. Теперь точные байты
   всех входных файлов связаны через execution-inputs; проверено на настоящем манифесте.
6. В первой реализации UI внутренние Notes попадали в общий публичный маппер.
   Исправлено до выкладки: Notes добавляются только в чтение выбранной админской
   записи. Регрессия исполняет настоящую функцию маппинга с обеими ветвями.
7. Загрузка/отказ API могли выглядеть как отсутствие досье. Состояния разделены;
   предупреждения стоят первыми, статус посещения виден, источник ведёт к разделу
   страницы и указывает фрагмент. Независимый UI review нашёл эти недочёты до выкладки.
8. Рабочие документы одновременно называли JG-2 незавершённым и утверждали, что у
   обновлений нет потребителя. Текущие разделы исправлены; принятая история отделена
   от инструкции следующего прохода. Guard требовал устаревшую фразу о незавершённом
   адаптере; теперь он и его отрицательный тест сохраняют точную текущую границу:
   живые партии работают, вся очередь и объём 100+ ещё не подтверждены.
9. Проверка настоящей карточки после выкладки выявила обрезание завершающего
   перевода строки в Notes общим getAirtableTextField. Досье в базе было целым,
   но UI показывал повреждение. Админский маппер теперь передаёт Notes без
   преобразований. Регрессия исполняет и настоящий маппер, и настоящий текстовый
   helper; прежняя заглушка helper скрывала этот дефект.

## Проверка настоящего корпуса

Повторно прочитаны те же 50 страниц последнего прохода. Итоговый разбор:
50 успешных страниц, 0 отказов, 2271 блок, 7 важных уведомлений, 0 блоков с символом
ошибки декодирования. Это проверка извлечения; не утверждение, что все факты уже
написаны или все карты просмотрены.

Найдены уведомления e4209 (будущий ремонт музея), e4252 (последствия землетрясения),
e4253 (повреждения инфраструктуры), e4257 (закрытые дома), e4259, e4260, e4261
(состояние храмов). Различаются «закрыто», «повреждено, но открыто» и будущие даты;
простое наличие слова closed не закрывает объект автоматически.

Чтение и настройка селекторов: 104 GET Japan Guide суммарно, включая первый
диагностический вариант; финальная пересборка сохранённых HTML — 0 GET. Google и
платный исполнитель модели в этом изменении не вызывались.

## Проверки кода

- Полный непрерывный verify на Node 22.23.0: `tmp/jg-full-facts-2026-09-10/verify-02.log`,
  сохранённый exit 0 в `verify-02.exit`; build завершён. Сетевые check-стадии и build
  выполняли разрешённое чтение Airtable, не запись.
- Регрессия нового пути: 30 именованных сценариев (`facts-tests-07.log`), включая
  приватность и сохранение байтов через настоящий админский маппер.
- Обычный Intake: 23 проверки; разбор родительских/дочерних записей: 36;
  поиск: 11. Реальные store/ingest используются с подменённым HTTP.
- Восемь мутаций убиты соответствующими именованными проверками; положительный
  контроль в той же изолированной копии прошёл. `mutations-admin.json`.
- Финальные eslint затронутого кода, check:docs, typography и diff --check — без ошибок.
- Полные статьи, выгрузки базы и тестовые каталоги остаются вне коммита в tmp/.
- После проверки живого UI выполнен verify-03: все тесты, включая 30 новых,
  прошли; команда завершилась exit 1 на требовании старой фразы документации.
  Guard и его тест затем обновлены: doc-contracts 84/84, check:docs без расхождений,
  eslint изменённых файлов и типографика прошли. Это не выдаётся за полный exit 0
  исправленного дерева; проверка точного коммита выполняется CI на Node 22/24.

## Проверка живого хранения

В существующий Draft / Todo POI-000660 «Храм Ёсиминэдэра» добавлены 18 фактов.
Команда copy/v2: `fullfacts-660-write-20260910`, исход verified, 6 GET и 1 PATCH;
подготовительная репетиция — ещё 1 GET. Сравнение полных записей до и после
подтвердило единственное изменённое поле Notes. Оба описания, статусы и опубликованные
поля сохранены. Доказательство: `canary-field-proof.json` и журнал в
`tmp/poi-jg-copy-runs/fullfacts-660-write-20260910/`.

Пять блоков изображений/карты честно оставлены unresolved: это проверка хранения
и показа, не утверждение о полном визуальном разборе этой статьи. Повторная запись
для исправления показа не требуется: исходные байты в Airtable корректны.

## Граница приёмки

Досье создаёт агент по полному доказательству; код проверяет форму и связи, а не
истинность каждой фразы. Изображения требуют просмотра, официальные актуальные
подтверждения — дополнительной проверки. Досье больше 90 000 символов требует
отдельного решения хранения; автоматического разбиения по записям нет.
Восемь ответов владельца § XIII и полная миграция прежних коротких Notes остаются
рабочей очередью, не скрываются статусом готовности адаптера.
