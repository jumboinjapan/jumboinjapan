```
Status: scope opened, acceptance in progress
Scope ID: poi-autoupdate-pilot-v1
Scope opened: 2026-09-08
Owner: Eduard Revidovich
Canonical current status: docs/poi-intake/README.md
Acceptance checks: 15
Checks met: 6
Milestones accepted: 2 of 5
Accepted: U1–U2, 2026-09-08 (10h-B, R3)
```

# Пилот автообновлений (SC‑005): реестр приёмки

Это **вторая область** после Airtable Intake Core (`parser-completion-ledger.md`,
`poi-parser-v1`, 30/30). Решение владельца I‑1.1 от 06.09.2026: пилот автообновлений — новый
пакет со своими критериями и своей приёмкой; знаменатель парсера 30/30 он не меняет и его
полномочий не наследует. Решения OD01–OD04 из аудита «Астра» 06.09.2026 заменены строками
опросников I и II (`pilot-owner-decisions-2026-09-06.md`); здесь они переоформлены в
критерии. Форма реестра — та же, что у парсера: в каждой вехе ровно три критерия, источник
точных enum и поведения — код, реестр называет границу и исполняемое доказательство.

## 1. Граница области

Пилот обновляет **существующие** записи POI по данным источника. Решениями владельца область
сужена до: поле — **только часы работы** (I‑2.2); записи — **только с `Source Key`** (I‑2.3;
legacy‑записи без ключа и сопоставленные матчером записи не обновляются); источник —
**только BODIK** (I‑2.4; Google Places — не источник обновлений); запуск — **только вручную**,
каждый прогон по отдельному разрешению (I‑4.1); при расхождении источника и базы по любому
другому полю — `needs_review`, человек решает (I‑4.4); частичный отказ серии — остановка с
сохранением доказанного префикса (I‑4.6). Правило поштучного, пополевого согласования
остаётся (I‑2.1). U1 и U2 приняты; пилот пока работает в режиме «только отчёт»:
писатель часов (U4) не подключён, живая серия требует отдельного разрешения.

Не входят в область: редакторские пути админки и крон координат на Vercel (журнал для них —
отдельный пакет, решение IV‑2 от 08.09.2026), удаление записей (DAG 2.9), миграция 444
legacy‑записей (после журнала, II‑6.3), статус «закрыто», координаты, имена и описания
(I‑2.2), автоматический стоп по месячному потолку Google (I‑4.2 — учётный предел).

## 2. Пять вех

| ID | Граница | Три критерия | Владеющий код и доказательство | Состояние |
|---|---|---|---|---|
| U1 | Журнал обновлений (DAG 2.8) | ✓ намерение `observe` (прежние значения по свежему чтению) и `update` (точная нагрузка PATCH) долговечно журналируются ДО эффекта; ✓ исход устанавливается только независимым чтением по `recordId`, закрытый список состояний с ровно двумя успешными; ✓ поздняя сверка по журналу без эффектов, тот же инструмент `poi:reconcile` | `scripts/poi-portals/lib/update-journal.mjs` (`poi-update-journal/v1`), `verified-update.mjs` (`poi-verified-update/v1`), `reconcile-writes.mjs` (`poi-update-reconcile/v1`), `airtable-store.mjs` (`readFreshByRecordId`, `update`); `tests/poi-update-journal.mjs`, `tests/poi-verified-update.mjs` | **ACCEPTED — независимый аудит Codex 08.09.2026 (10h-B, R3), 3/3**; production‑потребителя нет |
| U2 | Карточка и разрешение серии | ✓ серия идёт только по карточке `recordId + old → proposed`, собранной по свежему чтению до серии, с проверкой прежнего значения перед каждой строкой (`deferred` без эффекта при дрейфе или разошедшемся тождестве); ✓ разрешение владельца привязано к отпечатку карточки, списку полей и потолку PATCH, одноразово по отпечатку; ✓ прерванная серия оставляет доказанный префикс, хвост не начинается, карточка восстановления собирается по свежим чтениям без применённых строк | `update-card.mjs` (`poi-update-card/v1`), `update-approval.mjs` (`poi-update-approval/v1`), `runUpdateSeries`; те же сюиты | **ACCEPTED — независимый аудит Codex 08.09.2026 (10h-B, R3), 3/3** |
| U3 | Наблюдение часов из BODIK (режим «только отчёт») | ✓ контракт наблюдения: пустой, противоречивый или временный факт не маскируется под постоянный, тождество записи — только `Source Key`; ✓ отчёт «что изменилось бы» по записям с `Source Key` без единого эффекта, полный список `old → proposed` (не обрезанный sample); ✓ расхождения по другим полям попадают в очередь `needs_review`, а не в карточку | не начат | не начат |
| U4 | Писатель часов | ✓ единственный production‑потребитель `withVerifiedUpdates`, поля — только из разрешения; ✓ сухой эталон → разрешение → прогон, ноль PATCH вне разрешённых строк; ✓ отчёт из журнала, а не из ответов API (`applied`/`deferred`/`recoveryRequired` поимённо) | не начат | не начат |
| U5 | Ограниченная живая приёмка | ✓ первый update‑run по карточке ≤ потолка разрешения на реальных записях с `Source Key`; ✓ каждый исход независимо перечитан, `recoveryRequired` = 0 либо разобран руками по журналу; ✓ повторный прогон по той же карточке и тому же разрешению отвергнут | не начат; зависит от DAG 5.4 (независимая сверка импорта) по узлу 5.5 | не начат |

Итог: **6/15 критериев, 2 из 5 вех приняты.** U1 и U2 приняты независимым аудитом
Codex 08.09.2026 (10h-B, R3). Доказательство: `poi-update-journal` — 125 проверок,
`poi-verified-update` — 184; пять регрессий аудитора, четыре случая синхронных и
асинхронных отказов уведомления и три мутации возврата дефекта. Все находки R1/R2 закрыты.
Производственного потребителя нет; принятие ядра не разрешает живой PATCH.

## 3. Подтверждённые живые переходы

Пока ни одного: пилот живых эффектов не производил. Таблица заполняется по образцу
`parser-completion-ledger.md` § 3 после первой разрешённой серии (U5).

## 4. Как поддерживать реестр

Как у парсера (`parser-completion-ledger.md` § 4): критерий переходит в `ACCEPTED` только по
независимому аудиту с названным доказательством; переоткрытие — только production‑контрпримером;
знаменатель 15 меняется только решением владельца, записанным в
`pilot-owner-decisions-2026-09-06.md`. Текущий статус области — в `README.md` § 3; открытые
зависимости — в `poi-completion-dag.md` (узлы 2.8, 5.5).
