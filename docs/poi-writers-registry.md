# Реестр писателей POI

Уточнение 10.09.2026: новые writer не добавлены. `ingestPoi` сохраняет optional
`poi.factDossier` в собственном блоке Notes; `intake-japan-guide.mjs --write` требует
проверенный facts-пакет. Существующий `copy-japan-guide.mjs` принимает copy/v2 и заменяет
только собственный блок досье, сохраняя прежние guard, журнал и повторное чтение.
Схема Airtable не менялась. Детали — `poi-intake/japan-guide-operations.md`.


```
Status: current inventory
Дата последнего содержательного обновления: 8 сентября 2026 года
Canonical current status: docs/poi-intake/README.md
Карта изменений: docs/poi-intake/agent-maintenance-guide.md
```

Первый обход выполнен 11.08.2026; реестр дополнен контрактами таксономии, координат и
надёжной записи 03–06.09.2026. Инвентарь читают как минимальный набор известных писателей,
а не как доказательство полноты навсегда: при любом новом POST/PATCH/DELETE к POI обход
повторяют и обновляют этот файл в том же коммите.

Метод актуализации: найти все вызовы `POST`, `PATCH`, `PUT` и `DELETE` в `src/` и
`scripts/`, затем по коду отобрать те, что адресуют таблицу POI. Число совпадений не
фиксируется в прозе: оно не доказывает полноту следующего обхода. Ложные кандидаты
отбрасываются по цели запроса, а не по имени файла; проверочные скрипты отдельно проверяются
на отсутствие записывающего метода.

## Japan Guide: штатное исполнение сохранённых результатов

`npm run poi:jg-intake` (`scripts/poi-portals/intake-japan-guide.mjs`) использует тот же
`ingestPoi` и `airtable-store.mjs` за `withVerifiedWrites`. Собственного writer нет.
Без live-флага работает на локальном `base.json`; `--live-read` делает только GET.
`--write` оформляет техническое разрешение v2 по решению владельца VI и создаёт до
25 Draft / Todo с maxRenames = 0. Поля публикации и PATCH/DELETE запрещены в транспорте.
Входы JA-3/JA-4 сохранённые: вызовов Google/модели нет.

Каждый запуск сохраняет входы, свежую базу, эталон, запросы, разрешение и полный отчёт
в `tmp/poi-jg-runs/<runId>/`; журнал — там же `journal.ndjson`. Повтор с новым runId
сверяет базу и пропускает уже принятые Source Key. Неустановленные исходы прежних
журналов этой команды проверяет общий `reconcileWriteJournal`; неразрешённый исход
останавливает запись. Исходный журнал неизменяем. Локальный `active.lock` исключает
одновременные запуски этой команды в одном checkout; это не распределённая блокировка
Telegram и других писателей. Команды и восстановление — в runbook, § «Штатная запись
Japan Guide из сохранённых отчётов».

## Сводная таблица

| # | Путь | Операция | Поля | Гейты | Dry-run | Аудит | Кто запускает |
|---|---|---|---|---|---|---|---|
| 1 | `poi-intake.ts` → `ingestPoi` | create | полный набор + маркеры | канон, идемпотентность, дубли, `needs_review`, один place_id, политика координат (машинно либо по реестру решений владельца — 10f-P) | `dryRun: true` | Intake-маркер + Notes | чатбот Telegram |
| 2 | `poi-portals/lib/airtable-store.mjs` → `ingestPoiBatch`, за границей `withVerifiedWrites` (10f-R) | create | полный набор + маркеры + четыре поля таксономии v2 (`POI Type`, `POI Facets`, `Type Source`, `Taxonomy Version` — 10f-P) | те же + представимость таксономии и preflight живой схемы: writer сам сверяет схему, которую хранилище отдаёт методом `readSchemaTables` (Meta API, чтение; таблица по каноническому `POI_TABLE_ID`); ветка снимка — только по тождеству фабрики `createMemoryPoiStore`, не по объявлению | `--dry-write`, `--base-snapshot` (объект `createMemoryPoiStore`); в этих режимах границы 10f-R нет вовсе — журнала успеха без эффекта не бывает | Intake-маркер + Notes + **журнал `poi-write-journal/v1`**: намерение до эффекта, исход после независимого перечитывания (`tmp/poi-write-journal/<runId>/journal.ndjson`), поздняя сверка `npm run poi:reconcile` | скрипт парсера |
| 2а | `poi-schema/run-taxonomy-schema.mjs --execute` (bootstrap) → `taxonomy-schema-cli.mjs` → `taxonomy-schema-execute.mjs` | **schema: create field** | ровно четыре поля таксономии v2 в таблице POI (Meta API `POST …/tables/tblVCmFcHRpXUT24y/fields`) | bootstrap сверяет 15 модулей цепочки с карточкой до первого импорта; каноническая проверка карточки (база/таблица/поля из loader) ДО credentials, сети и маршрута свидетеля; замороженная карточка `v5`; предполёт read-only до журнала; эксклюзивный журнал `v2`; единый срок на каждый обмен; полная валидация сырого ответа; любое брошенное значение на границах readSchema/createField (в т.ч. сырой отозванный Proxy) безопасно описывается и не покидает исполнителя; исход любого POST — только свежим чтением, недоступность → `unknown` + `recoveryRequired`, без повтора; терминал `pendingFinalWitness` и поздний вердикт по журналу + новому свидетельству; gate — строгая грамматика журнала ∧ независимый свидетель, привязанный к канонической таблице и требующий ровно четыре fieldId; транспорт сам не выпускает неизвестное исключение | `--verify <card>`, `--witness <card>`, `--verdict <card> --approval <a>` (ноль POST) | журнал `tmp/poi-schema-executions/<отпечаток>/journal.jsonl` | владелец / Codex по отдельному L3-разрешению; **исполнена один раз 03.09.2026** (карточка `5fbdc2bd…`, gate `verifiedSuccess`, четыре поля созданы в `tblVCmFcHRpXUT24y`, журнал `tmp/poi-schema-executions/5fbdc2bd…/journal.jsonl`); повторный `--execute` запрещён; карточки R0–R4 устарели |
| 2б | `poi-portals/lib/airtable-store.mjs` → `update`, за границей `withVerifiedUpdates` (10h-B, DAG 2.8) | update | любые поля, названные карточкой обновления и списком полей разрешения (production `poi:jg-copy` — только Description Draft (RU/EN) и Notes; пилот часов пока без записи); поля тождества `POI ID`/`Source Key` — никогда | карточка `poi-update-card/v1` (recordId + old → proposed по свежему чтению до серии) + разрешение `poi-update-approval/v1` (отпечаток карточки, список полей, потолок PATCH, одноразовость по отпечатку) + свежее чтение перед каждой строкой: дрейф или разошедшееся тождество — `deferred` без эффекта; бюджет считается на границе; серия останавливается на первом неподтверждённом исходе, карточка восстановления — по свежим чтениям без применённых строк | нет (обёртка ставится только на живой путь; dry-run хранилище за границей эффекта не даёт) | журнал `poi-update-journal/v1`: `observe` (прежние значения) и `update` (точная нагрузка) — до PATCH, исход — независимым чтением по `recordId`; `npm run poi:reconcile` | `npm run poi:jg-copy`: только пустые Description Draft (RU/EN) и дополнение Notes, Draft/Todo по свежему чтению, разрешение VI; часы не пишет |
| 3 | `poi-portals/lib/airtable-store.mjs` | update | `POI ID` | разрешение коллизии номеров; с 10f-R ответ PATCH проверяется — отказ переименования больше не проходит молча и роняет создание с именем занятой записи | — | тот же журнал, что и у пути 2 (эффект внутри `create`) | тот же скрипт, внутри создания |
| 4 | `api/cron/refresh-coords/route.ts` | update | `Latitude`, `Longitude`, `Coords Checked At` — ровно `plan.fields` | контракт обновления `src/lib/poi-coordinate-refresh.ts` (10f-P, 03.09.2026): читает `Coordinate Policy`; обновляются только `exactObjectPoint` с полной парой и legacy без политики с полной парой (политика при этом не назначается); `representativePoint`, `notApplicable`, половина пары, пустая пара, неизвестная политика, `exactObjectPoint` без пары — в очередь не попадают и в Google не спрашиваются, сводка называет их по причинам; сдвиг > 3 км — только отметка проверки; план по свежему чтению перед пачкой (`changedSinceRead` → отложено); итог только независимым чтением после PATCH по каждому полю плана, включая `Coords Checked At` по значению момента — `recoveryRequired` (`ok: false`, HTTP 500, список record ID), после первого расхождения хвост не пишется (`остановлено`), без повтора/отката; брошенные значения — `describeThrownSafely` | нет | сводка в логах Vercel (`требуетВосстановления`, `остановлено`) | крон, ежедневно |
| 5 | `airtable.ts` → `updateAirtablePoiTitle` | update | `POI Name (RU)`, `POI Name (EN)` | `requireAdminSession` | нет | нет | админка |
| 6 | `airtable.ts` → `updateAirtablePoiSeoWorkspace` | update | 4 поля черновиков + `Copy Status` | `requireAdminSession` | нет | нет | админка |
| 7 | `airtable.ts` → `syncAirtablePoiApprovedText` | update | `Description (RU/EN)`, approved-поля, `Copy Status` | `requireAdminSession` | нет | нет | админка, «Утвердить и опубликовать» |
| 8 | `airtable.ts` → `deleteAirtablePoi` | **delete** | вся запись | `requireAdminSession` | нет | нет | админка |
| 9 | `scripts/refresh-google-coords.mjs` | update | `Latitude`, `Longitude`, `Coords Checked At` — ровно `plan.fields` | тот же контракт `src/lib/poi-coordinate-refresh.ts`, что у крона: та же таблица допуска, пропущенные по политике печатаются с причиной и в Google не спрашиваются; сдвиг > 3 км — только отметка; неизменившаяся пара — пара + отметка (одна семантика с кроном); при `--apply` план по свежему чтению перед пачкой и итог только чтением после PATCH по каждому полю плана, включая отметку — «ТРЕБУЕТСЯ ВОССТАНОВЛЕНИЕ», код 1, после первого расхождения хвост не пишется («ОСТАНОВЛЕНО после пачки N»), без повтора/отката; брошенные значения — `describeThrownSafely` | по умолчанию, запись по `--apply` | нет | руками |
| 10 | `scripts/fix-poi-typos.mjs` | update | текстовые поля | — | по умолчанию, запись по `--write` | резервный дамп в `tmp/` | руками |
| 11 | `scripts/fix-airtable-typography.mjs` | update | `Description (RU)`, approved, draft, `Working Hours` | — | по умолчанию, запись по `--write` | резервный дамп в `tmp/` | руками |
| 12 | интерфейс Airtable | create / update / delete | любые | — | — | нет | владелец, я |

## Что видно из таблицы

**Создание закрыто.** Оба кодовых пути создания идут через `ingestPoi`, несут все гейты и оставляют Intake-маркер. Третий путь создания — руками в интерфейсе Airtable — маркера не оставляет и остаётся единственным способом завести запись мимо конвейера. Именно так появились 24 пустые записи 10.08.2026. Закрыть его кодом нельзя; можно сделать видимым — запись без маркеров теперь отличима, и `check:poi` даёт по ней `FAIL intake_bypassed` (сделано 11.08.2026).

**Журналирование обновлений.** Старые восемь путей обновления не переведены на общий журнал. С 10h-B (08.09.2026) у парсера есть граница `withVerifiedUpdates` (строка 2б), которая журналирует обновление так же строго, как 10f-R — создание, с 09.09.2026 за ней стоит `poi:jg-copy` для пустых черновых описаний Japan Guide; подключение каждого из восьми путей — отдельная задача (админка и крон на Vercel — с отдельным решением о хранилище журнала, IV‑2). Восстановить прошлое значение поля можно только из истории ревизий Airtable — то есть вручную и в интерфейсе. Для полей, которые правит и человек, и автоматика (`Description (RU)`, `Copy Status`, координаты), это означает: если значение изменилось, ответить «кто и когда» нечем.

**Удаление — одна кнопка без следа.** `deleteAirtablePoi` сносит запись целиком по нажатию в админке. Проверки на входящие ссылки нет: если POI связан как `Parent POI` или упомянут в Route Stops, связи оборвутся молча. Отката нет.

**Мёртвый код удалён 11.08.2026.** `updateAirtablePoiText` экспортировался и не вызывался ниоткуда, при этом писал прямо в `Description (RU)` — то самое опубликованное поле, в обход черновиков и `Copy Status`. Пока функция существовала, она оставалась приглашением обойти редакторский путь.

**Асимметрия dry-run.** У всех трёх ручных скриптов dry-run по умолчанию — это правильно. У четырёх серверных путей (админка, крон) предпросмотра нет вовсе. Для админки это терпимо, там за кнопкой стоит человек; для крона — нет: он правит до сотни записей в сутки без единой возможности посмотреть, что он собирается сделать.

**Гейт координат был раздвоен — с 03.09.2026 он один.** Теперь допуск и состав записи решает один контракт `src/lib/poi-coordinate-refresh.ts`; оба писателя отправляют ровно `plan.fields`, ID полей координат существуют только в контракте, а состав писателей координат проверяется обходом всего `src/` и `scripts/` (`tests/poi-coordinate-refresh.mjs`). Открытая граница, названная явно: legacy-запись без политики с полной парой обновляется как прежде — это единственный случай, где refresh пишет, не имея явного решения; закрыть его может только миграция legacy-записей.

## Надёжная граница записи парсера (10f-R, 05.09.2026)

Пути 2 и 3 — и только они — составляют **parser-owned write-path**: это те эффекты, которые
автоматический перенос данных парсером производит сам, без человека за кнопкой. Доказательство
принадлежности структурное, а не по имени: живое хранилище `createAirtablePoiStore` production
собирает ровно в одном месте (`collect-pois.mjs`), а PATCH переименования живёт внутри его же
`create`. Общая точка приёма `ingestPoi` в границу не входит и не менялась: ею пользуется и путь 1
(Telegram-бот), и расширять пакет на чужие write-path без отдельного доказательства нельзя.

Что даёт граница:

- **Доказательство намерения и исхода.** Перед первым сетевым вызовом в журнал ложится намерение —
  с ожидаемыми значениями полей целиком и их digest, и с `fsync`; после эффекта — исход. Намерение
  трёхшаговое (R2): `prepare` со входом, `create` с полной нагрузкой POST — включая поля, которые
  ставит само хранилище (`POI ID`, `POI Category (EN)`, `Last Seeded At`), — и `rename` с новым
  номером перед каждым внутренним PATCH; хранилище объявляет каждый эффект границе (`onEffect`) и
  дожидается записи намерения, а границе имена полей не принадлежат — она берёт нагрузку из
  объявления (P07 не нарушен). Отказ записи намерения отменяет эффект (его ещё не было); отказ
  записи исхода после возможного эффекта — это `unknown` и `recoveryRequired`, а не «всё хорошо».
  Читатель журнала fail-closed, и та же грамматика стоит у писателя до записи (R3): одна попытка
  на ключ за журнал, `verified` только после объявленного эффекта и с тождеством из базы,
  закрывающая строка выводится из попыток и не может им противоречить; закрытые схемы строк,
  закрытый список способов проверки, календарно строгий момент (R1, R2). Строка подтверждается
  состоянием и `seq` только после долговечной записи и `close()`; после любого отказа дозаписи
  журнал запечатывается — по любому брошенному значению, с причиной от общего безопасного
  описателя и обычной `Error` наружу; код ошибки читается только безопасной процедурой, и это
  относится ко всем путям open/write/sync/close и к цепочке каталогов (R6, R7); читатель распознаёт и называет оборванный хвост
  незавершённого журнала, а пустую физическую строку в любом месте и любые байты после
  `runFinished` отвергает как повреждение (R4, R5, R6). Момент в сверке полей —
  календарно строгий, невозможный момент не подтверждает поле (R5). Долговечность
  охватывает создание журнала полной цепочкой: каждое вновь созданное имя — от корня журналов на
  первом запуске до каталога прогона — фиксируется в содержащем каталоге (R2, R3).
- **Исход устанавливается независимым перечитыванием — и сверкой полного ожидаемого итога.**
  `readFreshBySourceKey` идёт в базу мимо кэша: `findBySourceKey` отвечал из кэша, который
  наполняет тот же writer, — то есть writer подтверждал сам себя. Ожидаемый итог — нагрузка
  `create` с номером последнего `rename`; `verified` требует ровно одной записи, того же
  `recordId` и `POI ID`, что заявил writer, и совпадения каждого поля итога с прочитанным. PATCH
  переименования отказал, а в базе остался старый номер — `mismatch`/`recoveryRequired`, а не
  успех по совпадению остальных полей (R2). Постинвариант уникальности номера граница
  устанавливает сама вторым чтением — `readFreshByPoiId` (R3): отказ внутренней проверки
  уникальности writer'а после POST не становится `verified` из-за совпадения полей строки;
  тождество записи — только из базы, строка без `recordId` — `unknown`; ответ чтения доказывает
  себя сам — запись без запрошенного номера/ключа в ответе даёт `unknown`, фильтру сервера не
  верят (R4). Состояний пять, успех ровно один.
- **Частичный сбой сохраняет проверенный префикс.** Первый неподтверждённый исход останавливает
  хвост; доказанные записи остаются в отчёте поимённо, отказ приезжает ПОЛЕМ рядом с ними.
- **Ненулевой код возврата.** Отказ записи — провал прогона. Терминальный отказ отдельной POI по
  правилам приёма (`already_ingested`, `blocked_duplicate`, `needs_review`, `rejected_canon`) —
  нет: это штатный исход предметной работы.
- **Поздняя сверка без эффектов.** `npm run poi:reconcile -- <журнал> [--resolve]` устанавливает
  состояние чтением базы. Главный аварийный случай — эффект состоялся, исход не записался —
  разрешается по сохранённому намерению: найденная запись сверяется с ожидаемыми полями (R1). Он не
  повторяет запись, не откатывает, ничего не удаляет и журнал не переписывает — сверка пишет свой
  отчёт рядом, а не поверх улики.

Чего граница не даёт: атомарности (её у Airtable нет), автоматического восстановления и
журналирования ЧУЖИХ путей. Пути 4–12 остались как были — их контракты ниже.

## Что из этого следует для контрактов обновления

Восемь прежних путей обновления делятся на четыре контракта по смыслу, и смешивать их в один «update» нельзя:

| Контракт | Пути | Чем отличается |
|---|---|---|
| maintenance refresh | 4, 9 | Значение приходит извне по расписанию, человек не участвует. Нужен срок годности и запись о том, что проверка была. С 03.09.2026 подчинён `Coordinate Policy` через `src/lib/poi-coordinate-refresh.ts`: пишется только то, что по политике и есть кэш Google |
| editorial edit | 5, 6, 7 | Значение выбирает человек. Нужен журнал: кто, когда, что было |
| migration | 10, 11 | Разовая массовая правка. Нужен дамп до и возможность отката |
| owner override | 12 | Правка руками в интерфейсе. Нужна не защита, а распознавание: проектор обязан заметить расхождение и не затирать |

Группы в этой таблице не разбивают двенадцать путей на непересекающиеся части. Путь 12 — интерфейс Airtable — умеет одновременно создавать, обновлять и удалять, и попадает сразу в несколько строк. Путь 3 (разрешение коллизии `POI ID`) в классификацию обновлений не входит вовсе: это внутренний шаг создания и часть контракта create.

Считать так: программных путей создания два (1, 2), обновления восемь прежних (4–7, 9–11 и ручной интерфейс) плюс `poi:jg-copy` (2б), удаления один программный (8) плюс тот же интерфейс.

## Исполнение комментариев владельца (10.09.2026)

`poi:jg-intake --review-selection` подготавливает записанные в каноническом реестре
самостоятельные объекты и вызывает прежний `ingestPoi` за `withVerifiedWrites`.
Реестр связывает ключ с именами и направлением; не является второй таксономией.
Проверенные пары самостоятельных мест допускаются через существующий `force`
только после общей проверки всех остальных записей. Равный Place ID запрещает
такой допуск. Родитель находится через индекс `findBySourceKey`, а не через
`PoiLike`, в котором Source Key отсутствует. Это работает и внутри одной партии.

`poi:jg-copy` дополнительно принимает `poi-japan-guide-review-links/v1` для
существующих записей. Через прежний `withVerifiedUpdates` допускаются только
`Parent POI` и дописанный `Notes`; обе личности и цепочка родителей перечитываются
перед эффектом. Публикация и статусы не меняются. Применение авторского решения
XII — область этого режима; обычный copy-пакет сохраняет ограничение Draft/Todo.

Доказательство: `tests/poi-japan-guide-review.mjs` выполняет реальный Intake и
обновления с подставным транспортом: сохранённая связь, повтор без новых эффектов,
общий Place ID, посторонний дубль, изменённое тождество, цикл и опубликованные поля.
