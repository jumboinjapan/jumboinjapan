# Единый дизайн страниц туров · 20 сентября 2026

## Результат

Три фасада `/city-tour`, `/intercity`, `/multi-day` используют общий
шаблон, одинаковые обложки по размеру, типографику, отступы и карточки маршрутов.
Навигация между форматами осталась в шапке, повторное меню под обложкой удалено. Фотографии и фирменная палитра сохранены.
Длинные вступления и повторные советы заменены одним абзацем выбора.
Транспорт использует ту же сетку и отступы, что и маршруты. Фотографии 16:10 немного ниже основных 4:3, заголовки немного меньше. Вся карточка, включая фотографию, ведёт на прежнюю подробную страницу.
Каждый раздел завершается переходом к личному обсуждению поездки.

Дизайн-код: [tour-collections.md](design/tour-collections.md).
Рабочая копия: `/Users/jumbo/Projects/jumboinjapan-tour-design`.
Ветка: `codex/tour-design-system`, исходный коммит `4d12a31`.
Production-сборка для локального просмотра: `http://localhost:3101/city-tour`.
Переходы к двум другим разделам находятся в основном меню сайта.
Боевой сайт не опубликован и не изменён.

## Файлы

Три `page.tsx`, адаптеры `ExperienceCard`/`MultiDayRouteCard`, отдельный вариант
`TransportCard`, общие `TourCollection`/`TourRouteCard` и CSS-модуль.
Метаданные согласованы в `src/lib/tour-collection-metadata.ts`.
Существующие canonical, JSON-LD, адреса и аналитика сохранены.
У каждого раздела свои Open Graph/Twitter-заголовок, описание и обложка.
Новые зависимости не добавлены.

## Проверка

- `npm ci` и полный `npm run verify` завершились с кодом 0.
- Lint: 0 ошибок, 95 предупреждений в репозитории. TypeScript и сборка прошли.
- Проверены SSR, один h1, canonical, JSON-LD, OG/Twitter, активный раздел,
  18 статических карточек и 9 ссылок на транспорт. Логи в `tmp/tour-design-ssr.json`.
- Все прежние статические ссылки трёх фасадов остаются в HTML;
  сверка в `tmp/tour-design-link-parity.json`.
- 12 сочетаний страниц и ширин 320/390/768/1280px: горизонтальной прокрутки
  и выходящего за экран текста нет. Сетка меняется с одной на две и три колонки.
- В браузере проверены якоря, переходы к маршруту и транспорту, форма контакта,
  навигация мобильного меню и видимый клавиатурный фокус. Форма не отправлялась.
- Визуально проверены обложки, карточки и подписи на компьютере,
  планшете и телефоне. Движение карточек отключается при reduced motion.
- `git diff --check` без замечаний.

## Ограничения

Рабочая копия изолирована от незавершённых изменений владельца.
Ключей Airtable в ней нет: живые части verify штатно пропущены, обращений
к живой базе не выполнялось. Локальный просмотр показывает статические
маршруты. Код загрузки, отбора Published, дедупликации и преобразования
карточек конструктора побайтно совпадает с исходным, но интеграционная
проверка с живым Airtable в этом проходе не проводилась.

Полный журнал: `tmp/tour-design-verify.log`. Он локальный, не включён в Git.
Следующий шаг выпуска — проверить опубликованные программы в preview
с настройками проекта, затем перенести согласованный дизайн на боевой сайт.

Уточнение размера транспортного блока: повторно проверены все три страницы на
320/390/768/1280px и переход по фотографии. Новый журнал проверок:
`tmp/tour-transport-verify.log`.

Последняя корректировка пропорций: общая сетка маршрутов и транспорта,
заголовки транспорта 20px. После неё выполнены ESLint затронутого компонента,
production-сборка и браузерная проверка всех трёх страниц на 320/768/1280px.
Ширина карточек совпадает, фотографии транспорта ниже основных, переполнений нет.
Полный verify относится к предыдущей версии; для этой правки он не повторялся.
Журнал сборки: `tmp/transport-proportions-build.log`.

Повторное меню под обложкой удалено на всех трёх страницах вместе с неиспользуемыми
стилями и параметром current. Проверены ESLint, production-сборка, серверный HTML
всех трёх страниц и отображение в браузере; основное меню в шапке сохранено.
Журнал: `tmp/remove-duplicate-menu-build.log`.
