# Хаконе: архитектурный альбом, вторая версия

21.09.2026. Пересборка по поручению владельца после разбора макета Claude Design
из `Hakone itinerary layout.zip`. Референс использован для композиции;
его React-обвязка, заглушки фотографий и неработающие ссылки не переносились.

## Действующая композиция

- Локальная Cormorant Garamond для крупных заголовков Хаконе. Основной текст
  остаётся гротеском. Глобальный выбор Lora для остальных страниц не менялся.
- Компактная обложка: название и паспорт поездки, затем невысокая панорама.
- Пять самостоятельных блоков программы. Первые два полных предложения
  описания видны сразу; полное описание, заметка гида, часы и билеты
  открываются прежним окном остановки. Порядок и тексты приходят из конструктора.
- Подходящие фотографии показаны у Овакудани и музея под открытым небом.
  При отсутствии фотографии — полноценный текстовый блок без заглушки.
- Светлые дополнения: три колонки, обед — две. Короткий текст виден сразу,
  продолжение и практические сведения раскрываются без повторения анонса.
  До 768px — один столбец.
- FAQ: заголовок слева, вопросы справа; на телефоне последовательный поток.
- Постоянная кнопка обращения и одна спокойная ссылка внизу сохранены.
  Сравнение тарифов и промежуточные призывы не возвращались.

## Данные и границы

Оформление ограничено Хаконе и вариантом `album` общего компонента маршрута.
Прежний вариант других туров не менялся. Записей в Airtable не было.
Счётчик остановок вычисляется из действующего маршрута. Выезд от отеля,
около 10 часов и 1,5–2 часа на автомобиле взяты из существующего FAQ Хаконе;
в паспорте это пока локальные редакционные значения, не новые поля конструктора.

Фотографии по POI используются только как локальный резерв при отсутствии
`photoPath` в конструкторе: `POI-000039` → `hakone-3.jpg`, `POI-000038` →
`hakone-2.jpg`. Они проверены визуально. Исторический seed с ошибочной
привязкой витражной башни к круизу не использовался и не менялся.

Дополнения Tenseien и рестораны пока локальные редакционные записи;
Pola и Lalique — действующие POI. Подключение дополнительных групп к админке
остаётся отдельной работой: [hakone-additions.md](hakone-additions.md).
Метаданные, canonical и структурированные данные сохранены.

## Проверка второй версии

В браузере проверены 320, 390, 768 и 1440px. Горизонтального переполнения
нет; проверены раскрытие Pola с билетами, окно остановки и закрытие Escape.
На 1440px программа начинается примерно на 780px от начала страницы.

`npm run verify` завершился с кодом 0 (`tmp/hakone-revised-verify.log`).
Живые проверки Airtable пропущены из-за отсутствия ключей в рабочей копии;
браузерная проверка выполнялась отдельно с конфигурацией чтения данных.
Финальные изменения анонсов дополнительно прошли адресную проверку ESLint.

## Предыдущая версия

Коммит `720fcad`: [первый альбомный макет](https://jumboinjapan-h1lsrdbc1-jumboinjapans-projects.vercel.app/intercity/hakone).
Его тёмный блок дополнений и общий снимок рядом со списком заменены текущей
композицией. Основной домен не переключался.

## Публикация второй версии

Коммит `9b05d78` опубликован как Vercel Preview:
[открыть обновлённый макет](https://jumboinjapan-1e83wywoo-jumboinjapans-projects.vercel.app/intercity/hakone).
Сборка и публикация завершились успешно; браузер подтвердил оформление,
пять остановок, оба музея в дополнениях и FAQ.
Журнал: `tmp/hakone-revised-vercel-deploy.log`.

Локальный production-предпросмотр на 3101/3100 также обновлён. После
`verify` необходимо убирать сгенерированный `.next/cache/fetch-cache`
перед сборкой с данными: кеш проверки без ключей сохраняет пустые ответы.
После очистки кеша и повторной сборки (`tmp/hakone-revised-live-build.log`)
в браузере подтверждены все пять остановок и действующий SEO-заголовок.
