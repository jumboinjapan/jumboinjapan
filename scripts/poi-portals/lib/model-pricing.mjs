/**
 * Домен закреплённой таблицы цен, `poi-model-pricing/v1`.
 *
 * Модуль намеренно узкий: в нём одна константа и ни одной проверки. Сам
 * контракт таблицы цен — форма строк, единицы, округление и сверка с
 * профилем — приходит вместе с preflight, который единственный считает
 * деньги. До тех пор домен нужен уже двоим: профиль провайдера ссылается на
 * таблицу отпечатком, разрешение владельца — тем же отпечатком в потолках.
 *
 * Почему не литерал в каждом из них: два литерала одной спецификации
 * расходятся молча, а разойдясь, дают два разных домена с одинаковым
 * названием в коде. Почему не экспорт из профиля: публичная поверхность
 * профиля утверждена и от появления второго потребителя расти не обязана.
 */
export const MODEL_PRICING_SPEC = 'poi-model-pricing/v1'
